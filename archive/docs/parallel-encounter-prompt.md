# 并行任务 B：Act 1 遭遇、进阶与容量预审

工作区：C:\Users\19091\Desktop\sts2。你是并行预审 agent，主 M2 agent 正在做公开数据审计和契约。你的报告供主审与 M2 使用，不是正式白名单或验收结论。

先读取 AGENTS.md、spec-v6.md、docs/decisions.md、docs/m1-candidate-batches.md、docs/m2-agent-prompt.md，检查 git 状态。保留现有未提交改动；不得修改共享代码、规格、决策、周计划、M2 文件，不 commit/push，不启动训练，不另启 agent。只写本任务指定的独立报告；如需探针，仅写 gitignored reference/ 下本任务专用目录，不重建或覆盖共享后端。读取当前源码并记录上游提交、关键文件哈希，避免并行变更导致证据漂移。

以锁定后端完整调用链证明“当前模拟器怎样执行”，以一手游戏来源证明“正版应怎样执行”，两者分开。优先本地源码及已有证据；网上查证限于明确未解问题，不重复泛搜数据集。反编译仅按 D13 对具体争议定点仲裁，产物留在 reference/，不得复制进实现。未知项显式标记，不把枚举、注释、单个分支或上游宣称当行为通过。

用户已授权真实初态按样本分批支持遗物、药水、升级及原始进阶难度，但本任务不裁定支持范围、不降低难度、不删除真实内容。静态审计、实际探针、尚未运行的测试计划必须分别标注。报告每项结论给出文件/符号/行号、证据和实现依赖；最后列待主审决定事项与明确缺口。

## 任务与边界

只写 docs/encounter-ascension-audit.md。探针目录 reference/encounter-ascension-audit/。

1. 从 MonsterEncounter、MonsterGroup 初始化、怪物行动与状态结算清点 Act 1 普通遭遇和 Gremlin Nob、Lagavulin、Three Sentries 三个精英。
2. 优先详查现有 Jaw Worm/Cultist/Two Louse，以及 Small Slimes、Blue Slaver、Red Slaver、Looter、Large Slime、Two Fungi Beasts、Gremlin Gang、Lots of Slimes；用源码实际名称映射，别把单个怪物类型混为遭遇。覆盖这些之外的组合列索引及是否待查，不以代表组合推断全部组合通过。
3. 对每个详查遭遇列初始怪物槽位、全过程最大目标索引、死亡空槽与复用、分裂/召唤、多段意图、随机行动条件、特殊公开状态、生成状态牌。非攻击意图不能统一映成未知或零而失去语义。
4. 搜索整个相关初始化/结算链的进阶条件，列 A0–A20 实际改变机制的阈值和等价区间，包括玩家初态/牌组/药水容量影响。不需要盲测21份同机制场景，但不能只审怪物HP就宣布支持进阶。游戏正式规则与后端实现分别标明证据。
5. 对状态牌生成说明是否可能随回合持续增长，推导所需容量或指出不能给有限上界的原因。不要将最大初始敌人数当最大目标索引，不任意用“大数组”解决。
6. 给出 M4 约十种普通遭遇与三个精英的建议覆盖矩阵、依赖和每项测试场景。测试涵盖最大目标、分裂前后、非攻击意图、进阶阈值两侧、状态牌生成和完整战斗；探针未运行不得记为通过。

## 出口

遭遇映射和可达状态表、进阶差异表、容量论证/缺口、意图与观测扩展建议、最小测试清单。只提出建议，不开放白名单、不改环境、不将源码存在写成 M4 完成。完成后提供报告链接和证据，等待主审。

