# Week 4 补充计划：把卡牌整理成模型能使用的数据

修订日期：2026-09-06。面向：刚学到 Week 4 的初学者，以及协助实现的开发 Agent。

**你现在要学会的是：把一局战斗中能看到的信息，准确地整理成数字；再把 Agent 选出的数字，准确地送回游戏。** 本计划在此基础上保留“逐张卡牌 + 所在区域”的改造方向，帮助以后扩卡时少改输入代码。

状态：学习与实施建议稿。本次只修订计划，未实施实体观测迁移、未运行新版本验收，也未修改正式规格。文中指令描述未来实施步骤，不表示已获自动执行授权。执行依据仍为 [spec-v4.md](spec-v4.md) 和 [决策日志](docs/decisions.md)；涉及现行接口的变更须先登记，再同步规格，最后改代码。

## 1. 先知道自己站在哪里

本次核对了现有 wrapper、Python 环境入口、观测契约、决策日志、Week 4 计划和 Gate 记录。结论分开看：

| 项目 | 当前状态与证据 | 对你的意义 |
|---|---|---|
| 战斗环境 | 正式路线已按 D15/D16 使用锁定的 C++ `sts_lightspeed`，Python 做接口包装 | 不必从头重写战斗规则 |
| 现有输入 | `sts/env/wrappers.py` 已有 FlattenWrapper、TokenWrapper；牌堆按 Bash/Defend/Strike 三类计数 | 你有可阅读、可对照的起点 |
| Week 4 基础验收 | `docs/gate-1.md` 记录旧输入版本 Gate 1 通过，最终回归 138 passed | 这是已有文档记录；本次没有重新运行这些测试 |
| 新实体方案 | 尚是本计划中的建议 | 不可把旧版成绩当成改造后的成绩 |
| 学习进度 | 旧记录写有理解验收完成；你现在要求按初学者重新讲解 | 按你现在的理解重新走例子，不要求你凭旧记录跳过学习 |

**原 Week 4 的基本任务已经有交付记录。这份附件是额外的输入表示改造计划，不是原 Gate 1 遗漏的一整周必修课。** 它不能自动重置排期，也不能成为无限打磨基础设施的理由。

## 2. 先看一个完整闭环

```text
C++ 环境算出当前战斗状态
    ↓ 只取允许玩家知道的部分
观测 observation：手牌、牌堆、敌人、玩家、合法动作
    ↓ wrapper 按明确规则整理数字
两种输入：展平的数据 / 保留实体行的数据
    ↓ Agent 决策（现在可以是随机策略，以后才接神经网络）
31 个动作的概率 → 在 Agent 内抽出一个整数动作
    ↓ runner 原样交给环境 step(action)
下一份观测、这一步奖励、是否结束
    ↓ 未结束就重复；结束就停
```

这里有三个不同的“工作”：环境负责算游戏，wrapper 负责整理数据，Agent 负责选动作。runner 负责把一局按步骤运行起来。

例如手牌是 `[Bash, Strike]`，slot 从 0 开始计数。假定两张牌对目标 0 都合法：

- 打第一张 Bash：`0 * 3 + 0 = 0`。
- 打第二张 Strike：`1 * 3 + 0 = 3`。
- 结束回合：`30`。

交换成 `[Strike, Bash]` 后，“打 Bash”就变成动作 3。**牌名没变，执行编号变了。** 所以卡牌特征不用写“我是第 0 张”，但数据行与环境槽位的对应关系必须保留。

这里要分清两个容易重名的概念：

- `environment_target_index`：环境动作编号里的列号，当前取 0、1、2。
- `target_kind`：动作的真实语义，取 `ENEMY` 或 `NO_TARGET`。

无目标卡也占用环境编码列 0，但它的 `target_kind=NO_TARGET`；攻击敌人 0 同样占用环境编码列 0，但它的 `target_kind=ENEMY`，并指向敌人实体 0。程序必须结合卡牌是否需要目标来解析这两个动作，不能把 `NO_TARGET` 当成敌人 0。这些整数编号和合法性直接沿用环境；wrapper 和 runner 不另写一套出牌规则。

一种更直观的备选协议是让列 0 永远表示 `NO_TARGET`，敌人从列 1 开始。但项目预留最多 3 个敌人；若三只敌人都要可选，就需要每张牌 4 列，动作数由 `10×3+1=31` 变为 `10×4+1=41`，并在适配器中把敌人列 1–3 转回后端敌人索引 0–2。当前计划不自动做这项协议迁移；若决定采用，必须先登记决策并同步规格、C++ 适配器、测试、轨迹和未来模型输出维度。

## 3. “逐牌实体改造”到底改什么

### 3.1 把按牌名汇总的表，换成每张牌一条记录

旧版抽牌堆示例，字段顺序为 `[Bash, Defend, Strike]`：

```text
[1, 0, 2] = 1 张 Bash、0 张 Defend、2 张 Strike
```

新方案示例，名称只是帮助阅读，实际类别用注册 ID：

```text
card_id    location    其他已核验的可见属性
Bash       DRAW        ……
Strike     DRAW        ……
Strike     DRAW        ……
```

这里的“实体”就是一张实际存在的牌。“location”就是它在哪：手牌 HAND、抽牌堆 DRAW、弃牌堆 DISCARD、消耗堆 EXHAUST。两个 Strike 必须保留两行，不能去重成一行。

旧版在只有三类牌、没有实例差异的切片里，已经能准确表示牌堆构成，不能说旧版错了。新方案的收益在于：增加一种可用现有字段描述的牌时，数据结构不用再多加一列“某某牌数量”。游戏规则、可见性和旧模型是否能用新牌，仍须分别验证。

### 3.2 为什么要有两种 wrapper

未来要比较 MLP 和 Set Transformer，因此让它们看同一份事实。

- Flatten 路径把各字段按固定布局展开；以后模型可以把类别编码后接到 MLP。
- Token 路径保留“每张牌一行、每个敌人一行”的结构；以后模型再做可训练投影和注意力。

两种输入的形状可以不同，信息必须相同。比如一边知道“抽牌堆有 Bash 和 Strike”，另一边只知道“有两张牌”，就不公平。

本次建议将旧的单一浮点特征数组改为分别保存类别整数、数值、有效位的结构化数据。**这会改变 FlattenWrapper 的返回契约，必须登记迁移。** 数据整理阶段不训练 embedding，也不声称 TokenWrapper 已经是 Transformer。

### 3.3 本轮只需理解的几个词

| 词 | 在这里是什么意思 | 一个例子 |
|---|---|---|
| observation / 观测 | 模型获准看到的战斗信息 | 当前手牌和玩家血量 |
| schema / 字段契约 | 约定每个字段是什么、类型是什么 | `card_id` 是类别整数 |
| registry / 注册表 | 后端类别与输入 ID 的集中对照表 | Bash 对应一个稳定 ID |
| token / 实体行 | 一个对象的一组输入特征 | 一张 Strike 的记录 |
| padding / 补齐 | 为固定数组长度加入的空行 | 只有 2 张手牌时余下的空位置 |
| entity mask / 实体有效位 | 告诉程序哪些行是真的对象 | 空手牌位置为 False |
| action mask / 动作合法位 | 告诉 Agent 哪些动作现在可执行 | 能量不足时某些出牌动作为 False |
| embedding / 类别向量 | 模型里可训练的类别查表结果 | 模型学习某张牌的表示；以后再实现 |

真实零费用卡仍是有效实体，不能因为数值为 0 就当空行。实体 mask 与动作 mask 是两个不同问题：一张真实存在的牌，也可能因能量不足暂时不能打。

## 4. 把任务放回正确的学习阶段

| 阶段 | 做什么 | 完成时要理解什么 |
|---|---|---|
| 现在先复习 | 用真实观测走一遍现有闭环 | 谁算游戏、谁整理数据、谁选动作 |
| 本次补充改造，登记后实施 | 逐牌记录、location、注册表、同源双 wrapper、动作接入与回归 | 信息如何保留、隐藏信息如何排除 |
| Week 5–6 | 依原规格安排扩容、规则 Agent、评估 | 规则策略和随机策略如何公平比较 |
| Week 7–8 | 接 MLP + PPO；若采用新类别接口，在 MLP 内接 embedding/属性投影 | 模型参数如何更新，策略如何学习 |
| Week 11–12 | 手写 QKV、注意力、ISAB、PMA、实体动作评分 | 实体之间如何交互，换位为何影响对应动作而不改变全局含义 |
| Week 14–16 | 离线轨迹与 DT，落实历史动作语义与因果检查 | 用过去预测现在，不能读取当前答案或未来信息 |

相对旧附件，本版把 embedding 前向/反向、可训练 NO_TARGET 评分模块、DT 专用解析器及序列化建设移回对应模型/数据阶段。现在保留动作映射与旧观测快照的正确性；不要求你本周掌握 PPO、注意力公式或 DT。

这意味着未来实施前应调整 D5/D22 的输入表示约定；D21 继续保持“wrapper 只做确定性转换”的边界。若 MLP 阶段接类别 embedding，应登记其用于 MLP 的时点。具体动作头仍按 D17 在模型阶段落实。

## 5. 一次只完成一个可看见的步骤

下面统一使用 P0–P5 编号，避免与已有 Week 4 的 T1–T6、旧附件的 T0–T7 混淆。全部是待执行计划，不能从旧文档勾选状态推定完成。

参考总预算 12–18 小时，包含讲解、实现、检查；不是对运行时长的保证，也不另开一个自动顺延周。实际投入必须落在原 §9/§10 的排期内；单一问题连续超过 3 天无进展，执行 §10.3。若改造与主线冲突，记录未完成范围并遵守原 Gate 路径，不自行延长截止日。

### P0：读懂一份现有输入（1–2 小时）

当前状态：**已完成（2026-09-06）**。已用训练 seed `100000` 的双虱初态走通一次真实动作，并完成环境列 0 与 `NO_TARGET/ENEMY_0` 两层语义辨析。

Agent 做：从训练范围 seed 运行一个真实初态，展示手牌、抽牌/弃牌计数、玩家和敌人字段、合法动作。沿当前 `lightspeed.py → wrappers.py → Agent → episode_runner.py` 找到数据流，不先改代码。

你做：任选一张牌，说出它的数据在哪一行、可以选择哪些动作；指出 seed 为什么只用于复现，不能送进模型。

产物：一份带字段注释的真实样例和一个动作编号演算。可以直接放在改造报告里，不新增一堆教程文件。

完成标准：你能解释一次 `step` 的输入与输出。**下一次学习从这里开始即可。**

### P1：写清新记录的字段，再登记变更（2–3 小时）

当前状态：**已完成（2026-09-06）**。D24、`spec-v4.md` 和 `docs/observation-contract.md` 已同步；P2、P3 已依照该契约完成正式后端与双 wrapper 迁移。

Agent 做：对照现有观测白名单设计逐牌记录，列出每个字段的来源、意义、dtype、有效区域、缺失处理、缩放规则；区分 card ID 与槽位。给出旧字段到新字段的映射，然后先登记决策，再同步规格和观测契约。

建议最小结构：

```text
CardRecord
  card_id       稳定的卡牌类别 ID，不是每张牌的唯一编号
  location      HAND / DRAW / DISCARD / EXHAUST
  categories    现有已核验的离散属性
  numeric       现有已核验的可见数值
```

你做：把 `draw=[1,0,2]` 手工展开成三张记录；把其中一张 Strike 移到 DISCARD，指出哪些字段和数量应该变化。

产物：字段表、注册表约定、schema 版本、容量和动作映射约定。

完成标准：每个字段都有明确来源；不凭后端“读得到”就认为玩家“看得到”。已有 `[未核实]` 项保留标记与限制，新未核实字段不自动加入输入。

### P2：让正式环境真正导出这些记录（3–4 小时）

当前状态：**已完成（2026-09-06）**。项目 C++ 适配层、pybind 暴露与 Python 规范观测已迁移到 V2；锁定扩展重新构建成功，针对 C++ 接口和 Python 薄包装的回归为 `39 passed`。P2 当时的完整套件快照是 `110 passed, 28 failed`，准确记录了 P3 尚未迁移的阶段断点；这些失败现已在 P3 全部解决。

Agent 做：修改项目 C++ 观测适配层、绑定和 Python 规范化入口，按现有锁版构建流程更新项目补丁。游戏结算和 RNG 消耗不变；不能只把旧计数在 Python 中展开，就宣称完成了可扩展的后端导出。

手牌保留当前槽位顺序；非手牌区域只按已公开字段确定性排序。重复牌保留重复记录。EXHAUST 在当前真实切片为空，这不表示消耗机制已验证。

你做：看同一局打牌前后的两份记录，追踪一张牌所在区域和各区域数量。

产物：来自正式 C++ 路径的新观测样例。

完成标准：记录与数量一致，推进游戏不会改写已经保存的旧观测；隐藏抽牌顺序不进入输出。

### P3：同一份记录，整理成两种输入（2–3 小时）

当前状态：**已完成（2026-09-06）**。两个 wrapper 共用唯一的确定性准备函数；结构化 `FlatInput` 与 `TokenInput` 可逐字段互相恢复，验收序列化已升至 V2。完整套件由 P2 阶段断点的 `110 passed, 28 failed` 恢复为 `145 passed`。

Agent 做：共用一个确定性预处理函数，输出类别整数、数值、实体有效位和环境动作 mask；分别按固定布局展平或保留实体维度。迁移所有依赖旧返回类型的调用点和测试。

输入整理只用 NumPy 等现有依赖即可。卡牌 ID 是查表用的类别标签，不能直接作为有大小关系的连续数值；实际可训练查表到模型阶段再接。

你做：在两种输出中找到同一张 Bash，检查其区域、属性和数量是否一致。不要用“数组长度相同”来证明信息相同。

产物：两份能追溯回规范记录的输入及对应关系表。

完成标准：相同观测重复转换相同；有效信息不丢；补齐行清理一致；wrapper 不读取 `info`、seed 或模型参数。

### P4：验证仍能出牌，并做第四类卡探针（2–3 小时）

当前状态：**工程已完成（2026-09-06）**。真实双 wrapper 闭环与动作路由回归通过；测试专用第四类卡仅扩 registry 与观测数据即可经过四个区域和两条输入路径。专项合计 `48 passed`。第四类卡不进入正式 reset。

Agent 做：接回原 AgentDecision 和 runner，通过新双 wrapper 跑到终局。用不同的测试牌/敌人验证槽位与目标路由；无目标动作保持编码列 0，结束回合保持 30。

额外造一种只使用现有字段和目标类型的“测试卡”，加入测试注册表并放入各区域。只改测试数据和注册条目，数据整理层应能处理它。它不进入正式 reset，不代表真实游戏已支持一种新机制。

你做：先预测 `[Bash, Strike]` 换成 `[Strike, Bash]` 后“打 Bash”的编号，再看测试结果；解释为什么第四类测试卡通过不等于模型已经会用它。

产物：真实闭环结果、字段等价结果和合成扩展探针结果。

完成标准：整数动作原样到达环境，非法概率为 0，runner 不二次抽样、不重算合法性，终局不再调用 Agent。本步不实现可训练动作评分网络。

### P5：验收、记录、停止扩展（2–3 小时）

当前状态：**工程已完成（2026-09-06）**。实体版 10000 配置 × 四路径 × 三轮、60 场成对确定性重放及完整回归通过，最慢 Token 路径中位数约 3790 steps/s。独立结果见 `docs/entity-foundation-report.md`。学习复盘不伪造为用户已作答，可在后续具体扩卡例子中继续巩固。

Agent 做：保留既有关键回归，对新版本重新运行适用的确定性、10000 场稳定性和分路径性能检查。复用现有验收入口并明确其适配改动，不能照抄旧版通过数。

你做：读报告中的一个成功例和一个边界例，例如“第四类数据可进入”与“超容量明确报错”，解释分别证明什么。

产物：`docs/entity-foundation-report.md`，实施时有真实结果再创建，包含版本、实际文件改动、精确命令、通过/失败/跳过/未执行、样例与限制。

完成标准：新输入路径有自己的证据；你能独立解释下节问题。随后回到原排期的 Week 5–6 主线，不直接跳过规则 Agent 和评估去训 PPO。

## 6. 用六个问题检查理解

先自己回答，再展开参考。学习时一次讲一个问题；答不清就回到具体数据，不需要背术语。

<details>
<summary>1. 两张 Strike 为什么不能只留一个 ID？</summary>

因为剩下一张和剩下两张不是同一个牌堆。实体记录需要保留重复数量；若选择计数表示也必须明确保存数量，不能无声去重。
</details>

<details>
<summary>2. “抽牌堆有 Bash”与“下一张抽到 Bash”有什么差别？</summary>

前者是无序构成；后者泄露当前设定下不能使用的真实抽牌顺序。非手牌记录按公开字段规范排序，不按后端真实顺序输出。
</details>

<details>
<summary>3. 为什么真实存在的牌也可能不能打？</summary>

实体有效位说明它存在；动作合法位说明现在允许执行什么。二者不能互相替代，合法性由环境给出。
</details>

<details>
<summary>4. 交换两张手牌后，卡牌 ID 和动作编号分别怎样变？</summary>

卡牌身份不变，记录行随牌移动，对应出牌编号随槽位变化。不能只保留集合摘要却丢掉牌与输出编号的对应关系。
</details>

<details>
<summary>5. 一万个随机对局无报错，是否说明 AI 已经学会游戏？</summary>

没有。这说明被测路径在这些样本中运行稳定；随机 Agent 没有学习参数，这也不是全机制正确性的证明。
</details>

<details>
<summary>6. 为什么保存的旧观测不能跟着下一步一起变？</summary>

以后训练需要“执行前状态、所选动作、执行后状态”。旧数组被复用改写会让训练数据失真。现在保证快照独立，DT 专用数据系统以后再做。
</details>

## 7. 给实现者保留的技术约束

这节用于实施核对。初学者第一遍可以先读完 P0–P5，再逐项理解。

### 7.1 输入协议

- 注册表集中、稳定、可版本化；建议 `PAD_ID=0`，有效 ID 与 PAD 分离。已有条目不因排序改变 ID；未知有效 ID 必须报错。
- 原始记录变长；张量按登记容量补齐。手牌 10 槽；当前无生成牌机制的最小切片可建议合并非手牌容量为 10，集中配置、超容量报错，不静默截断。
- **现有代码的敌人观测容量为 5，动作每张牌的目标跨度为 3。** 两者不能混为一谈。本轮建议保留现有敌人观测布局，核对有效实体与合法目标的映射，不擅自改成 51 个动作，也不无说明丢掉两行。原附件“敌人维持 max_target=3”的说法在此纠正。
- 抽牌、弃牌、消耗区按允许字段规范排序，不用对象地址、内部 UUID、创建顺序、真实抽牌索引或 RNG；手牌/敌人维持动作对应。
- 数量从有效记录导出；同 ID 的不同可见属性不能合并。空区域只有零数量和无效行，不沿用旧“零计数 pile token 仍有效”的表示。
- 无效数值行用赋值/选择清零，无效类别设 PAD；不能用 `0 * NaN` 清理，也不能先对无效越界 ID 查表再 mask。有效行 NaN/Inf 明确报错。
- 两路径共用字段、dtype、类别映射和缩放；类别不连续归一化，不用评估集拟合统计量。检查信息等价发生在可训练压缩之前。
- 更改容量可能改变 MLP 输入宽度，更改词表会影响未来 embedding；版本不匹配显式拒绝或走明确迁移，不承诺旧权重自动兼容。

### 7.2 动作、奖励与范围

- 动作仍为 `slot * 3 + target`，结束回合 30，无目标卡仅列 0；合法 mask 由环境给出，在 softmax 前施加，训练时用掩码后的分布计算梯度。
- Agent 返回 `probabilities[31]` 和内部已抽出的 `action`；runner 原样转交，终局停止。非终局全非法报错。
- 交换敌人时只交换需要敌人目标的动作列；无目标卡仍列 0。未来模型内使用独立 NO_TARGET 语义，不能把敌人 0 当它的专属目标。
- 普通 MLP 不天然具有集合换位等变性；现在验证输入信息和动作映射，完整 Set 模型的概率/value 换位测试留到模型阶段。
- 正式任务维持 A0 Jaw Worm、Cultist、Louses 与当前三类牌。测试卡不扩大正式机制；遗物、药水、升级、完整 run 不进入本轮。
- 保持当前奖励、γ=1、硬超时口径与 A/B 回报一致；不修改 `eval_seeds.json`，训练 seed ≥100000，评估 seed ∈[0,1000)，每个 run 记录评估文件哈希。

### 7.3 本轮验收清单

| 检查 | 必须观察到的结果 |
|---|---|
| 注册 ID 与第四类合成卡 | 已登记类别可经过双路径；未知有效类别报错；不增加按牌名特判 |
| 重复牌、区域与可见属性 | 重复数量、location、同 ID 属性差异保留；真实轨迹数量与区域移动一致 |
| 隐藏顺序与调试信息 | 固定可见状态，仅扰动隐藏值，模型输入不变；导出不消耗 RNG |
| padding、空区域、容量 | 无效垃圾不污染输出；有效零值不误删；超容量明确失败 |
| 双路径信息等价 | 按布局恢复的允许字段、有效位、数量、动作 mask 对应一致 |
| 执行动作与快照 | 手牌/目标路由正确；无目标语义独立；step 不改旧 obs；终局停止 |
| 正式 C++ 回归 | 真实两种 wrapper 均能打完；关键结算、确定性、稳定性与奖励检查有新结果 |
| 吞吐 | 分开记录后端、含导出/wrapper/随机 Agent 的路径；不能用旧计数版速度冒充实体版 |

Gate 1 仍按现行规格 §5.1/§10：有限日志校准记录、关键结算回归、固定 seed 确定性、10000 场随机稳定性、单核吞吐至少 2000 步/秒。具体场数和各路径覆盖写清；新增数据检查是迁移验收，不另造 Gate。原有日志差异与未验证范围继续保留，不扩建全机制对拍。门槛失败照原降级规则处理。

### 7.4 以后必须落实，但不作为本周欠账

- MLP 接入：类别 embedding、数值投影、有限梯度与保存重载检查；模型参数归模型所有。
- Set 接入：真实网络的手牌/敌人换位测试、NO_TARGET 评分、padding 与空区域数值稳定性。
- 数据阶段：保存动作前的合法 `obs_t` 与整数 `action_t`，记录奖励、终止标志、episode 边界及 schema/registry 版本；再从当时观测解析所选牌与目标，不能去下一步手牌找旧槽位。
- DT 接入：历史动作语义输入来自当时选中的实体；当前标签仍是动作整数。从截至 `s_t` 的上下文预测 `action_t`，不能读当前已选动作或未来状态。固定合法前缀和 RTG 后改变未来 token，当前预测不变。评估 RTG 用预设目标减去已发生奖励。

这些是阶段待办，不声称网络已实现，也不要求本次为它们建空模块。

## 8. 实施时的阅读入口与同步范围

| 文件 | 用途 |
|---|---|
| [spec-v4.md](spec-v4.md) | 当前范围、周次、Gate 与止损规则 |
| [docs/decisions.md](docs/decisions.md) | D5、D16–D23；先登记表示迁移，不能覆盖旧证据 |
| [docs/observation-contract.md](docs/observation-contract.md) | 现有字段、shape、可见性与动作约定 |
| [sts/env/lightspeed.py](sts/env/lightspeed.py) | 正式 C++ 到 Python 的观测入口 |
| [sts/env/wrappers.py](sts/env/wrappers.py) | 当前双 wrapper 与共用编码 |
| [sts/agents/masked_policy.py](sts/agents/masked_policy.py) | 掩码概率与决策接口 |
| [sts/agents/episode_runner.py](sts/agents/episode_runner.py) | 环境闭环 |
| [docs/week-4-plan.md](docs/week-4-plan.md) | 既有 Week 4 教学拆解和进度 |
| [docs/gate-1.md](docs/gate-1.md) | 旧输入版本验收记录，不能代表新版本 |
| [docs/week-4-t5-results.md](docs/week-4-t5-results.md) | 既有压力测试与性能口径 |

实施同步范围：先 decisions，再 spec §3/§4.2 的实体输入及 §9 的阶段说明，再 observation contract；构建补丁、依赖调用处、测试与结果报告随后更新。动作语义和 DT 说明中的歧义按阶段登记，不趁机改奖励或全部排期。尊重已有未提交文件；不自动 push、发布或升级依赖。

本次编辑保留用户指定的原文件名，以免已有引用失效；实施时新增文件使用英文小写 kebab-case，代码注释与文档用中文。

## 9. 本计划的证据边界与修订说明

[L1] 已核对本地 `spec-v4.md`、`docs/decisions.md`、`docs/observation-contract.md`：项目一手规格，2026-09-06。用于确认正式 C++ 路线、信息边界、阶段安排与接口红线。

[L2] 已核对本地 `sts/env/lightspeed.py`、`sts/env/wrappers.py`：项目一手实现，2026-09-06。用于确认当前计数表示、类别编码、手牌/敌人观测容量与双 wrapper 起点；未由本次阅读推定运行正确。

[L3] 已阅读 `docs/gate-1.md`、`docs/week-4-t5-results.md`、`docs/week-4-plan.md`：项目已有验收与学习记录，2026-09-06。其中测试次数和通过结论是既有记录，本次未复跑。

本次主要修订：增加初学者闭环与逐步练习；将模型/DT 实现移回原阶段；纠正 5 行敌人观测与 3 个动作目标的混淆；区分旧验收、新方案和实际授权；保留逐牌扩展、同源信息、隐藏顺序、动作对应及新版本实测要求。原附件中的外部引用核验日期与附件哈希不作为本次已核验事实续写；本计划不新增论文性能或游戏机制数值主张。
