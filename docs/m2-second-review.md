# M2 第二轮主审与定点收口任务

日期：2026-09-11。裁定：本轮修复取得实质进展，但 M2 尚未完成，不进入 M3。下一轮集中解决入口 A 的可构建性，不重复泛搜数据。

## 独立核验

- 主审实跑审计专测：23 passed；完整回归：301 passed；规格检查：PASS，749行。
- 主审对真实 JSONL 进行内存变异，原文件未改：HP=-50、删除首个 draw_pile、增加 UNKNOWN_AUDIT_RELIC 均被 excluded，原因码正确。原始首场保留为 visible_entry_verified_internal_state_unproven。
- R1 的原始反例和状态降级通过本轮复核；这不等于所有输入域均已穷尽验证。
- R3 已诚实登记内部状态未证明；R4 补充了容量、状态字段和含剩余预算观测的50回合提案；R5 未将 Pen Nib 静态疑点当成已确认缺陷。这些可以保留，但仍不是可直接实施的完整契约验收。

## 剩余核心问题

### S1：入口 A 仍存在无条件排除，R2 未实质收口

`audit_summary_early_reconstruction()` 无条件追加 RELIC_ENTRY_COUNTER_MISSING、POTION_ENTRY_INVENTORY_MISSING、ENCOUNTER_INSTANCE_STATE_MISSING，并固定返回 not_reconstructable_from_summary。遭遇证据的 missing 仍包含 initial intent 和 initial HP。

入口 A 的目标是用真实构筑和玩家入场状态重新初始化战斗；敌人初始 HP、意图和实例标识可由已核实的遭遇生成规则与独立环境 seed 生成。它们未被原日志记录，不能单独成为 A 的不可构建理由。遗物也必须逐件判断是否有动态计数，不能把无计数遗物统一判成缺 counter。

主审实际读取本地12份原始 summary，第一场均在 floor 1。其中 sample-007.run 的 Neow 为 HUNDRED_GOLD / NONE，neow_bonus_log 的卡牌、遗物增删为空，HP变化为0。这是比复杂 post-init 恢复更合适的定点研究对象，但当前并未证明其可构建。

### S2：A/B 两种输入契约尚未分开

当前契约列出两种 entry_timing，却仍把 piles、已执行效果等统一列为必填，canonical 又固定 B。内部状态审计提出退回 A，但没有给出从 B 到 A 时 HP、Neow counter、开战效果等字段的逆向证据。

必须区分：原生 A 来源；具有逐字段证据的 B→A 转换；不能转换的 B。不能直接把 post-init counter/HP 当成 pre-init 输入再次初始化。

## 给原 M2 agent 的提示词

请读取并执行本文件，沿用原 M2 权限边界。不要修改正式环境、训练、提交推送或进入 M3，不修改三个并行预审原报告。

1. 优先研究已下载的 sample-007.run 首场。逐字段核实基础牌组/进阶诅咒、角色初始 HP/maxHP 与进阶初始化顺序、Neow 日志完整性和 HP 时点、遗物是否需要 counter、药水初始库存与容量、遭遇标签映射。使用本地来源与锁定后端，只有具体语义缺口才定点核查源代码；引用到路径/行或固定版本，不再泛搜数据集。
2. 每个字段给出 recorded、derived-with-evidence、resampled-by-policy 或 unknown。空药水/无计数遗物可以在有规则及日志完整性证据时推导，不能靠猜。敌人初始 HP/意图等可按 A 初始化策略重采样，不再要求原局实例快照。
3. 修改 summary 判定，按实际字段条件产生阻碍原因，删除无条件的上述三种排除；不强求成功。添加有充分证据的 A 路径测试，以及缺关键玩家字段被拒绝的测试。合成测试不替代真实 sample-007 的字段证明。
4. 若 sample-007 可构建，输出第一份来源驱动的 pre-init 场景清单和字段证据；无需提前实现正式 reset。再对12份本地样本复用相同判定，并记录真实覆盖与缺口。若仍不可构建，精确指出最小缺失字段、已查证据及为什么不能推导，不要再次返回通用“缺所有内部状态”。
5. 将契约拆成明确的 A/B 分支：A 的初始化输入与生成字段，B 的完整恢复要求；禁止无证据 B→A。明确当前到底准备实现哪条路径；没有可构建路径时标为研究提案，不能称已锁定的 M3 实现契约。
6. 分开报告“至少一个初态可构建”“正式内容覆盖”“独立 run 的训练/开发/评估划分”。首层单场成功不能代表30–40卡或正式数据集完成。

交付逐项结论、真实场景字段证据、测试结果和最小未解决项，等待主审。保留本轮已通过的反例修复，不重做无关预审。
