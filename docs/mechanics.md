# 结算顺序规格（mechanics.md）

> 地位：本文档是结算顺序的**书面规格**，`tests/test_ordering.py` 的唯一依据。
> 单元测试照着本文档写，**不是**照着代码写——否则测试只会验证你实现了什么，不会验证你实现对了什么（v4 执行说明）。
> 与 runlogger 日志对拍冲突时：以日志为准，修订本文档并在文末"变更记录"登记。
> 适用范围：v4 §4.1 **第一档最小切片**。第二档扩容时另起章节。
> 数值与 AI 策略未经对拍核实处一律标注 `[未核实]`，不得当作既定前提（v4 附 B）。
> 版本：v0.1（2026-09-03），回放对拍前的工作稿。

---

## 0. 术语与约定

- **回合** = 一个完整循环：玩家回合 + 敌人回合。回合数从 1 起。
- **实体** = 玩家或敌人。
- 状态效果以**剩余回合数**计（Vulnerable 2 = 还有 2 个递减周期）。
- 牌库为列表，**列表尾 = 牌库顶**。
- 手牌槽位 0-9，**按抽牌顺序分配**（v4 §4.3 关键约定：槽位分配必须确定，否则 replay 与 oracle 对拍失败）。
- 所有 `[未核实]` 项的共同出处：社区共识数值 / 记忆，未经 runlogger 日志或游戏内实测确认。

## 1. RNG 流（v4 §3 / Gate 1 第 3 条）

主 seed 派生 4 条**独立** RNG 流（numpy `PCG64`，经 `SeedSequence` spawn），流之间不得交叉取数：

| 流            | spawn key | 用途                      |
| ------------ | --------- | ----------------------- |
| `shuffle`    | 0         | 洗牌、抽牌顺序                 |
| `enemy_ai`   | 1         | 敌人行动选择、初始意图             |
| `enemy_roll` | 2         | 敌人 HP roll、Louse 每场咬伤定值 |
| `misc`       | 3         | 未来扩展保留                  |

- seed 只进环境，**不进观测**（v4 §3：防学到伪相关）。
- 每个随机操作的**消耗次数与顺序**是可复现性的一部分：同一 seed + 同一动作序列，流的消耗计数必须逐步相同（Gate 1 第 3 条按此断言）。
- `[未核实]`：STS1 对固定首行动（Jaw Worm 必 Chomp）是否消耗 RNG。实现按"消耗一次"约定，回放对拍时校准。

### 1.1 游戏本体的真实 RNG 结构（2026-09-03 已核实）

游戏并非单流，而是**比本规格更彻底的多流设计**。反编译代码 `generateSeeds()`（ForgottenArbiter《Correlated Randomness in Slay the Spire》，2026-09-03 核验）显示 STS1 维护 12 条命名 RNG，**全部用同一个 run seed 初始化**：

- 全程持续：`monsterRng` / `eventRng` / `merchantRng` / `cardRng` / `treasureRng` / `relicRng` / `potionRng`
- **每层战斗重置**到同一初始状态：`monsterHpRng` / `aiRng` / `shuffleRng` / `cardRandomRng` / `miscRng`

独立性的来源不是不同种子，而是**独立的 `java.util.Random` 实例、互不干扰的消耗序列**。

对本项目的三层含义：

1. **多流设计是对游戏结构的映射，不是自创**。对应关系：`shuffleRng` ↔ `shuffle`、`aiRng` ↔ `enemy_ai`、`monsterHpRng` ↔ `enemy_roll`、`cardRandomRng`/`miscRng` ↔ `misc`（最小切片无随机卡牌来源，未单列）。实现上我们用不同种子 spawn 派生替代"同种子不同实例"，独立性保证等价。
2. **战斗内随机性 = f(run seed, 层数)**，与路径历史无关（那 5 条流每层重置）。第 3 周对拍只需日志提供 run seed 与层数，即可重建该场战斗的全部随机序列——无需重放整局。
3. 若要逐字节复刻游戏随机序列（Gate 1 第 1 条的完整形态），需精确复刻 `java.util.Random`（48-bit LCG，约 30 行；`sts_lightspeed` 与 SlayTheSpireFightPredictor 已验证此路线可行）。替代路线：日志注入（把日志中的 HP roll / 洗牌顺序作为固定输入喂给模拟器，只测转移逻辑不测 RNG）。第 3 周二选一，届时登记。

`[未核实]`（沿用）：固定首行动是否消耗 RNG；Louse 咬伤定值与 Curl Up 的 block roll 走 `monsterHpRng` 还是 `miscRng`——第 3 周对拍时反推。

## 2. 战斗初始化 `reset(seed)`

按序执行：

1. 主 seed → 派生 4 条 RNG 流（§1）。
2. 构建牌库（固定构建顺序）：Strike ×5 → Defend ×4 → Bash ×1。
3. `shuffle` 流洗牌一次（Fisher-Yates 全排列）。
4. 生成敌人：按遭遇配置的顺序实例化；每个敌人 HP 在其区间内用 `enemy_roll` 流依序 roll；Louse 的每场咬伤定值同时 roll 并存于实例。
5. 敌人初始意图：按生成顺序用 `enemy_ai` 流决定（Jaw Worm 首回合固定 Chomp，见 §7）。
6. 玩家：HP 80、block 0、能量 3、回合数 = 1。
7. 抽 5 张（§6），手牌槽位 0-4 按抽牌顺序。
8. 状态 = 玩家行动阶段。

## 3. 回合结构（结算主循环，按编号顺序执行）

### P — 玩家回合开始

1. 玩家 block = 0。
2. 能量 = 3；回合数 += 1。
3. 回合开始触发（最小切片：无）。
4. 抽 5 张（§6）。
5. 玩家 debuff **不在**回合开始递减（递减时机见 S2）。

### A — 玩家行动（循环，每次一个动作）

1. 执行一个合法动作：`play_card(slot, target)` 或 `end_turn`。
2. 卡牌结算（§5）。
3. 敌人全灭检查（§X1）→ 全灭则**立即**胜利，本回合后续一切不执行。

### T — `end_turn` 触发

1. 手牌按**槽位升序**依次置入弃牌堆。
2. 玩家 debuff 时长各 -1（Weak）。
3. 进入敌人回合（N）。

### N — 敌人回合（按生成顺序逐个敌人）

对每个**存活**敌人依次：

1. 其 block = 0。
2. 执行其意图（§7）。
3. 其回合结束触发（Cultist：Ritual → 力量 +3）。
4. 玩家 HP ≤ 0 → **立即**判负，后续敌人不再行动（§X2）。

全体敌人行动完毕后：

5. 所有存活敌人 debuff 时长各 -1（Vulnerable / Weak）。
6. 回到 P（新玩家回合开始）。

> **裁定**：debuff 递减时机 = 受影响方回合结束时（玩家在 T2，敌人在 N5）。此约定与"Vulnerable 2 给你两轮 ×1.5 攻击窗口"的公认行为一致（单测 T07/T08）。在最小切片中，"回合末递减"与"回合初递减"两种约定**不可区分**（无中途上 debuff 的来源）；第 3 周对拍若日志能区分，以日志为准修订本条。
>
> **裁定**：敌人 block 清零在**各自回合开始**（N1），非全局统一清零——与 STS1 逐实体回合模型一致；敌人回合内获得的 block 存续到你的回合结束。

### X — 战斗结束

- **X1 胜**：所有敌人 HP ≤ 0，任意时刻判定，立即生效。
- **X2 负**：玩家 HP ≤ 0，任意时刻判定，立即生效。
- **X3 硬超时**：回合数 > 50 判负（config `max_turns`，默认 50；v4 §3：防死锁轨迹污染数据）。
- **X4** 结束即冻结全部状态；奖励计算按 v4 §4.4，不在本文档范围。

## 4. 伤害计算（攻击方 → 目标）

按序执行：

1. `base` = 卡面基础伤害（玩家）或意图伤害 + 当前力量（敌人，见 §7）。
2. 攻击方有 Strength → `base += Strength`（负亦然）。**先加力量**（v4 §5.3 裁定）。
3. 目标有 Vulnerable → `base = floor(base × 1.5)`。
4. 攻击方有 Weak → `base = floor(base × 0.75)`。
5. `base < 0` → 取 0。
6. 结算到目标：**先扣 block，余量扣 HP**。
7. HP ≤ 0 → 死亡（即时；其未执行的意图作废）。

> **裁定**：每步乘法后立即 floor；Vulnerable 先于 Weak 计算。
> **对拍注**：Vuln+Weak 同时生效才可区分两种乘法顺序，而最小切片中**不存在**两者同时作用于同一伤害的情形（玩家无力量来源、敌人无 Weak 来源）——故本裁定在第一档内不可被日志证伪；第二档（Inflame 等）入库前必须完成对拍校准。手算单测按本裁定执行（T06）。

## 5. 卡牌结算（最小切片卡表）

| 卡 | 费用 | 效果 |
|---|---|---|
| Strike | 1 | 对单体目标造成 6 点攻击伤害 |
| Defend | 1 | 获得 5 点 block |
| Bash | 2 | 对单体目标造成 8 点攻击伤害，**然后**施加 Vulnerable 2 |

结算顺序：

1. 扣除能量（先扣费，后结算）。
2. 卡牌离开手牌 → 结算效果 → 置入弃牌堆。
3. 攻击卡执行 §4 流程。
4. **Bash 裁定**：先伤害、后施加 Vulnerable——Bash 自身伤害只吃到目标**已有**的 Vulnerable，不吃自己上的（T16/T17）。`[未核实]` 以对拍为准。
5. 无目标卡（Defend）在动作编码中目标槽固定 0。

`[未核实]`：以上卡面数值为社区共识值，第 3 周对拍时逐一确认。

## 6. 抽牌 / 洗牌

1. 抽 1 张 = 弹出牌库顶（列表尾）。
2. 抽牌时牌库空 → **先**把弃牌堆整体洗入牌库（`shuffle` 流 Fisher-Yates 一次），再抽。
3. 牌库、弃牌堆皆空 → 本次抽牌放弃（手牌可为 0，此时唯一合法动作 = `end_turn`）。
4. 手牌已满 10 → 抽牌失败，牌留在牌库顶。`[未核实]` STS1 规则；最小切片抽 5 不会触发，防御性实现按此条。
5. 洗牌 RNG 消耗次数计入可复现性断言（T12）。

## 7. 敌人规格（最小切片）

> 通用：敌人攻击伤害 = 意图基础值 + 当前 Strength（§4 第 2 步）。敌人可拥有 block（Thrash/Bellow 来源，N1 清零）。

### Jaw Worm（单敌遭遇）

- HP：40-44 区间 roll。`[未核实]`
- 行动表：
  - **Chomp**：攻击 11。`[未核实]`（A2+ 为 12，本项目固定 A0 数值）
  - **Thrash**：攻击 7，自身获得 block 5。
  - **Bellow**：自身力量 +3，获得 block 6。
- AI：首回合**固定 Chomp**；此后行动概率 45% Thrash / 30% Bellow / 25% Chomp，连用同一行动的约束规则 `[未核实]` —— 以日志反推为准（第 3 周任务）。

### Cultist（单敌遭遇）

- HP：48-54 区间 roll。`[未核实]`
- 行动：第 1 回合 **Incantation**（赋予自身 Ritual：**回合结束**力量 +3）；此后每回合 **Dark Strike** 攻击 6。
- 推演锚点：敌 T1 无攻击；敌 T1 末力量 3 → 敌 T2 Dark Strike 9 → 敌 T3 12（T09/T10）。

### Louse ×2（多敌遭遇；红/绿）

- HP：红 10-15、绿 11-17 区间 roll。`[未核实]`
- **Bite**：攻击，伤害值**每场战斗开局 roll 一次**（5-7），存于实例，全程不变。`[未核实]`
- **Spit Web**（绿）：玩家 Weak +2。
- **Grow**（红）：自身力量 +3。
- **Curl Up**：首次受到攻击伤害时获得 block 3-7（每场一次）。`[未核实]`
- 各体行动概率与首行动 `[未核实]`，以日志反推为准。
- 生成顺序 = 动作目标索引顺序（0 = 第一只）。

## 8. 动作空间与合法性（v4 §4.3）

1. 动作总数 `A = max_hand × max_target + 1`，末位 = `end_turn`。配置默认 `max_hand = 10`，`max_target = 3`（第一档实际敌人 ≤ 2；中等档预留 3）。
2. 索引解码：`i < A-1` → `slot = i // max_target`，`target = i % max_target`；`i = A-1` → `end_turn`。
3. 动作合法 ⟺ 槽位有牌 **且** 当前能量 ≥ 卡费 **且** 目标合法（攻击卡：目标存活；无目标卡：target = 0）。
4. 环境直接输出合法动作列表与 mask，模型不猜（v4 §3）。
5. 环境拒绝一切非法动作（Gate 1 第 4 条：随机策略非法动作率恒 0）。

## 9. 遭遇配置（最小切片）

| 遭遇 | 敌人（按生成顺序） |
|---|---|
| `jaw_worm` | Jaw Worm ×1 |
| `cultist` | Cultist ×1 |
| `louses` | Louse ×2（红 + 绿）`[未核实]`：STS1 该遭遇的组成 |

评估时按遭遇类型分桶报告（v4 §6.3）：Jaw Worm 是 race、Cultist 要求快杀、Louses 考多目标分配——总体胜率会把差异平均掉。

## 10. 结算顺序单测清单（→ `tests/test_ordering.py`）

每条由本文档推导手算期望值，**先于实现写好**。编号即测试 ID。

| ID | 规格依据 | 前置与操作 | 手算期望 |
|---|---|---|---|
| T01 | P1 | Defend 后过一整轮 | 新玩家回合开始 block = 0 |
| T02 | N1.1 | Jaw Worm 打出 Thrash → 我方 Strike 打它 | 6 伤先扣 5 block，HP 仅 -1；其下回合开始 block 清零 |
| T03 | §4.2 | Bellow ×2 后 Chomp | 11 + 3 + 3 = 17 |
| T04 | §4.3 | Strike 打 Vulnerable 敌人 | floor(6 × 1.5) = 9 |
| T05 | §4.4 | 玩家 Weak 时 Strike | floor(6 × 0.75) = 4 |
| T06 | §4 裁定 | 玩家 Weak、敌人 Vulnerable，Bash | floor(floor(8×1.5)=12 ×0.75) = 9 |
| T07 | N5 | T1 Bash Cultist，之后每轮 Strike | T1 伤 12；T2 伤 9；T3 伤 6（两轮 ×1.5 窗口） |
| T08 | T2 | T1 敌回合 Louse Spit Web（W2） | 我 T1、T2 Strike 各伤 4；T3 伤 6 |
| T09 | N1.3 | Cultist 流程 | 敌 T2 Dark Strike 9；敌 T3 12 |
| T10 | §7 Cultist | 敌 T1 Incantation | 玩家 T1 结束时 HP 无损 |
| T11 | §6.2 | T1 打光 5 张手牌 → T2 | T2 抽牌先触发洗牌（弃 5 洗回），手牌 5 张 |
| T12 | §1/§6.5 | 同 seed 两次重跑 | `shuffle` 流消耗计数逐步相同 |
| T13 | X1 | Cultist HP ≤ 6 时 Strike | 立即胜；其 Dark Strike 不再执行 |
| T14 | N | Louses 击杀第一只 | 第二只照常行动；死亡者意图作废 |
| T15 | §8.3 | 目标死亡 / 能量不足 | 对应动作非法；`end_turn` 恒合法 |
| T16 | §5.4 | 敌人无 Vulnerable 时 Bash | 伤 8（**非** 12），随后敌人 Vulnerable 2 |
| T17 | §5.4 | 敌人已有 V1 时 Bash | floor(8×1.5) = 12；随后 V1+2 = V3 |
| T18 | §0 | 同 seed 两次 reset | 手牌逐张相同（ID 与槽位） |
| T19 | Gate 1 #3 | 同 seed 随机策略 200 步重放 | 状态哈希逐步相同 |
| T20 | X3 | 强制拖到第 51 回合 | 判负 |
| T21 | T1 | end_turn 后检查弃牌堆 | 顺序 = 手牌槽位升序 |
| T22 | §8.3 | 能量 3 打 Bash + Strike | 余 0；再 Strike 非法 |
| T23 | §6.4 | 构造手牌 10 张再抽牌 | 牌留牌库顶（防御性条目，可暂 skip） |
| T24 | X2 | 双 Louse 围攻下玩家 HP → 0 | 立即负；第二只不再行动 |

第 3 周结合日志反推补充敌人 AI 侧条目（目标 20-30 条，本表为骨架 24 条）。

## 11. 变更记录

| 日期 | 变更 | 触发 |
|---|---|---|
| 2026-09-03 | v0.1 初稿 | 开工（第 1 周），待第 3 周回放对拍校准 |
| 2026-09-03 | 新增 §1.1：游戏本体 RNG 结构核实（12 条命名流、5 条战斗流每层重置、同 seed 初始化） | 用户提问触发查证，来源 ForgottenArbiter 博客反编译代码 |
