# 卡牌输入最终复核与合并结论

日期：2026-09-13。工作区：sts2-full-card；分支：codex/ironclad-full-expansion。

## 结论

可合并到本轮声明的工程范围：75类战士卡牌、150个基础/升级版本、5张辅助牌；第一幕A20、现有5遭遇、8遗物/15药水。未发现剩余阻塞。未执行合并或推送，不代表正式训练、全组合穷举或形式化等价。

## 最终实现

- B01：临时费用同回合保持、跨回合在消耗堆恢复；合法性和实际支付通过定向验证。
- B02：Havoc不可执行顶牌进入明确消耗区域，耗尽触发保留。最终复核复用了已实现规则，没有新增正版仲裁证据。
- B04：非X负费用归不可主动打出；四区编码保持类别占位语义。
- B05：普通CARD仍115维；公开source_mode/source_will_exhaust/pending_replay_count继续以5维独立候选上下文进入动作头，并补入价值头。价值端对有效候选上下文取均值，排除padding；当前同一选择的上下文相同，普通阶段为零，空候选为零。
- 模型为unified-entity-set-v4；接口仍unified-entity-interface-v4，运行观测v5。2层、64宽、4头、FF128、120242参数。价值头输入变为69维。旧模型权重形状与指纹不兼容，不能精确恢复为本版本。
- 撤销+100的canUpgrade额外过滤及其测试。正常升级规则不变，触及+100后的后端资源上限明确报错；用户已接受此极端限制，不作为合并阻塞。

## B05证据的准确边界

真实手动/自动/重复路径验证了公开字段取值及路由，但不同场景的卡组和公开状态不同，未证明移除上下文后的完整模型输入发生碰撞。保留三字段是公开流程提示的工程选择，不宣称已证明每一字段不可替代。

人工成对编码测试隔离重复次数通路，验证value变化及反向梯度。追加相同候选、候选重排、带NaN的无效padding均不改变value；空候选产生有限value。此测试证明接入与汇总正确性，不证明真实状态碰撞或训练收益。未来若使用候选特有的不同上下文，必须另审价值端汇总语义，不能把本轮的共享上下文约定悄悄改变。

## 当前版本验证

- 定向行为/模型/恢复检查：34 passed。
- 完整CPU回归：950 passed in 37.30s。总数仍950是删除1条违反裁定的+100过滤测试、新增1条价值上下文综合测试，并非新增测试未运行。
- 全150版本随机/规则集成：300局、6665 transition、66次选牌、300自然终止、0截断、0异常；与组合场景合计350局、7897 transition、125次选牌。
- 50局组合：1232 transition、59次选牌、50自然终止、0截断、0异常。
- 150/150准入与spec-v6检查通过。
- C++构建与导入通过；工作副本增量补丁反向检查通过。使用提交中的LF补丁，从锁定后端HEAD在隔离索引顺序应用基础补丁、全卡增量补丁通过。初次直接对CRLF工作副本执行索引检查失败，改用提交原始字节后通过，未放宽匹配规则。
- 32步全卡输入诊断：7次真实选牌，最多103实体，batch32×103，0自然终止、3次采集预算截断；120242参数。有限反向、模型/Adam/Torch RNG保存恢复、下一次更新逐值一致通过。只验证CPU FP32无活动环境更新边界。

产物位于runs/card-token-final-review-20260913：model/report.json、model/update-1.pt、combinations.json、integration.json。旧F5结果保留历史身份，不用旧checkpoint证明本版本恢复。

## 提交与边界

实现提交：1dc281d（F6）。最终文档提交为本文件所在提交。

资源边界未扩大：后端队列、实例编号与实体容量继续保护，容量文件仅因同范围的模型契约与后端变更重绑指纹。未修改原训练工作区，未启动正式长训。后续新敌人/药水、任意真实卡组、完整run及长期成长奖励另行验收。
