# 并行任务 A：遗物与药水后端预审

工作区：C:\Users\19091\Desktop\sts2。你是并行预审 agent，主 M2 agent 正在做公开数据审计和契约。你的报告供主审与 M2 使用，不是正式白名单或验收结论。

先读取 AGENTS.md、spec-v6.md、docs/decisions.md、docs/m1-candidate-batches.md、docs/m2-agent-prompt.md，检查 git 状态。保留现有未提交改动；不得修改共享代码、规格、决策、周计划、M2 文件，不 commit/push，不启动训练，不另启 agent。只写本任务指定的独立报告；如需探针，仅写 gitignored reference/ 下本任务专用目录，不重建或覆盖共享后端。读取当前源码并记录上游提交、关键文件哈希，避免并行变更导致证据漂移。

以锁定后端完整调用链证明“当前模拟器怎样执行”，以一手游戏来源证明“正版应怎样执行”，两者分开。优先本地源码及已有证据；网上查证限于明确未解问题，不重复泛搜数据集。反编译仅按 D13 对具体争议定点仲裁，产物留在 reference/，不得复制进实现。未知项显式标记，不把枚举、注释、单个分支或上游宣称当行为通过。

用户已授权真实初态按样本分批支持遗物、药水、升级及原始进阶难度，但本任务不裁定支持范围、不降低难度、不删除真实内容。静态审计、实际探针、尚未运行的测试计划必须分别标注。报告每项结论给出文件/符号/行号、证据和实现依赖；最后列待主审决定事项与明确缺口。

## 任务与边界

只写 docs/relic-potion-backend-audit.md。探针目录 reference/relic-potion-audit/。

1. 清点锁定后端中 Ironclad 可达遗物和药水的枚举、持有结构、计数、目标和选择类型，形成覆盖索引。清点不是要求全量正版数值复核。
2. 深查以下机制代表：Burning Blood、Neow's Lament（以源码实际ID为准）、Anchor、Vajra、Pen Nib、Happy Flower、Bag of Preparation、Potion Belt、Sacred Bark、Sozu；药水 Fire、Explosive、Block、Strength、Dexterity、Energy、Swift、Fairy in a Bottle、Liquid Memories、Entropic Brew。不存在或命名不同须报告实际映射，不能编造。
3. 沿 GameContext → BattleContext.init → 开战/回合/出牌/受伤/死亡/胜利钩子 → 战后状态同步核查。逐项列：注入必需参数、公开动态计数、影响的观测字段、隐藏内部字段、动作/二次选择、生成依赖和升级交互。
4. 专门检查药水使用/丢弃、目标合法性、被动触发、槽位容量变化，以及药水产生新药水或卡牌后的机制闭包。只给药水观测却没有动作，不能算支持；二次选择不得让后端自动代选。
5. 查明现有 minimal-v1 默认实际拥有的遗物及其是否生效，用构造和结算调用链证明；不能仅凭规格“无遗物”字样下结论。指出胜利HP奖励的取值是在治疗前还是治疗后，作为待主审核对事项，勿自行修改奖励。
6. 为每个深查条目给出最小行为测试场景与预期证据，区分正版已核实数值和后端推导；现有API无法执行时报告接口缺口，不通过修改正式环境造测试入口。

## 出口

覆盖索引、代表机制详表、发现的实际缺口/疑似缺陷、reset与观测/动作所需字段、按依赖分批的建议、可直接实现的测试清单。不要声称整个遗物/药水系统已行为验收。完成后提供报告链接、命令与未解决项，等待主审。

