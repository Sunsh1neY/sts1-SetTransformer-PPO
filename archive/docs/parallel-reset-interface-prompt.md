# 并行任务 C：初态重建边界与接口迁移预审

工作区：C:\Users\19091\Desktop\sts2。你是并行预审 agent，主 M2 agent 正在做公开数据审计和契约。你的报告供主审与 M2 使用，不是正式白名单或验收结论。

先读取 AGENTS.md、spec-v6.md、docs/decisions.md、docs/m1-candidate-batches.md、docs/m2-agent-prompt.md，检查 git 状态。保留现有未提交改动；不得修改共享代码、规格、决策、周计划、M2 文件，不 commit/push，不启动训练，不另启 agent。只写本任务指定的独立报告；如需探针，仅写 gitignored reference/ 下本任务专用目录，不重建或覆盖共享后端。读取当前源码并记录上游提交、关键文件哈希，避免并行变更导致证据漂移。

以锁定后端完整调用链证明“当前模拟器怎样执行”，以一手游戏来源证明“正版应怎样执行”，两者分开。优先本地源码及已有证据；网上查证限于明确未解问题，不重复泛搜数据集。反编译仅按 D13 对具体争议定点仲裁，产物留在 reference/，不得复制进实现。未知项显式标记，不把枚举、注释、单个分支或上游宣称当行为通过。

用户已授权真实初态按样本分批支持遗物、药水、升级及原始进阶难度，但本任务不裁定支持范围、不降低难度、不删除真实内容。静态审计、实际探针、尚未运行的测试计划必须分别标注。报告每项结论给出文件/符号/行号、证据和实现依赖；最后列待主审决定事项与明确缺口。

## 任务与边界

只写 docs/reset-interface-audit.md。探针目录 reference/reset-interface-audit/。

1. 比较两个入口：A 开战前的 GameContext/牌组/HP/遗物/药水/遭遇；B 开战后第一决策点的手牌/牌堆/怪物/状态。沿 BattleContext.init 和后续调用给出字段流与初始化顺序，列哪些由输入决定、哪些随机生成、哪些会触发一次性效果。
2. 回答：汇总 .run 日志缺手牌/敌人意图是否必然不可用？只能基于可验证字段回答。开战前状态完整时，隐藏洗牌/怪物初始化可以重新采样，但必须标为真实构筑和入场状态驱动的重新模拟，不能叫原局精确回放；开战前遗物计数、药水库存等关键字段缺失仍不能默补。未核验字段时点，不得推断完整。
3. 阅读现有公开样本审计脚本和报告（若存在），审查其“不可重建/可重建”判据是否混淆上述两个入口。报告尚未生成不等待，可依据后端和已落盘原始样本开展；不重复主 M2 的数据搜索和下载工作。不从单份详细日志推断所有汇总数据的能力。
4. 分别列 reset 两种入口的必需字段、可再生成字段、拒绝条件。检查开战效果/抽牌重复执行、初始药水容量、遗物计数、卡牌实例动态值、敌方意图内部状态与隐藏历史依赖。详细可见快照不一定足以恢复模拟器的内部状态。
5. 审计当前 bindings → Python规范观测 → registry → Flat/Token wrapper → 模型头 → AgentDecision/runner → 轨迹/checkpoint 中的散落动作和shape常量，给文件/符号清单和依赖顺序。提出集中契约的最小迁移方案，具体动作编号由主 M2 最终裁定，不自建竞争版本。
6. 五敌目标、药水目标/无目标、二次选择、可变牌堆容量和进阶字段分别说明影响的接口；标出旧 minimal-v1/checkpoint 的兼容边界。公开观测相同但内部隐藏状态不同，不应要求把隐藏状态暴露给Agent。
7. 给可实施的验收用例：开战效果只执行一次、reset roundtrip可见字段一致、同场景同复跑seed复现、不同seed允许隐藏状态变化、缺关键字段拒绝、mask与解码一致、最大目标、迁移版本拒绝。对不能用现有接口运行的测试明确标待实现。

## 出口

入口A/B字段与时点对照、汇总数据使用的必要充分条件或明确未知、初始化风险、接口迁移依赖表和测试清单。为主 M2 提供核查意见，不直接修改其报告或划定真实数据可用性结论。完成后提供报告链接，等待主审。

