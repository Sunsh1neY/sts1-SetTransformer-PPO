> **历史学习材料。** 当前学习入口为 [学习路径 v2](docs/learning-path-v2.md)，执行依据为 [spec-v6.md](spec-v6.md)；下文旧 STS2 目标、排期、预算和规模不指导当前实施。

# 学习路径 v1（历史存档）

> 2026-09-08：本文已由 [《STS RL 学习路径 v2》](docs/learning-path-v2.md) 重写替代。
> 新版说明当前 STS1 项目应学到的能力、学习顺序与理解证据。下面保留旧文供历史对照，
> 其中 STS2、26 周、完整 run 等目标和排期均不再生效。执行依据仍为 `spec-v4.md`。

## 以下为旧版原文

# 思维
你的两种思维划分是对的，但对于这个项目，还必须加入第三种：

- **学生思维**：理解原理，建立可迁移的知识结构。
- **工程师思维**：交付可运行、可测试、可复现的系统。
- **研究者思维**：提出可证伪假设，用对照实验判断一个设计是否真的有效。

只用学生思维，容易陷入“PPO、Attention、RL 论文都看懂了，但项目跑不起来”；只用工程师思维，容易变成“调用了库、模型也训了，但不知道为什么有效或失效”；缺少研究者思维，则会在偶然的训练曲线里过度解读结果。

最适合你的方法不是把三种思维按天切开，而是采用：

> **工程主线驱动，原理按需下钻，实验结果裁决方向。**

也就是先由工程师确定当前最小可交付物，再由学生思维补齐完成它所需的底层知识，最后由研究者思维设计评估，决定是否继续投入。

例如：

```text
工程问题：
“我需要让 Agent 在战斗中选择合法动作。”

学生下钻：
“我需要理解 categorical policy、softmax、交叉熵、
policy gradient、动作掩码与 logits 的关系。”

工程实现：
“实现 State -> Model -> Masked Action -> Env.step() 闭环。”

研究验证：
“比较无掩码 / 掩码、随机策略 / 规则策略 / 学习策略，
检查非法动作率、胜率、血损和训练稳定性。”
```

不要先完整学完 Transformer、完整学完强化学习、完整读完 AlphaStar，再开始做项目。那样学习路径会无限膨胀。每一项理论学习都应当有一个工程出口。

下面是我建议你直接放进仓库的学习路径文档。它把目标、方法论、时间安排、每周节奏和止损条件放在一起。

# STS2 RL Agent 学习与实施路径

## 0. 项目定位

### 最终目标

在赴墨尔本学习 AI 前，完成一个可复现的《杀戮尖塔 2》Ironclad 自动决策项目：

- 有可运行、可测试、可复现的 headless 战斗环境；
- 支持固定 seed、状态回放、合法动作掩码；
- 有随机策略与规则策略基线；
- 有行为克隆（BC）或 PPO 强化学习智能体；
- 在固定种子评估集上，学习策略显著优于随机策略；
- 最好能达到或超过简单规则策略；
- 有完整实验记录、训练曲线、消融实验和项目报告。

### 非硬性目标

以下内容只在主线稳定后再投入：

- 完整 A0 run；
- A5 或 A10 难度；
- Expectimax / Beam Search 战斗搜索；
- Deck-Map 双塔评估器；
- 多角色；
- 人类高手轨迹大规模行为克隆；
- PBT、历史策略池、复杂奖励塑形；
- 完整真实游戏桥接验证。

---

## 1. 三种工作思维

### 1.1 学生思维：从原理到实现

学生思维负责回答：

- 这个算法在优化什么？
- 数学符号分别对应代码里的什么张量？
- 为什么这个方法会不稳定？
- 哪些假设在当前游戏环境中不成立？
- 这个概念能否用一个最小例子实现和验证？

学生思维的输出不是笔记本身，而是以下至少一种成果：

- 手推公式；
- 最小可运行实现；
- 单元测试；
- 小型 toy experiment；
- 面向未来自己的技术说明。

学习原则：

1. 不为“知识完整性”学习，而为当前工程问题学习。
2. 每学一个核心概念，至少写一个最小实验。
3. 能解释输入、输出、损失函数、梯度流和失败模式。
4. 区分“看懂论文”和“能在代码中定位实现”。

### 1.2 工程师思维：从目标到闭环

工程师思维负责回答：

- 当前最小可交付成果是什么？
- 哪些模块是阻塞项？
- 如何让结果可复现？
- 如何快速定位是环境、模型、奖励还是训练循环的问题？
- 哪些复杂设计现在不值得做？

工程师思维的核心单位是一个垂直切片：

```text
可观测状态
  -> 模型或规则策略
  -> 合法动作
  -> 环境状态变化
  -> 日志与评估指标
```

任何新算法都必须先接入这个闭环，再讨论效果。

工程原则：

1. 环境正确性优先于模型复杂度。
2. 基线优先于 SOTA 设计。
3. 先做可测的小子集，再扩展游戏覆盖范围。
4. 每一个模块都必须有明确输入、输出和测试。
5. 一次只改变一个关键变量。
6. 先解决最影响项目进度的瓶颈。

### 1.3 研究者思维：从假设到证据

研究者思维负责回答：

- 这个设计声称改善什么？
- 与什么基线比较？
- 指标是什么？
- 是否控制了随机种子、训练步数和模型参数量？
- 结果是否可重复？
- 有没有替代解释？

研究者思维的工作单位是一个实验卡片：

```text
假设：
动作掩码能降低非法动作率，并提升 PPO 训练稳定性。

变量：
是否启用 masked softmax。

控制项：
环境版本、模型规模、训练步数、随机种子、奖励函数。

基线：
Random Agent、Rule Agent、无掩码 PPO。

指标：
非法动作率、胜率、平均血损、策略熵、value loss。

判定：
若多个 seed 下非法动作率下降且胜率提高，则保留该设计。
```

原则：

1. 不把单次训练成功当作结论。
2. 不把训练 reward 当作真实能力。
3. 固定种子评估集不参与训练。
4. 每次实验都保存配置、代码版本、随机种子和结果。
5. 结果不显著时，接受“暂时无证据支持”这一结论。

---

## 2. 总体方法论：双环推进

项目采用两个持续循环。

### 工程闭环

```text
定义最小目标
  -> 实现
  -> 测试
  -> 运行
  -> 记录问题
  -> 修复或缩小范围
```

工程闭环关注：

- 能不能运行；
- 接口是否正确；
- 环境是否可复现；
- 训练是否稳定；
- 结果是否可交付。

### 理解闭环

```text
遇到概念或问题
  -> 阅读原理
  -> 手推或复现最小例子
  -> 映射到当前代码
  -> 写测试或小实验
  -> 形成自己的解释
```

理解闭环关注：

- 为什么这样设计；
- 公式如何对应实现；
- 失败时应该查看什么；
- 哪些知识可以迁移到下一个任务。

### 二者的关系

```text
工程目标决定当前学什么；
理论学习决定怎样正确实现；
实验结果决定接下来做什么。
```

禁止的工作模式：

```text
“先把所有理论学完再写代码”
“先把系统写完整再做测试”
“先堆复杂算法再建立基线”
“只根据训练曲线判断算法效果”
```

---

## 3. 时间分配原则

### 默认周分配

项目主线采用以下比例：

| 工作类型 | 时间占比 | 目标 |
|---|---:|---|
| 工程实现与调试 | 45% | 建立可运行闭环 |
| 原理学习与最小复现 | 25% | 建立可迁移理解 |
| 实验、评估与分析 | 20% | 用证据做技术决策 |
| 文档、代码整理、英语与申请准备 | 10% | 沉淀可展示成果 |

如果每周投入 40 小时，建议为：

- 18 小时：环境、模型、训练代码；
- 10 小时：PyTorch、Transformer、RL 原理；
- 8 小时：跑实验、画图、分析失败；
- 4 小时：整理 README、实验日志、英语和留学事务。

### 动态调整规则

不同阶段比例不同：

| 阶段 | 工程 | 学习 | 实验 | 原因 |
|---|---:|---:|---:|---|
| 环境阶段 | 60% | 20% | 15% | 5% | 环境是后续全部工作的前提 |
| BC 阶段 | 40% | 35% | 20% | 5% | 需要补齐监督学习与动作建模 |
| PPO 阶段 | 35% | 30% | 30% | 5% | 训练和实验设计同等重要 |
| 收尾阶段 | 30% | 15% | 40% | 15% | 要稳定复现、分析并展示结果 |

当出现以下情况时，暂停扩展功能，回到工程问题：

- 同一 seed 不能复现；
- 非法动作率大于 0；
- 规则基线行为不可解释；
- 环境状态不能序列化和恢复；
- 训练曲线有结果但无法定位数据来源；
- 新模块没有对应测试。

---

## 4. 学习主线

### 4.1 编程与工程基础

需要掌握：

- Python 数据模型与类型标注；
- `pytest` 单元测试；
- `dataclass`；
- JSON / CSV / Parquet 数据记录；
- Git 分支、提交和实验版本追踪；
- 配置文件管理；
- 日志系统；
- 性能 profiling；
- 可复现随机数管理；
- Python 多进程基础。

工程产出：

- `reset(seed)` 与 `step(action)` 环境接口；
- 状态序列化和恢复；
- 固定 seed 回放；
- 自动化测试；
- 实验配置文件；
- 训练结果目录规范。

### 4.2 深度学习基础

需要掌握：

- 张量 shape；
- 前向传播；
- 自动微分；
- 反向传播；
- 交叉熵；
- softmax；
- categorical distribution；
- Adam / AdamW；
- batch、epoch、learning rate；
- overfitting；
- train / validation / test 划分；
- checkpoint；
- gradient clipping；
- mixed precision 的基本概念。

最小复现任务：

1. 用 PyTorch 在 MNIST 或简单分类数据上训练 MLP。
2. 自己实现 softmax cross entropy 的最小版本。
3. 训练一个模型模仿简单规则策略。
4. 保存、加载模型并复现预测结果。

### 4.3 Transformer 与集合表示

需要掌握：

- Query、Key、Value；
- scaled dot-product attention；
- multi-head attention；
- residual connection；
- layer normalization；
- feed-forward network；
- attention mask；
- positional encoding；
- permutation invariance；
- Set Transformer 的适用场景；
- attention pooling。

本项目中的映射：

```text
玩家状态       -> Player Token
敌人状态       -> Enemy Tokens
手牌           -> Hand Card Tokens
抽牌堆 / 弃牌堆 -> Deck-related Tokens
遗物           -> Relic Tokens
全局状态       -> Global Token

实体 Token 集合
  -> Set Transformer Encoder
  -> Attention Pooling
  -> Policy Head + Value Head
```

最小复现任务：

1. 手写单头 attention。
2. 使用 PyTorch 实现 multi-head attention。
3. 在一个“集合分类”玩具任务中比较 MLP 和 Set Transformer。
4. 用相同训练预算比较 MLP、Set Transformer 在战斗状态上的表现。

### 4.4 强化学习基础

需要掌握：

- MDP；
- state、action、reward、transition；
- episode；
- return；
- discounted return；
- Monte Carlo；
- TD learning；
- value function；
- action-value function；
- policy gradient；
- REINFORCE；
- baseline；
- advantage；
- GAE；
- actor-critic；
- entropy bonus；
- PPO clipped objective；
- KL divergence；
- exploration；
- reward shaping；
- on-policy 与 off-policy 的区别。

建议学习顺序：

```text
Multi-Armed Bandit
  -> Tabular Q-Learning
  -> REINFORCE
  -> Actor-Critic
  -> GAE
  -> PPO
  -> Action Masking
  -> Behavior Cloning + PPO
```

最小复现任务：

1. 在 CartPole 上实现 REINFORCE。
2. 在 CartPole 上实现 Actor-Critic。
3. 在 CartPole 上实现 PPO。
4. 给一个离散动作环境加入非法动作掩码。
5. 将 PPO 接入 STS2 战斗子环境。

### 4.5 实验设计与统计

需要掌握：

- 基线；
- 消融实验；
- 控制变量；
- train/evaluation seed 划分；
- 均值、标准差、置信区间；
- 多随机种子重复实验；
- reward 与真实指标的区别；
- 数据泄漏；
- 失败实验记录；
- 实验可复现性。

每次实验至少保存：

```text
- Git commit hash
- 环境版本
- 模型结构
- 超参数配置
- 训练随机种子
- 评估随机种子
- 训练步数
- 训练时间
- checkpoint 路径
- 胜率
- 平均血损
- 非法动作率
- 训练曲线
```

---

## 5. 阶段计划

## Phase 0：第 1 至 2 周

### 目标

建立项目骨架和最小环境闭环。

### 学生任务

- 学习 Gymnasium 环境接口；
- 学习伪随机数、seed 与可复现性；
- 学习 Python `dataclass`、序列化和 `pytest`；
- 理解状态机与有限状态转移。

### 工程任务

- 初始化 Git 仓库；
- 建立目录结构；
- 定义 `GameState`；
- 定义 `Action`；
- 实现 `reset(seed)`；
- 实现 `step(action)`；
- 实现最简单的一回合战斗；
- 实现状态保存与恢复；
- 建立单元测试。

### 研究任务

验证：

```text
相同 seed 是否产生相同轨迹？
状态保存并恢复后，后续轨迹是否一致？
环境能否拒绝所有非法动作？
```

### 出口条件

- 同一 seed 的整段 action trajectory 完全一致；
- 环境可以完成至少一个最小战斗；
- 状态可以序列化、反序列化和回放；
- 非法动作被环境拒绝；
- 有最少 10 个基础单元测试。

### 止损条件

若两周后仍无法跑通最小战斗：

- 停止研究 Transformer 和 PPO；
- 缩小卡牌、敌人和机制范围；
- 优先完成可验证的战斗子环境。

---

## Phase 1：第 3 至 6 周

### 目标

完成有限机制的战斗模拟器和规则基线。

### 学生任务

- 学习游戏循环、状态转移、动作空间设计；
- 学习 profiling；
- 学习如何设计 deterministic test；
- 学习离散动作空间和 action mask。

### 工程任务

- 实现 Ironclad 基础卡组；
- 实现抽牌、弃牌、消耗、能量和回合结束；
- 实现 3 至 5 种敌人；
- 实现随机策略；
- 实现简单规则策略；
- 实现固定 seed 评估集；
- 实现战斗日志和回放。

### 规则策略示例

```text
优先击杀可击杀敌人；
否则使用高伤害攻击；
若预计伤害较高且有防御牌，则优先格挡；
尽量花完能量；
无可用动作时结束回合。
```

### 研究任务

比较：

```text
Random Agent
vs.
Rule Agent
```

评估指标：

```text
- 战斗胜率
- 平均剩余生命
- 平均血损
- 平均战斗回合数
- 环境吞吐
- 非法动作率
```

### 出口条件

- Rule Agent 稳定优于 Random Agent；
- 指标可自动生成；
- 固定 seed 评估可复现；
- 每个核心卡牌机制都有测试。

---

## Phase 2：第 7 至 10 周

### 目标

完成状态编码、动作掩码与行为克隆基线。

### 学生任务

- 学习 PyTorch 基础；
- 学习 MLP、embedding、softmax、cross entropy；
- 学习分类问题中的类别掩码；
- 理解 logits、mask 和概率分布之间的关系；
- 学习 behavior cloning。

### 工程任务

- 定义数值化状态编码；
- 定义分层动作表示；
- 实现 masked categorical distribution；
- 用规则 Agent 或人工轨迹采集 demonstration；
- 实现 BC 训练循环；
- 实现模型评估与 checkpoint；
- 实现 MLP policy。

### 研究任务

验证：

```text
BC Agent 是否能模仿 Rule Agent？
Masked BC 是否将非法动作率压到接近 0？
BC Agent 的实际战斗胜率是否接近 Rule Agent？
```

### 出口条件

- BC loss 下降且验证集行为合理；
- 模型在环境中可运行；
- 非法动作率接近 0；
- BC Agent 显著优于随机策略；
- 所有训练配置被保存。

---

## Phase 3：第 11 至 14 周

### 目标

完成 MLP PPO 战斗智能体。

### 学生任务

- 学习 policy gradient；
- 手推 REINFORCE；
- 学习 value baseline；
- 学习 advantage 和 GAE；
- 学习 PPO clipped objective；
- 学习 entropy bonus、KL 和 gradient clipping；
- 理解 on-policy sampling。

### 工程任务

- 先在 CartPole 或类似环境复现 PPO；
- 将 PPO 接入战斗环境；
- 实现 rollout buffer；
- 实现 GAE；
- 实现 actor-critic；
- 实现训练仪表盘；
- 保存 policy entropy、value loss、episode return、胜率。

### 训练顺序

```text
Random Policy
  -> Rule Agent baseline
  -> MLP BC
  -> MLP PPO from scratch
  -> BC initialized PPO
  -> PPO with action mask
```

### 研究任务

比较：

```text
PPO from scratch
vs.
BC initialized PPO
vs.
Rule Agent
```

指标：

```text
- 固定 seed 战斗胜率
- 平均血损
- 训练样本效率
- 策略熵
- value loss
- 非法动作率
- 多个随机 seed 的方差
```

### 出口条件

- PPO 可稳定训练，不出现 NaN；
- 学习策略显著优于随机策略；
- 最好接近或超过规则策略；
- 能解释训练失败的主要原因；
- 有至少一次完整对照实验。

### 止损条件

若 PPO 不能超过规则策略：

1. 检查环境转移和奖励是否正确；
2. 检查动作掩码是否导致梯度或采样错误；
3. 检查 reward scale；
4. 在更简单的敌人和卡组上训练；
5. 检查 rollout、GAE、value target 和终局状态处理；
6. 暂不进入 Transformer、搜索和宏观决策。

---

## Phase 4：第 15 至 18 周

### 目标

比较 MLP 与 Set Transformer，并扩展战斗机制。

### 学生任务

- 深入学习 self-attention；
- 学习 Set Transformer；
- 理解 permutation invariance；
- 学习 attention pooling；
- 理解实体 Token 的类型嵌入。

### 工程任务

- 实现 card token、enemy token、player token；
- 实现 typed projection；
- 实现 Set Transformer encoder；
- 实现 attention pooling；
- 保持与 MLP 相同的动作头和训练流程；
- 扩展卡牌、敌人和状态效果。

### 研究任务

比较：

```text
MLP PPO
vs.
Set Transformer PPO
```

控制变量：

```text
- 相同环境版本
- 相同训练步数
- 相同评估 seed
- 相近参数量
- 相同奖励函数
- 相同 PPO 超参数
```

### 出口条件

- Set Transformer 能稳定训练；
- 模型输入不依赖会漂移的手牌索引；
- 结果可与 MLP 公平比较；
- 给出保留或放弃 Set Transformer 的证据。

---

## Phase 5：第 19 至 22 周

### 目标

扩展为有限范围的完整 run，并接入宏观规则策略。

### 学生任务

- 学习分层决策；
- 学习稀疏奖励与长时程信用分配；
- 学习 Monte Carlo return 与 TD return；
- 理解为什么完整 run 的训练比单战斗更难。

### 工程任务

- 实现地图和节点推进；
- 实现基础选牌；
- 实现营火、商店或事件的最小版本；
- 宏观决策先用规则策略；
- 战斗继续使用学习策略；
- 建立完整 run 日志。

### 系统形态

```text
地图路径：Rule Agent
选牌：Rule Scorer
商店：Rule Agent
营火：Rule Agent
战斗：PPO / BC + PPO Agent
```

### 研究任务

验证：

```text
学习型战斗 Agent 是否能提高完整 run 的存活率？
不同规则宏观策略是否影响 Agent 的战斗数据分布？
战斗训练与整局评价之间是否一致？
```

### 出口条件

- 可以完整运行一局有限机制的 run；
- seed 可复现；
- 战斗策略可被替换；
- 可以输出整局胜率和 Act 到达率。

---

## Phase 6：第 23 至 26 周

### 目标

完成评估、报告、演示与项目收尾。

### 工程任务

- 清理训练和评估命令；
- 固化依赖版本；
- 编写 README；
- 固化固定评估 seed；
- 导出训练曲线和结果表；
- 录制 Agent 对局；
- 整理 checkpoints；
- 给关键模块补充测试。

### 研究任务

至少完成以下实验：

```text
1. Random Agent vs Rule Agent
2. Rule BC vs Random
3. PPO from scratch vs BC initialized PPO
4. MLP PPO vs Set Transformer PPO
5. Action mask ablation
```

每个实验报告：

```text
- 环境版本
- 训练步数
- 随机种子数
- 固定评估种子集
- 平均胜率
- 标准差或置信区间
- 平均血损
- 失败模式
```

### 最终交付物

```text
- 可运行代码仓库
- 环境接口文档
- 训练脚本
- 评估脚本
- 基线策略
- 模型 checkpoint
- 实验记录
- 项目报告
- 演示视频或 GIF
```

---

## 6. 每周工作节奏

### 周一：定义本周工程目标

写清楚：

```text
本周唯一核心交付物：
本周需要理解的理论：
本周实验假设：
本周验收指标：
最大风险：
```

示例：

```text
核心交付物：
完成可回放的最小战斗环境。

理论：
随机种子、状态机、Gymnasium step API。

实验假设：
同一个 seed + 同一串动作应产生相同状态轨迹。

验收：
100 次 replay 中状态哈希完全一致。

最大风险：
随机数状态未被保存。
```

### 周二至周四：实现与下钻学习

每天的推荐结构：

```text
2 至 4 小时：工程实现
1 至 2 小时：针对当前阻塞点学习原理
30 分钟：记录问题、提交代码、整理实验日志
```

### 周五：跑实验和排查

重点不是增加功能，而是回答：

```text
这周做的东西是否真的正确？
它是否改善了目标指标？
结果是否能复现？
```

### 周末：复盘与整理

完成：

```text
- 写本周实验结论
- 更新 README 或技术日志
- 清理临时代码
- 确定下周唯一核心交付物
- 记录未解决风险
```

---

## 7. 日常学习规则

### 每个概念使用“四问法”

学习一个概念时，必须回答：

```text
1. 它解决什么问题？
2. 数学上它在优化或估计什么？
3. 代码中它对应哪些输入、张量和函数？
4. 它失败时会出现什么可观测现象？
```

以 PPO 为例：

```text
1. 解决什么问题？
在不让策略更新过大的前提下，提高长期回报。

2. 优化什么？
裁剪后的策略梯度目标，加 value loss 和 entropy bonus。

3. 代码对应什么？
log_probs、advantages、ratio、clip、returns、value prediction。

4. 失败现象？
策略熵快速归零、value loss 发散、KL 激增、胜率不提升、
动作塌缩、NaN。
```

### 每个模块使用“可替换原则”

核心模块必须能被替换：

```text
环境：真实游戏 / Python 模拟器
状态编码器：MLP / Set Transformer
策略：随机 / 规则 / BC / PPO
价值函数：无 / MLP / Transformer
宏观决策：规则 / 学习模型
```

这样实验才有意义，系统也更容易调试。

### 每次遇到失败，按这个顺序排查

```text
1. 环境是否正确？
2. 状态是否完整？
3. 动作是否正确且被合法掩码约束？
4. 奖励是否与真实目标一致？
5. 数据是否正确记录？
6. 训练循环是否正确？
7. 超参数是否合理？
8. 模型结构是否真的需要更复杂？
```

不要反过来先改模型规模或换论文算法。

---

## 8. 必读材料顺序

### 第一批：立即学习

```text
- PyTorch 官方基础教程
- Sutton & Barto《Reinforcement Learning》：
  Chapter 1, 2, 3, 5, 6, 13
- OpenAI Spinning Up：
  Policy Gradient、Actor-Critic、PPO
- Gymnasium 环境接口文档
```

### 第二批：开始 BC 和 Transformer 时学习

```text
- Attention Is All You Need
- The Annotated Transformer 或等价实现讲解
- Set Transformer
- Behavior Cloning 基础材料
- Action Masking in Reinforcement Learning 相关实现和论文
```

### 第三批：PPO 稳定后学习

```text
- PPO 原论文
- GAE 原论文
- AlphaStar 的监督预训练与 RL 微调部分
- DouZero 的 Monte Carlo 训练设计
- OpenAI Five 的训练工程和评估思路
```

原则：

```text
论文服务于当前实现问题。
没有当前工程问题时，不强制精读复杂论文。
```

---

## 9. 项目成功标准

### 最低成功

```text
- 有可复现的战斗环境；
- 有 Random 与 Rule 基线；
- 有可运行的 BC 或 PPO；
- 学习策略显著超过 Random；
- 有固定 seed 评估；
- 有清晰 README 和实验记录。
```

### 良好成功

```text
- BC + PPO 接近或超过 Rule Agent；
- Set Transformer 与 MLP 有可信对照；
- Agent 可完成有限范围整局 run；
- 有真实游戏状态抽查验证；
- 项目结果可复现。
```

### Stretch Goal

```text
- A0 全局策略有稳定胜率；
- 学习型宏观决策优于规则策略；
- Expectimax 和策略网络组合优于纯 PPO；
- 开始 A5 或 A10 难度爬坡。
```

---

## 10. 最重要的原则

```text
先让系统正确，再让系统更聪明。

先做基线，再做创新。

先做短回合战斗，再做完整 run。

先证明一个模块有效，再叠加下一个模块。

先记录失败原因，再调整超参数。

工程负责把问题变小，
学生负责把原理弄懂，
研究负责决定什么值得继续做。
```
```
