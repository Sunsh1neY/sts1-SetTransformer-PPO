# 四组首轮交付接收审核（2026-09-13）

范围：只读核实任务、worker状态及复跑已有定向测试；未合入worker代码，未开放新遭遇。主集成基线f850f6f。各组测试仅代表当前本地worker实际构建，不代表从提交重建已复验。

| 组 | 当前交付 | 本次复跑 | 接收结论 |
|---|---|---|---|
| A | HEAD f850f6f，3个未跟踪探针/构建/测试文件，无a.md及a-fields.md | 3 passed，5.39秒 | 有机制诊断成果，未完成补丁/证据/字段包交接，不可直接准入 |
| B | 7ebeb9c，干净，仅b-fields.md | 无机制测试可复跑 | 字段方案完成；5遭遇机制实现未完成 |
| C | 6fb91a0，干净，有独立补丁、9测试、报告及字段包 | 9 passed，0.43秒 | A0机制诊断可复验；全卡生产接入、生命周期和出口待完成 |
| D | HEAD f850f6f，3个已跟踪修改、2个未跟踪补丁/测试文件，无d.md及d-fields.md | 21 passed，0.52秒 | 有机制诊断成果，中央契约含worker_diagnostic_encounters；合并前须隔离测试开关并审查契约影响 |

合计33项定向测试通过，0失败/跳过；不与旧317直接相加冒充同轮完整回归。22遭遇仍未正式开发准入。

## 字段及审批审查

- 不重复请求已获授权的倒地保留token、targetable分离、复用共享卡牌费用和候选接口、奖励不变等原则；新增字段或真正语义变更才交用户。
- B-LC-4提议复活/阶段转换清空后端意图历史，与现有“直接复用后端设置记录”有冲突。后端前两格参与选招，不可为观察简化擅自清空。建议新实例重置，同实例复活/阶段转换保留后端自然行为；如需另定义观测历史，应单独说明并审批。
- B通用phase词表NONE/PHASE_1/REVIVE_PENDING/PHASE_2尚待审批；可选状态与行为阶段混在一个字段，需说明为何既有公开意图/状态不足。不能把提案视为机制已核实。
- B要求路由事件不进入训练数据过宽：应禁止进入模型语义特征，但恢复候选对应关系所需的不透明路由数据可按共享协议保留。无需把路由generation作为模型输入。
- C推荐新增卡牌region=stasis，现有resolving在NORMAL非空时拒绝；这是真正新增词表值，需用户批准，并明确扣牌与持有铜球之间可见关联的表达，不能仅有区域而丢失返还目标关系。
- C的info.battle_exit包含deck_changes和gold_delta，属环境出口新增字段，待审批；不进token/reward，不含逃跑原因。
- C称Time Warp/Reactive等“已有状态”需区分后端、诊断输出与共享编码器词表；不能据后端有状态就跳过新增词表审批。
- A/D缺完整字段包，尚不能给出全22遭遇一次性字段批准清单。先整理真实缺口，再统一提交。

## 下一步

1. A/D补齐报告、字段包、实际后端补丁和提交；B继续已授权且不依赖字段批准的机制诊断，不能因phase待批停下全部工作。
2. 汇总最小字段申请包；纠正上述历史语义冲突，不擅改现有契约。
3. 用户批准具体字段后，R先落生命周期，再接C依赖；各遭遇走观测/合法动作/结算/回归链条后逐个准入。
4. 当前不直接cherry-pick未审补丁，不推送、主分支合并或训练。本报告未修改worker工作文件。
