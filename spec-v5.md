# STS RL Agent 项目方案 v5

修订日期：2026-09-08。变更登记：[决策日志 D26](docs/decisions.md)。

> **文档状态：已冻结为历史方案。** 当前唯一执行依据为 [spec-v6.md](spec-v6.md)（D27）。下文保留 v5 发布时的原文、进度和历史证据，不再作为新工作的执行指令。
> `spec-v4.md` 冻结为历史方案；v3 及更早版本仍为冻结愿景，STS2 / A10 / 完整 run 不进入排期。
> 学习目标与方法见 [学习路径 v2](docs/learning-path-v2.md)，旧 `学习路径.md` 为历史存档；执行排期与止损分别以本文 §9 / §10 为准。v5 不重置项目起算日、预算和 Gate。

本版重点：先完成可诊断、可复现的最小 PPO+MLP 闭环，再分批扩容；扩容前同时核对观测容量与动作表达能力。正式游戏规则、31 位动作、奖励和评估种子保持现状；未来协议迁移必须另行登记。审核建议对应的条款见附 C。

---

## 0. 项目定位

### 0.1 真实目标

**通过工程实践理解主流 Transformer 技术原理。** 游戏是载体，不是目的；不服务于留学申请或导师匹配。

具体到可验证的理解目标：

- 手写 QKV 投影、multi-head attention、attention pooling，并能解释每个张量形状的来源；
- 理解 padding mask 与 causal mask 是同一机制的两种掩码形状；
- 理解置换不变编码（Set Transformer）与自回归序列建模（Decision Transformer）各自的适用边界；
- 能说清"下一 token 预测"如何等价于"下一步决策"，以及 return-conditioning 在其中多做了什么。

### 0.2 交付物

1. 可复现的 headless STS1 Ironclad 战斗环境（正式训练后端为 `sts_lightspeed` C++，Python 模拟器保留为教学/验证用途），带基础回归与现有真实日志的有限字段校准记录；
2. 随机策略、规则策略两条基线；
3. A 阶段：Set Transformer 状态编码器 + PPO；MLP vs Set Transformer 配对对照实验；
4. B 阶段：Decision Transformer 离线自回归动作预测；
5. A/B 两族模型在同一任务、同一评估集上的对照分析报告；
6. 完整实验记录与一键复现脚本。

### 0.3 明确非目标

不做，且不在项目中途重新讨论：

- STS2（生态不完善，无高质量对局来源，EA 版本漂移）；
- 以反编译游戏二进制为开发主路线；仅允许 D13 的定点规格争议仲裁，产物留在被忽略的 `reference/`，不得照抄进模拟器；
- Ascension 难度爬坡（A5 / A10）；
- 完整 run（地图、商店、营火、事件）；
- 遗物、药水、卡牌升级；
- Expectimax / Beam Search 搜索轨道；
- 卡组-地图双塔评估器；
- PBT、80/20 历史对手池、风格统计量辅助奖励；
- KL 锚定退火（前提是大规模人类 BC 策略，本项目不具备）；
- 真实游戏桥接作为训练通道。

---

### 0.4 当前进度与证据边界

以下为 2026-09-08 方案修订时的文档与产物核对，不是本次重新执行训练或完整回归。后续状态以带配置、源码指纹和原始报告的验收记录更新，不能仅凭文件存在勾选完成。

| 阶段 | 工程状态 | 证据与限制 |
|---|---|---|
| Week 1–4 / Gate 1 | 已有通过证据 | [Gate 1 报告](docs/gate-1.md)；旧输入版本结果只证明当时范围 |
| D24 逐牌实体 V2 | 已完成 P2–P5 | [实体基础报告](docs/entity-foundation-report.md)；2026-09-06 一万唯一配置、四路径各三轮、60 场重放；审核重新核对历史报告，未重跑一万配置 |
| D25 S1 模型契约 | 已完成，审核补边界修复 | 动作批量维度、mask、梯度、注册表与 checkpoint 兼容性 |
| D25 S2 小样本过拟合 | 已完成 | [过拟合报告](docs/mlp-overfit-report.md)：81 样本、33 对同 mask 换位样本、三个初始化各 1500 步，动作与配对准确率均 100%；仅证明固定样本可拟合 |
| D25 S3 PPO | 工程完成（2026-09-08补记） | [首轮训练报告](docs/ppo-minimal-report.md)：262144步、恢复一致、300局开发回报差CI为正；单初始化，value诊断仍保留 |
| D25 S4 / S5 | 待完成 | 多初始化配对诊断、恢复验证与冻结产物不能由 S2 或单次训练替代 |
| 中等档、规则 Agent、Gate 2 | 待完成 | M1 有来源审计，代码扩容在 S5 之后 |
| Set Transformer / DT / A/B 对照 | 尚未进入 | 沿用 §9 顺序与 §10 降级路径 |

审核报告记录的完整回归为 **191 通过、0 失败、0 跳过**；Ruff 当时因未安装而未执行完成，不记通过。小规模性能首次失败、复查通过均保留；复查 `scope=smoke, checks_passed=true, passed=false`，不是新的正式验收。已有日志字段缺失、初态差异仍属有限校准边界，不宣称全机制等价。

同日S3后续实测补记：完整回归259通过、0失败、0跳过；本轮新增代码Ruff通过，PPO首轮
训练及恢复证据见上述报告。此补记不覆盖此前审核时点，也不替代S4多初始化验证。

**工程完成与理解完成分别记录。** 测试或训练通过不自动替用户完成学习复盘；每个学习步骤保留“先给短例子预测，再讲解并验证”，以能解释数据、动作和训练目标为理解出口。

---

## 1. 硬约束

| 项 | 值 |
|---|---|
| 起始 | 2026-09-01 |
| 出发 | 2027-02 初 |
| 自然周 | 约 22 周 |
| 扣除 | 旅游 1.5 周、行前准备 1 周、缓冲 1.5 周 |
| 有效周 | **18 周** |
| 周工时 | **30 小时** |
| 总预算 | **540 小时** |

> v3 假设的 40 小时 × 26 周 = 1040 小时，是本预算的两倍。凡从 v3 继承的排期数字一律无效。

---

## 2. 已裁定决策

| # | 决策点 | 裁定 |
|---|---|---|
| 1 | 时间预算 | 18 周 / 30 小时每周 / 540 小时 |
| 2 | 环境来源 | 正式训练使用 `sts_lightspeed` C++ 战斗后端；规格用 **STS1**；Python 模拟器保留教学/验证用途；现有 `runlogger` 日志用于有限字段校准（D20） |
| 3 | 技术中心 | 路线 C：先 A（Set Transformer + PPO）→ 后 B（Decision Transformer），**两者对照即产出** |
| 4 | 机制范围 | 最小切片 → D25 S1–S5 → 按机制批次扩中等档；扩容先审计容量、可见字段与动作语法，不做完整 Act 1 |
| 5 | 观测/动作表示 | 环境返回结构化 dict；`FlattenWrapper` 与 `TokenWrapper` 同源派生；动作扁平 `(槽位, 目标)` + mask |
| 6 | 版本处置 | v5 为执行依据，v4 冻结为历史方案，v3 及更早版本冻结为愿景；不重置排期 |
| 7 | 奖励 | 当前直接终局奖励：非终局 0，失败及硬超时 0，胜利加剩余血量分，γ=1；暂不实现势能反馈；A/B 共用奖励与累计口径（D18/D20） |
| 8 | 评估协议 | 1000 隔离种子、按遭遇类型分桶、配对 bootstrap 95% CI、附效应量 |
| 9 | 正确性验证 | 保留单测、确定性、动作、稳定性及性能门槛；现有日志有限校准，缺失字段记为未验证；不扩日志工具、不做全机制对拍（D20） |
| 10 | 工具链 | 锁定 CleanRL PPO 来源，Set Transformer 手写，YAML + dataclass；TensorBoard + JSON，源码快照与可恢复 checkpoint |
| 11 | 止损 | 日历闸门 + 指标门槛，谁先触发算谁；降级路径预先写死 |

---

## 3. 从 v3 继承的设计约定

以下与项目规模无关，全部保留：

**状态编码**

- 类型化专用 MLP 投影：卡牌、敌人、玩家各走独立小 MLP 投影到统一 `d_model`；
- 注入 `entity_type_embedding` 与 `location_embedding`（手牌 / 抽牌堆 / 弃牌堆 / 消耗堆）；按 D24，四区卡牌统一为逐牌多重集，抽牌堆、弃牌堆和消耗堆只按允许公开字段确定性排序，不暴露实际抽取顺序；
- **卡牌语义特征替代手牌索引**——索引每回合含义漂移，是已知陷阱；
- 全局池化用 attention pooling (PMA)，不用裸 sum-pooling；
- **seed 不进状态编码**，只保留给环境复现，防学到伪相关；模型状态仅使用 D19 规定的玩家可见信息，排除 RNG 状态、消耗计数及未来随机结果。

**动作与掩码**

- 合法动作列表由环境直接给出，模型不猜合法性；
- 掩码在 softmax **之前**施加，非法 logits 置 `-inf`，梯度用掩码后分布计算（naive masking 有偏）；
- 单场战斗超过 N 回合判负的环境侧硬超时，防死锁轨迹污染数据。

**奖励**

- 当前使用 §4.4 的直接终局奖励，γ=1；势能型辅助反馈暂不实现，未来若引入仍须满足最终成绩与累计一致约束；
- 监控主动送死率，防奖励黑客。

**原则**

- 接口质量是学习成败的一级因素（NLE 复盘教训）。观测/动作参数化写错，后面所有算法工作都在错误物理里优化。

**反面教材（CombatSolver 三条）**

1. 手写评估权重系统性低估长线收益 → 禁止密集手写 shaping；
2. 深度截断处的价值质量决定成败；
3. 特定流派搜索空间爆炸 → 本项目不做搜索，自动规避。

---

## 4. 环境规格

### 4.1 机制范围

**第一档（第 1-4 周，最小切片）**

- Ironclad 初始卡组：Strike ×5、Defend ×4、Bash ×1
- 敌人 3 种：Jaw Worm、Cultist、Louses（多目标）
- 当前 C++ `reset` 限定 A0 及上述三种遭遇，范围外配置直接拒绝；后续扩容时同步开放配置、观测与回归
- 状态效果：Block、Vulnerable、Weak
- 机制：能量、抽牌、弃牌、洗牌、回合结算

**第二档（第 5 周起安排，D25 最小闭环收口后按批次实施）**

- Ironclad 常见卡 30-40 张（含力量体系，如 Inflame、Demon Form 类长线卡）
- Act 1 敌人约 10 种 + 精英战
- 状态效果补：Strength、Dexterity、Poison、Thorns、Artifact
- 不含：遗物、药水、卡牌升级

以上为目标范围，不是已开放清单。30–40 指可用卡类别，不是每局牌组张数；候选卡表、固定牌组、遭遇与状态字段按 [Week 5–6 计划](docs/week-5-6-plan.md) 分批锁定。M1 已完成部分来源审计，十张候选卡的数值核验仍待完成；Hugging Face 数据仅作候选目录，不能自动作为数值真相。

每批按“来源与动作语法核验 → 输入/动作容量契约 → C++ 与规范观测 → 双 wrapper 与旧范围回归 → 相同预算从头短训 → 配对诊断”顺序推进。首次退化停在该批定位；warm-start 可作为补充诊断，不能代替从头训练。卡、敌人、观测和奖励不能在同一实验里无记录地同时改变。

需要二次选择手牌或牌堆对象的卡暂不开放，不能让后端自动代选掩盖动作表达缺口。候选遭遇须核对初始、分裂、召唤后的最大目标索引；当前 Gremlin Gang / Lots of Slimes 暂不开放，容量处理见 §4.3.1。

### 4.2 观测

**信息边界（D19，2026-09-05 用户确认）：只使用玩家可见信息。** 以机制范围内的当前手牌、双方血量与可见状态、敌人显示的意图构造观测；内部状态是否可编码，须依据游戏内可见性逐项核验，不能以“模拟器能读取”为依据。

```python
obs = {
    "hand":         list[CardObservation], # 按环境手牌槽位顺序
    "draw_pile":    list[CardObservation], # 无序多重集的规范排序
    "discard_pile": list[CardObservation], # 无序多重集的规范排序
    "exhaust_pile": list[CardObservation], # 无序多重集；当前真实轨迹为空
    "enemies":      [n_enemy, D_enemy],    # 变长，每个敌人一个 token
    "global":       [D_global],            # 定长：能量、四区计数、玩家 HP/block/buff、回合数
    "hand_mask":   [n_hand],             # 语义示意；张量容量以接口契约为准
    "enemy_mask":  [n_enemy],
    "action_mask":  [A],                   # 合法动作掩码
}

CardObservation = {
    "card_id":     int,   # 项目稳定注册 ID；PAD=0，有效卡从 1 开始
    "location":    int,   # HAND / DRAW / DISCARD / EXHAUST
    "upgraded":    bool,
    "cost":        int,   # 当前公开费用；未知时数值清零并令 cost_known=False
    "cost_known":  bool,
    "target_kind": int,   # 注册表语义：NO_TARGET / ENEMY
}
```

两个 wrapper 从同一份 dict 派生，保证 MLP 与 Transformer 的特征同源，对照实验才干净：

- `FlattenWrapper` → 结构化 `FlatInput`；MLP 在模型内完成类别 embedding/数值投影后 flatten
- `TokenWrapper` → 保留相同字段实体维度的 `TokenInput`；模型内的类型专用可训练投影再产生 `[n_token, d_model]`，给 Set Transformer（D21/D24）

D25 S2 起，在模型内以固定正数尺度调整连续值数量级；费用/能量除以3，HP、block、
total_enemy_hp、intent_damage除以100，回合除以50，牌数及其他层数/次数除以10。
one-hot和类别ID不按大小缩放，不裁剪数值、不用评估集拟合统计量。尺度当前由
`sts/models/mlp.py` 集中声明并随 checkpoint 版本校验；后续 Set 编码器须复用同一份定义，不能复制后各自修改。若抽取共享编码模块，在 Set 接入时实施，不提前堆建框架。

审核的固定初始化探针曾观察到全局 Tanh 饱和比例从 91.67% 降至 0%；这仅说明该样例的初始数量级改善，不证明 PPO 已能学习，不能代替 S3/S4。

观测字段与数据边界：

- **逐牌记录与注册表**：`CardRegistry` 集中维护稳定项目 ID、后端 ID、名称与 `target_kind`；当前固定 `PAD=0, Bash=1, Defend=2, Strike=3`，未知有效后端 ID 报错。每张重复牌保留独立记录；手牌记录行维持动作槽位对应，但槽位号不进入语义特征。
- **补齐可见状态**：卡牌语义及当前费用、玩家能量与格挡、双方可见 buff/debuff 的层数或时长、敌人当前显示的意图与伤害等。已实现且可见的 Weak 等字段不能遗漏；手牌外费用在显示语义核验前使用 `cost_known=False`，未知不能编码成真实 0。具体显示字段与数值的可见性待逐项核验，未核实项标记 `[未核实]`。
- **意图按显示语义编码**：内部招式 ID 须映射为经核验的可见信息；不直接提供当前未显示的伤害 roll、未来招式或未来随机结果。
- **区分牌堆内容与顺序**：D22 已核验玩家可查看抽牌堆、弃牌堆无序构成；D24 将正式输入从三类计数迁移为带 location 的逐牌记录，不提供抽牌堆实际抽取顺序。消耗堆从接口起纳入，当前三张牌下真实轨迹为空；这不表示消耗机制已经验证。
- **调试信息不自动进入模型**：seed、RNG 状态及计数、完整内部牌序可另存供复现和验证使用；编码器只读取观测白名单，训练与评估不能拼入整个 `info`。
- **A/B 遵守同一状态信息边界**：DT 可以读取此前已获得的合法观测、已执行动作和已发生奖励。训练用 return-to-go 可由完整轨迹计算；评估时须使用预设目标并按已发生奖励更新，不能读取本场尚未发生的真实回报。目标回报条件不属于游戏状态观测。

语义层使用四个变长列表；张量准备层固定 10 行手牌，并将三个非手牌区合并到配置化 `pile_capacity` 行，当前最小切片为 10。超容量必须在丢信息前报错。无效行类别使用 PAD、数值清零；有效真实零值不能当作 padding。类别整数不按大小缩放，wrapper 不含可训练 embedding。

验收：固定允许输入的当前可见信息、合法动作及历史，仅改变隐藏值时，编码后的模型输入必须一致；固定目标回报并关闭模型随机性后，动作分布也保持一致。可见信息的完整性另按白名单逐项核对。D22 计数版结果只作为历史输入版本；D24 实体版 C++ 导出、双 wrapper、扩展探针与一万配置验收已完成，证据见 §0.4。未来更改字段或容量仍须重新验证受影响的完整路径，不能借用旧版本通过结论。

### 4.3 动作

扁平化 `(hand_slot, target)` 组合 + mask：

```
A = max_hand * max_target + 1     # 末位 = end_turn
```

不用 factored 双头（选卡头 + 目标头），原因：无目标卡的条件依赖在 PPO 里容易出微妙 bug，在 DT 里要预测多 token。扁平 + mask 让 DT 的 action 天然是单个离散 token，与 next-token prediction 完全对齐。

**关键约定**：动作索引指向**手牌槽位**，槽位分配必须确定（按抽牌顺序），否则 replay 与 oracle 对拍失败。这与 §3 的"卡牌语义替代索引"不冲突——token **特征**用语义（绝不编码槽位号），动作**输出索引**指槽位。

### 4.3.1 扩容前的输入与动作容量契约

当前正式配置如下；观测容量与可选择的动作目标容量是两件不同的事。

| 项 | 当前值 / 规则 |
|---|---|
| 手牌容量 | 10 行，行号映射环境槽位，槽位号不进语义特征 |
| 敌人观测容量 | 5 行；不等于五个敌人都能被动作寻址 |
| 动作目标容量 | 3 列，`target=0,1,2` |
| 动作数 / 结束回合 | `10 * 3 + 1 = 31` / 编号 30 |
| 非手牌合并容量 | `pile_capacity=10`，当前全部卡牌张量容量 20 |
| 输入 / 注册表版本 | schema 2；registry 1；稳定卡 ID：PAD=0、Bash=1、Defend=2、Strike=3 |

反例：把 `slot=0,target=3` 套进当前编码得到 3，环境会解成“槽位 1、目标 0”。补一行敌人观测无法解决。无目标卡使用环境列 0，但模型语义必须独立保留 `NO_TARGET`，不能等同于“敌人 0”。

M1/M2 扩容实施前，使用小型配置/契约集中声明手牌容量、敌人观测容量、动作目标容量、牌堆上界、字段顺序、类别表以及输入/动作/序列化版本。派生动作数和 shape，不在各处独立修改 `31`、`3`、`5`、`20`。当前实现的散落常量是待整理项，本节不声称集中契约已经实现。

容量上界须覆盖生成状态牌及最大目标索引，不能只看起始牌数或存活敌人数。超容量在丢失数据或错误编码前显式报错；禁止静默截断、目标重编号混淆或未知卡回退为 PAD。

若未来采用五目标，则动作数为 `10 * 5 + 1 = 51`，但**本版不直接批准或实施 51 动作迁移**。须先登记决策，再联动 C++ 导出与动作解码、合法 mask、规范观测、双 wrapper、模型头、Agent/runner、checkpoint、轨迹序列化和加载校验；不兼容版本明确拒绝，不能靠 shape 恰好一致放行。

验收必须同时覆盖：最大目标边界、超容量拒绝、完整环境 mask 与 Agent 所见 mask 一致、手牌/敌人换位后的动作对应、旧配置回归及新范围稳定性。具体字段与 dtype 继续以 [接口契约](docs/observation-contract.md) 为详细依据。

### 4.4 奖励

整场最终成绩（D18，2026-09-05 用户确认）：

$$R = \mathbb{1}[\text{win}] \cdot \left(1 + \lambda \cdot \frac{\text{HP}_{\text{剩余}}}{\text{HP}_{\max}}\right)$$

- 当前血量系数 $\lambda = 0.5$；第 5-6 周仅在实际回报分布显示需要时校准一次，**之后锁死**。GAE 的同名 λ 是独立训练参数
- **所有失败同分：正常失败与硬超时判负均为 0**。把敌人打到残血后失败与几乎未造成伤害就失败，最终成绩相同；伤害量和持续回合不另计最终成绩
- 最终成绩 $R$、逐步奖励 $r_t$、累计回报 $G_t$ 分别定义。**A/B 使用同一逐步奖励定义和回报累计口径**，从战斗起点累计的回报须与 $R$ 一致；奖励定义变更后，全部离线数据集回报须重算

当前基础版本直接使用终局奖励（D20）：

- 非终局 $r_t = 0$；胜利终局 $r_t = 1 + 0.5 \cdot HP_{剩余}/HP_{max}$；失败及硬超时终局 $r_t = 0$
- A 阶段的奖励折扣与 B 阶段的 return-to-go 均使用 $\gamma = 1$，$G_t = \sum_{k=t}^{T-1} r_k$，因此从战斗起点累计的 $G_0 = R$
- 任务硬超时是终止状态，按失败记分且不自举：接口返回 `terminated=true`、`truncated=false`、`info.timeout=1`；采样批次截断须与任务硬超时区分，在未结束的战斗中不能伪造失败终局
- **势能型辅助反馈暂不实现**，U8 的基础版本已销账；只有学习实验明确显示需要时才另行登记其折扣、边界和累计方式，并验证仍满足 D18。未来势能差公式应为 $\gamma\Phi(s_{t+1}) - \Phi(s_t)$，不能仅靠这个公式宣称累计目标不变

奖励验收须覆盖不同初态、伤害量和持续步数：所有失败及硬超时轨迹累计回报为 0，胜利轨迹符合 $R$ 公式，A/B 对同一奖励序列累计一致。**实现与测试结果单独记录，本节规定不代表代码已通过验收。**

胜利时保留剩余血量分，使目标回报可以区分不同获胜轨迹；是否改善 DT 的条件控制由 B 阶段实验判断。当前以最小奖励实现推进学习，不为辅助反馈增加前置工程。

---

## 5. 正确性验证

### 5.1 Gate 1 五项检查（第 4 周末，D20 修订）

1. 对现有可用 `runlogger` 片段完成**有限字段校准记录**：列明已比较字段、实际差异、缺失字段与未覆盖范围；缺失或未覆盖项记为“未验证”。不扩充日志工具，不将缺失字段视为匹配，也不因日志覆盖不足阻塞训练或单独触发砍掉中等档
2. 结算顺序单元测试全过
3. 同一 seed 跑两次，轨迹逐字节相同
4. 随机策略 10000 场不崩、非法动作率 0、硬超时触发率极低
5. 单核吞吐 ≥ 2000 步/秒

第 2–5 项继续作为基础门槛。第 1 项的已知差异按其对动作、奖励、确定性与实验结论的影响修复；报告必须区分“已比较且一致”“存在差异”“未验证”，不能把有限字段匹配或无崩溃写成整个模拟器已准确还原游戏。

### 5.2 日志回放的覆盖面处理

人类日志是完整游戏，含未实现的卡、遗物、药水。**按子集过滤**：只比较与当前任务范围和初始化条件相容的战斗片段；对 schema 实际提供且时点可对齐的字段做校准。未记录的 HP、block、buff 等字段不得默认正确，也不补造。

按 D20，当前不新增日志字段或 mod，不开展全机制逐状态对拍。后续更大范围测试用于发现差异，发现后按影响修复；扩大覆盖有助于发现问题，但不能保证所有机制差异都会显现。日志覆盖不足不阻塞基础建设和学习实验，正式报告注明锁定的后端版本与验证范围。

已有 `runlogger` 侦察和有限校准记录见 [决策日志](docs/decisions.md)。当前缺失字段与初态差异仍未充分校准；新增日志版本必须重新核对 schema。`determinismfix` 是否必要仍为 `[未核实]`，不将它列为本项目训练的默认依赖。

### 5.3 结算顺序单元测试清单

优先测试状态机细节，每条从机制规格手算期望值；新增机制批次仍需必要的单卡及接口回归，不以“写错立刻可见”为由省略关键验证：

- Block 在回合开始清零 vs 结算前清零
- 力量加值与易伤倍率的运算顺序（STS 是先加 Strength 再乘 Vulnerable）
- Weak 在回合结束还是回合开始递减
- 抽牌堆空时弃牌堆洗回的时机与 RNG 消耗次数
- 敌人死亡瞬间其意图是否仍生效
- 多目标伤害分配与死亡连锁
- Artifact 抵消 debuff 的优先级

约 20-30 条，优先保留已有且能检查关键结算和接口约定的回归。它们有助于定位已覆盖的问题，不能据此排除所有环境错误；不为凑数量扩建全机制测试。

### 5.4 差分测试（教学与针对性定位）

`sts_lightspeed` 已按 D15/D16 成为正式训练后端。Python 与 C++ 的差分可保留为教学工具，用于定位具体差异；它不是 C++ 后端独立于自身的正确性证明，也不作为进入模型阶段的全机制覆盖前提。

**执行规则**：只处理当前学习或训练实际遇到的差异。比较工具须把解析失败、进程异常、跳过与真实匹配分开报告，不得将失败样本计为通过；不扩建大范围逐状态对拍系统。

> 原第 2 周半天试 build 已由 D15 收口。构建成功不等于全部机制覆盖已验证，范围仍以具体证据为准。

---

### 5.5 正式验收与冒烟检查的证据要求

本节落实已有 Gate 1 / D24 门槛，不新增 Gate。报告必须记录配置版本、实际样本数、各路径/遭遇、性能轮次、确定性覆盖和源码/扩展哈希。

- 正式最小切片验收：10000 个唯一训练配置，raw / dict / Flatten / Token 四路径各三轮；三遭遇各 20 场完整重放。检查异常、完整 mask、非法动作、奖励、硬超时与吞吐；按原验收脚本门槛判定，不临时改阈值。
- 样本不足、确定性样本为零、性能轮次为空、吞吐为 NaN 或非有限值，不得判为正式通过。新增遭遇须补对应覆盖，旧三遭遇报告不能覆盖新范围。
- `checks_passed` 仅表示本次配置的检查通过；正式通过须读取 `passed`。CLI 返回 0 或 `scope=smoke, checks_passed=true, passed=false` 均不能宣称正式验收完成。
- 通过、失败、跳过、未执行/未验证分别报告；缺依赖、没有可比样本或没有报告都不等于通过。修复后复查保留原失败记录，不能用复查结果覆盖首次证据。
- 引用历史报告必须核对其源码、补丁、扩展、配置及输入版本的适用范围；相关代码变化后，旧报告仍是历史证据，不能当作新版本已验证。正式性能验收按 §8.3 独立运行。

审核已经修复的边界作为后续回归约束：`reset/step` 控制参数拒绝浮点、字符串、布尔，保留 Python/NumPy 整数支持；拒绝不得改变战斗状态。模型批量动作与 checkpoint 的约束见 §7.5 / §8.1。

---

## 6. 评估协议

### 6.1 种子隔离

- `eval_seeds.json`（1000 个种子）**第 1 周生成并提交进版本控制**
- 训练采样区间与评估区间互斥：正式评估 `seed ∈ [0,1000)`，训练 `seed >= 100000`
- 开发诊断也使用 `>=100000` 且与训练互斥的区间；正式评估集不参与反复调参，S3/S4 的开发 CI 不能代替正式 Gate
- 每个 run 记录 `eval_seeds.json` 的哈希，保证跨周结论可比；已固定 SHA-256 为 `38a093486535aa529d54ebfcfd535e67fe043daff8286b5740b5766a6131af9c`

### 6.2 配对比较

同一批 1000 种子上跑全部策略（随机 / 规则 / PPO+MLP / PPO+SetTransformer / DT），每个种子下各策略面对**完全相同**的初始牌序与敌人。

理由：STS 战斗的种子间方差极大（抽牌运影响支配性），非配对比较下 1000 局可能检不出真实差异。

### 6.3 分桶

按遭遇类型分层报告。不同敌人对策略要求不同：Jaw Worm 是 race、Cultist 要求快杀、Louses 是多目标分配。总体胜率会把差异平均掉。

多敌人桶是 Set Transformer 最可能显出优势的地方（集合结构真正有意义），也是核心对照最该看的桶。

按锁定的评估配置报告每个桶的实际样本数，不沿用“每桶约 100–150 局”的估算。正式 1000 seed 文件不可改写；若需补充诊断，使用事先登记的独立开发配置并单列结果，不能混入正式样本改变门槛。

### 6.4 指标

| 优先级 | 指标 | 说明 |
|---|---|---|
| 主 | 回报均值 | §4.4 的整场最终成绩 R；失败及硬超时均为 0，唯一主指标 |
| 次 | 胜率 | |
| 次 | 平均剩余血量 | |
| 次 | 平均回合数 | |
| 断言 | 非法动作率 | **必须恒为 0**，是正确性检查不是优化目标 |

### 6.5 "显著优于"的判定

- 配对 bootstrap 重采样 10000 次，回报差值 95% CI **下界 > 0**
- 不用 t 检验：回报分布双峰（赢/输两簇），正态假设不成立
- **同时报效应量**（Cohen's d 或差值/标准差）。1000 局配对下微小差异也会"显著"，需区分统计显著与实质显著

---

## 7. 模型架构与训练边界

### 7.1 两类 Transformer 的 token 对应

| 经典 Transformer 概念 | A 阶段 Set Transformer | B 阶段 Decision Transformer |
|---|---|---|
| token | 四区的一张卡 / 一个敌人 / 玩家 | `return-to-go / state / action` 轨迹项 |
| 输入编码 | 类别 embedding + 模型内固定数值缩放 + 类型专用投影 | action embedding；return/state 投影 |
| 位置信息 | 无槽位位置编码，保留 entity type 与 location 语义 | timestep embedding，同一时刻三个 token 共享 |
| 注意力 mask | 实体 padding mask，集合内双向注意力 | causal mask 与轨迹 padding mask |
| 汇总 | PMA 全局摘要 + 独立实体编码 | 使用截至当前 state 的因果上下文 |
| 训练输出 | 候选动作 policy 与全局 value | 预测当前 action，只在有效动作预测处计算 loss |

实体数由当前牌堆/手牌/敌人配置决定，批张量长度由容量契约决定。当前卡牌容量 20、敌人容量 5，另有玩家全局输入；有效实体数与 padding 长度不能混称。中等档容量待审计，不预写“40 个实体”的固定接口。

B 的 state 表示复用 A 的集合编码器：内层编码当前无序实体，外层建模有序历史。池化摘要之外保留当前手牌、目标实体供动作头使用。DT 在 `R₀ s₀ a₀ R₁ s₁ a₁ ...` 中利用截至 `s_t` 的上下文预测 `a_t`；训练标签不能通过未移位输入泄漏到同一步预测。

### 7.2 A 阶段

- Set 编码器：类型化投影 → ISAB × 2，保留实体编码；PMA 产生全局 `[d_model]` 摘要。
- policy：结合实体与全局上下文按 §7.4 逐候选打分；value：读取全局摘要。
- 手写 QKV、multi-head attention、ISAB、PMA，不以 `nn.MultiheadAttention` 代写学习目标；通过数值与 mask 验证后可用内置实现提速，保留手写参照。
- 数值编码沿用 §4.2 的同一份尺度和版本；MLP 与 Set 使用同源原始事实，不引入可训练 wrapper 或评估集统计。

MLP 基线使用 `FlattenWrapper`，类别 embedding 与数值投影在模型内完成，再 flatten 接 policy/value。参数规模与正式 Set 对照保持同量级并报告实际参数数。D25 当前 MLP 输出固定 31 位 logits，允许顺序敏感；它不被要求具有 Set 的结构置换等变性。S2 记住换位样本不能证明结构等变，也不能证明游戏策略变强。

### 7.3 B 阶段

- 轨迹为 `(R_t, s_t, a_t)`，`R_t` 用 §4.4 同一奖励序列的未折扣 return-to-go。
- causal mask + timestep embedding；预测 `a_t` 时只读取截至 `s_t` 的因果上下文，以及当前手牌/目标实体。
- 交叉熵监督训练；loss 仅在有效动作预测位置计算，padding 和未来动作不可泄漏，合法性使用当时环境 mask。
- 推理目标回报从训练数据分布的预设高分位数选择，之后按已发生奖励更新，不能读取本局未来真实回报。

离线数据默认来自规则 Agent + ε 噪声；若按 §9.1 改用已验证 PPO，记录来源、策略版本与噪声。数据质量和覆盖是实验风险；DT 能否达到或超过生成策略、return-conditioning 是否有作用，须由实验判断，不预先承诺性能上限或提升。

### 7.4 Set / DT 动作头（D17）

保留每张手牌、每个目标的独立编码，用共享评分函数评价“这张牌打这个目标”，排列为 `slot * max_target + target`；结束回合单独打分。无目标卡用独立 `NO_TARGET` 语义生成其环境列 0 分数。所有候选合并后统一施加环境 mask、softmax，形成一个离散分布。

仅重排手牌或敌人并同步动作映射时，相应 logits/概率必须随之重排；全局 value 和结束回合分数保持不变，padding 不影响有效输出。这是 Set/DT 的结构验收，不套用到作为顺序敏感对照的 flatten MLP。

反例：`[Bash, Strike]` 与 `[Strike, Bash]` 可以有相同池化摘要和相同 mask，但“打 Bash”的动作编号不同。仅凭摘要输出固定槽位分数会丢失实体对应；PMA 负责全局上下文，不能取代实体动作评分。具体共享评分函数第 11 周确定。

### 7.5 Agent、runner 与批量模型接口

执行接口沿用 D23：Agent 返回 `probabilities[A]` 与 `action: int`；当前 `A=31`。Agent 内部完成掩码后分布构造与抽样，runner 只校验、转交整数、调用环境，不能持有采样 RNG 或猜合法性；终局不再调用 Agent。全零 logits 经 mask 产生均匀合法随机策略。

训练接口须区分执行概率与可微计算：模型给定动作必须是与观测同设备的 `torch.int64[B]`，逐样本校验范围及原观测 mask。`[B,1]` 不得广播为 `[B,B]` log-prob；log-prob、value、熵和训练目标的 batch 维度须明确且匹配。非法给定动作在损失计算前拒绝，不把 `-inf` 继续送入 PPO ratio。

用于更新的 log-prob、熵、ratio 和梯度留在 torch 中；旧 log-prob/value 作为固定样本数据保存并 detach。只在向执行接口交付时转为 NumPy/整数，不能用 NumPy 概率反向拼出训练图。

### 7.6 S3 rollout 与 PPO 更新契约

现有 `run_episode` 的动作序列、步数和总回报适合执行验收，不能单独满足 PPO 数据需求。采集器显式保存每个 transition：

| 字段 | 对齐要求 |
|---|---|
| 原观测与原 action mask | 本步动作选择前的观测，保存后不被下一步覆盖 |
| action、旧 log-prob、旧 value | 来自同一原观测；动作经原 mask 检查 |
| reward | 执行该动作后环境返回的奖励 |
| terminated / episode 边界 | 对应本 transition 的任务终止；不得错用上一局标志 |
| 批次末观测与 value / 续采状态 | 用于未终止战斗的自举和下一批继续采样 |

更新必须从原观测及原 mask 重算动作分布，禁止用下一步 mask 评价旧动作。环境不访问模型，模型不读取 C++ 私有对象；不为此引入通用 RL 框架。

GAE 边界：真正胜负和任务硬超时不自举；rollout 批次用尽而战斗未结束时从下一状态 value 自举。优势递推不能跨 reset 进入另一场战斗，新局初态 value 不能接到旧局末尾。若未来支持外部时间截断，必须先约定终止、自举和 episode 边界语义；当前正式采集器按 D20 处理。

S3 先通过手算短轨迹、原 mask 对齐、终止/切段、无跨局优势以及恢复一致性检查，再开始固定预算短训。PPO 从新初始化开始，不使用 S2 对随机动作标签的记忆权重当作已学习策略。S3 单次初始化的改进是管线诊断，S4 才做预先登记的多初始化配对检验。

---

## 8. 工具链与复现

| 组件 | 选择与边界 |
|---|---|
| PPO | 以 `scripts/cleanrl-lock.json` 锁定的 CleanRL `ppo.py` 为起点，保留许可；逐段对应见 [PPO 来源记录](docs/ppo-source.md) |
| 架构 | 手写注意力与集合编码；模型替换不改变环境和 rollout 契约 |
| 配置 | `dataclass` + `yaml.safe_load` + `argparse`，不增加 Hydra 或通用 RL 框架 |
| 记录 | D25 S3 已选 TensorBoard + 本地 JSON；完整配置与产物写入 `runs/<run_id>/` |
| 正式后端 | `scripts/lightspeed-lock.json`、适配补丁及构建脚本锁定；Python 是规范层与教学实现 |

### 8.1 每次训练的交付档案与恢复验收

每个 run 必须保存：

1. git commit、工作树状态、实际运行源码 ZIP 与逐文件哈希清单；包含影响运行的未提交/未跟踪代码，不能只存 commit。源码快照仅归档项目相关文件，不混入凭据或反编译参考材料。
2. 完整解析后的配置、命令、依赖/解释器版本、后端锁文件、补丁和实际扩展哈希；输入/动作/序列化、registry、数值编码版本与尺度。
3. 环境 seed 区间与逐局分配、模型初始化、策略抽样、minibatch 洗牌 RNG 的种子；开发诊断区间和固定 `eval_seeds.json` 哈希。模型输入仍禁止读取这些复现信息。
4. 数据/轨迹及诊断配置哈希、曲线、逐局结果、分桶指标和报告；明确通过/失败/跳过/未验证范围，保留失败实验。
5. 模型权重、优化器、训练步数/iteration、配置以及所用 Python/NumPy/torch/采样 RNG 状态；记录 checkpoint 的恢复边界。
6. 进行中战斗的恢复依据。当前 S3 约定保存 seed 与已执行动作，通过确定性重放恢复战斗；无需序列化 C++ 私有对象，但不能悄悄重开一局冒充精确续训。

保存和加载都校验实际 embedding 词表容量、registry 内容/哈希、输入与动作契约、数值编码版本；容量或语义不匹配明确拒绝。不能让只有四行 embedding 的模型被标记为支持 `card_id=4` 后成功加载。

恢复验收比较“不中断”与“同一 checkpoint 恢复”后的下一批轨迹、训练指标和权重是否一致，并记录运行平台/确定性设置；只验证权重可加载不算可恢复训练。跨平台若不能逐位复现，明确其边界，不能把近似一致改写成逐字节一致。

### 8.2 监控与诊断

记录回报均值、胜率、遭遇分桶、策略熵、policy/value loss、新旧策略 KL、clip fraction、explained variance、grad norm、非法动作率和吞吐。出现非有限梯度/损失或非法动作立即停止并保存诊断证据。

熵快速下降、explained variance 长期接近 0 是排查信号，不凭单一指标判定原因。先看奖励与 episode 时序、原 mask、旧 log-prob、优势、自举和批量 shape，再判断模型容量；扩容后先查首次退化批次。

### 8.3 并行实现与性能测量

并行开发优先使用独立工作树，明确文件归属与最终集成版本；训练前保存实际源码快照。不要让不同任务同时改动同一实现文件后仍以单一 commit 解释结果。

正式性能验收安排独立运行，避免与训练、编译等竞争 CPU/内存；记录线程数、硬件、预热、配置、重复轮次及各路径中位数。性能失败与复查分别保存；审核中的吞吐波动不足以单独归因于新增参数校验。

### 8.4 已登记的 S3 首轮配置（尚非通过结果）

继承 D25 S3 与 `configs/ppo-minimal.yaml`，不在 v5 另起参数版本：

| 项 | 已登记值 |
|---|---|
| 总环境 transition / rollout | 262144 / 8 环境 × 128 步，共 256 次 PPO 更新 |
| 每次更新 | 4 epoch × 4 minibatch |
| 优化 | 学习率 0.00025 线性衰减；Adam eps=1e-5；clip=0.2 |
| 折扣与损失 | gamma=1；GAE lambda=0.95；entropy 系数 0.01；value 系数 0.5；grad norm 上限 0.5 |
| 环境训练 seed | `[1000000,2000000)`，逐局分配 |
| 模型 / 采样 / 洗牌 seed | 600000 / 610000 / 620000 |
| 开发诊断 | 900000 起 300 局，三遭遇各 100；独立 Agent RNG；训练前后配置一致 |
| 正式评估文件 | 只记录哈希，不用于本次开发调参 |

S3 完成单初始化的训练与诊断；S4 在运行前登记多初始化、固定预算、配对配置和报告口径。不得挑选成功的一组代表全部运行，也不将一次开发 CI 通过称为跨初始化可靠或正式 Gate 3 通过。

---

## 9. 18 周排期

保留自 2026-09-01 起算的原预算与 Gate 周次。Week 标签同时用于工作包导航，不代表已有对应周数的实际工时证据；提前完成工作包不重置日历，新增审核条款也不延长后续截止。当前事实以 §0.4 为准。

| 周 | 主线工程 | 原理下钻 | 出口 |
|---|---|---|---|
| 1 | v4 定稿、仓库与固定评估 seed、日志侦察（历史工作包） | 状态机、RNG、复现 | 评估集入版本控制 |
| 2–3 | 最小切片；锁定 C++ 后端；关键结算/接口回归、有限日志校准（已有记录，不重新试 build 选路线） | POMDP、mask、结算顺序 | 随机策略闭环与校准边界 |
| 4 | 规范观测与双 wrapper、Gate 1；D24 逐牌实体迁移另行验收（已完成） | 信息边界、槽位与语义 | Gate 1 + V2 独立证据 |
| 5–6 | 先完成 D25 S1–S5；再审计容量、卡表与动作语法，按机制批次扩中等档；规则 Agent、配对评估；按需一次校准并锁定血量系数 | 最小训练闭环、GAE 边界、分层评估 | **Gate 2** |
| 7–8 | 在锁定中等档完成 PPO 逐行核对、MLP 正式训练与监控；复用代码，重新验证新范围 | GAE、ratio clipping、mask 下梯度 | 中等档训练跑通 |
| 9–10 | 中等档 PPO 调参与诊断 | 熵、value 失配与失败轨迹 | **Gate 3** |
| 11–12 | 手写 QKV / MHA / ISAB / PMA，复用数值编码与动作契约，接入已稳定的训练主线 | 注意力、置换性质、padding mask | Set 训练与结构验收 |
| 13 | MLP vs Set 正式配对实验、分桶及效应量 | 区分架构效果与训练噪声 | **Gate 4** |
| 14 | 按锁定环境生成目标 20 万条离线轨迹，默认规则 Agent + ε 噪声；记录质量、覆盖与回报一致性 | 离线数据分布 | 可复现数据集 |
| 15–16 | DT 序列化、causal mask、timestep embedding、复用 A 编码器 | 自回归与 return-conditioning | **Gate 5** |
| 17–18 | 报告、README、复现脚本、代码整理；不写新功能 | — | 他人可复现 |

### 9.1 最近执行顺序与完成标准

| 顺序 | 工作 | 工程完成标准 |
|---|---|---|
| S3 | 核对已有 PPO 实现，完成数学/恢复检查和固定预算短训 | §7.6 时序正确，无 NaN/非法动作，训练与诊断曲线、源码与恢复证据完整 |
| S4 | 预登记多初始化的配对诊断 | 独立开发配置下报告 PPO 相对随机的回报差、paired bootstrap 95% CI、分桶与初始化差异；最低改进门槛为回报差 CI 下界 > 0 |
| S5 | 冻结最小可学习检查点 | 代码与配置版本、权重、优化器/RNG、恢复验证、seed/哈希和结果摘要齐全 |
| M1/M2 | 来源、卡表、固定牌组、遭遇与容量契约 | 逐候选动作可表达，生成牌/分裂/召唤不越界，扩容批次先登记 |
| M3–M8 | 按批次扩容、规则策略与正式评估 | 每批旧范围回归 + 从头短训 + 配对诊断；最终按 Gate 2 收口 |

S1/S2 已有证据，不因 v5 重做；相关代码若变更则针对受影响约束复验。S3–S5 仅验证最小切片，不替代中等档 Gate 2/3 或架构 Gate 4。学习复盘按 §0.4 单列。

### 9.2 排期风险

第 5–6 周同时包含最小训练闭环和机制扩容，新增接口要求须在对应批次实施，不能再开一轮基础框架重构。日历或指标触发 Gate 时照 §10 执行，任何问题连续三天无进展按元规则换方向。

Set 放在已稳定的 MLP/PPO 后实现，便于定位新架构问题；简单切片能否体现 Set 优势仍是待实验假设，不为取得优势临时增加机制。

第 14 周数据质量影响 B 阶段。若规则策略质量不足且 PPO 已有验证，可按记录使用 PPO 加噪声生成；Gate 3 降级后仍使用规则策略。生成策略与训练数据分布的变化必须可追溯。

20 万轨迹的生成时间、存储和 DT 训练耗时均为 `[未核实]` 的预算项。先由实际平均轨迹长度、独立吞吐和小样本训练测量估算，不沿用“算力不是瓶颈”的保证；资源不足按既定止损处理，不挤占第 17–18 周。

---

## 10. 止损与降级

日历闸门与指标门槛，**谁先触发算谁**。降级路径已写死，不留到当时决定。

正常路线在锁定中等档上执行 Gate 2/3；D25 最小切片诊断不能替代。仅在 Gate 1 已正式触发“锁定最小切片”降级并登记时，后续 Gate 才在该降级范围继续，周次、指标和后续降级路径不变，报告明确范围。当前 Gate 1 已通过，不启用此例外（D26）。

| 检查点 | 周 | 门槛 | 触发降级则 |
|---|---|---|---|
| Gate 1 环境 | 4 | §5.1 完成有限校准记录且第 2–5 项通过 | 基础门槛失败则砍掉中等档扩容、锁定最小切片；日志字段或片段覆盖不足不单独触发 |
| Gate 2 基线 | 6 | 规则显著优于随机（配对 CI 下界 > 0） | 不降级，回查 Gate 1，并诊断规则实现、输入、机制与样本；失败本身不能证明某一原因 |
| Gate 3 PPO | 10 | PPO+MLP 显著优于随机 | **PPO 出局，A 阶段改 BC + Set Transformer 监督学习**，架构对照保留 |
| Gate 4 对照 | 13 | 对照跑完，**无论结果如何** | 无差异也算通过，如实报告 |
| Gate 5 DT | 16 | DT 不低于生成数据的策略 | 砍掉 return-conditioning 的性能主张，只报实现与分析 |

### 10.1 为什么 Gate 3 是最重要的一行

它的降级路径是"PPO 出局，改监督学习"，不是"再给两周"。这条能救整个项目：Set Transformer vs MLP 的对照在 BC 框架下同样成立，且更干净（监督学习方差小，架构差异更易检出）。核心目标是理解 Transformer，不是证明 PPO 能训起来。**RL 是手段不是目的。**

若 Gate 3 降级：第 11-13 周不受影响；第 14 周数据生成改用规则 agent 而非 PPO 策略。退路是通的。

### 10.2 为什么有"跑完即通过"的门槛

Gate 4 不要求 Set Transformer 赢。若在当前机制范围下 MLP 打平，那是真实结论，附分析（实体数太少、状态维度不足以体现置换不变优势）写进报告即为合格产出，比调参凑出的胜利更有价值。预留"允许失败"的出口，能防止第 14 周开始偷偷调参。

### 10.3 元规则

**任何单一问题连续投入超过 3 天无进展，强制停下写一段**：卡在哪、试过什么、下一步的两个**不同**方向；然后选更不一样的那个。给 debug 死循环用，PPO 调参尤其需要。

### 10.4 第 17-18 周

不设指标门槛，纯保护性预留。**不允许写新功能。** 一个跑得起来但无人能复现的项目，半年后回看等于不存在。

---

## 11. 仓库与文档分工

| 位置 | 用途与状态 |
|---|---|
| `spec-v5.md` | 当前唯一执行依据 |
| `spec-v4.md` / 更早方案 | 冻结历史；不据此启动新工作 |
| `AGENTS.md` / `README.md` | 执行约定、当前入口及复现指引 |
| `docs/decisions.md` | 改方案/协议前登记；D26 记录本次 v5 修订 |
| `docs/mechanics.md` / `docs/observation-contract.md` | 结算顺序书面依据 / 输入与动作详细契约 |
| `docs/project-audit-2026-09-08.md` | 审核时点的事实、已修复问题、建议和验证限制 |
| `docs/mlp-smoke-plan.md` / `docs/week-5-6-plan.md` | 当前任务分解、工程与理解进度 |
| `sts/env/` / `patches/` / 后端锁文件 | Python 规范层与教学环境 / 正式 C++ 适配及版本复现 |
| `sts/models/` / `sts/agents/` | 模型内表示与 policy/value / 动作选择及执行边界 |
| `sts/train/` / `sts/eval/` | rollout、更新与恢复 / 独立开发及正式配对评估；文件存在不代表已验收 |
| `configs/` / `scripts/` / `runs/` | 运行配置 / 入口脚本 / 原始实验档案 |
| `tests/` | 按机制规格、接口契约与手算数学例子验证，不仅复述实现 |

Set、DT、离线数据模块和 `docs/report.md` 在对应阶段按需创建；不预建空目录或空文件。新增独立文档、脚本采用英文小写 kebab-case；既有 Python 模块沿用包导入名称，不为命名整理破坏当前接口。

[学习路径 v2](docs/learning-path-v2.md) 负责学习目标、三种思维、双环推进和预测后验证，不覆盖本方案范围与 Gate。旧 `学习路径.md` 为历史存档，其中 STS2 目标、旧排期和规模主张均不指导当前执行。历史验收报告、决策变更行及 run 内源码指纹保持原始版本，不能为切换 v5 批量改写历史。

---

## 12. 未决事项与已销账项

| 事项 | 状态 / 时点 | 执行边界 |
|---|---|---|
| Set/DT 共享动作评分函数 | 第 11 周定 | D17 的实体对应关系和换位性质已锁定 |
| 中等档卡表数值、牌组与遭遇范围 | M1，扩容前 | 来源审计已有记录；十张数值抽查及最终白名单仍待核验 |
| 输入/动作容量及是否需要协议迁移 | M1/M2 | 当前保留 31；超过三个可寻址目标或需要二次选择时先登记迁移，否则排除候选 |
| S4 多初始化诊断配置 | S4 运行前 | 固定预算、配对样本、抽样 RNG 与报告口径，不能看结果后选种子 |
| 血量系数 λ 终值 | 第 5–6 周按需校准一次后锁定 | 当前 0.5；变更须重算所有离线回报并保持 A/B 一致 |
| TensorBoard vs W&B | 已销账：D25 S3 | TensorBoard + JSON，不再重新选型 |
| 是否纳入 `sts_lightspeed` | 已销账：D15/D16 | 正式 C++ 后端已锁定，不重复半天试 build 决策 |
| 奖励折扣/终止/累计口径 U8 | 已销账：D20 | γ=1，终局按 §4.4；硬超时不自举；势能反馈暂不实现 |

未决项不能放宽现有容量、来源或信息红线。方案改变须先更新决策日志；学习问题按当前最小步骤讲解，不因未来未决项停下已经授权的当前工作。

---

## 附 A：工时分配（沿用 v4 修正表）

以下比例沿用 v4；各行合计 100%，服务于 §1 的总工时预算，不新增学习或文档排期：

| 阶段 | 工程 | 学习 | 实验 | 文档 |
|---|---|---|---|---|
| 环境阶段（1-6 周） | 60% | 20% | 10% | 10% |
| 算法阶段（7-10 周） | 40% | 30% | 25% | 5% |
| 架构阶段（11-13 周） | 30% | 40% | 25% | 5% |
| 离线阶段（14-16 周） | 35% | 30% | 30% | 5% |
| 收尾阶段（17-18 周） | 20% | 0% | 20% | 60% |

## 附 B：事实核验与剩余限制

| 项 | 当前证据与未覆盖范围 |
|---|---|
| runlogger 数量、格式和覆盖 | 已有本地侦察与有限校准；缺失字段、初态不一致仍未充分校准，不再写成完全未调查 |
| C++ API / 构建 / 机制覆盖 | D15/D16 已锁定并构建，当前最小切片有回归；中等档与真实游戏全机制等价未验证 |
| HF 卡表数值 | `[未核实]`；候选语义抽查不等于数值销账，不能覆盖一手来源 |
| 正式后端吞吐 | 最小 V2 有历史一万配置三轮证据；新扩容、PPO 训练吞吐及并行加速须独立测量 |
| determinismfix | `[未核实]` 是否为当前有限日志校准所必需，不作为默认训练依赖 |
| 扩容遭遇与动作目标 | 审核依据锁定后端确认 Gremlin Gang / Lots of Slimes 超出当前动作目标容量；其他分裂/召唤分支待逐候选核验，不宣称正版游戏全覆盖 |
| DT 性能与资源 | `[未核实]`；数据质量、return-conditioning 收益、耗时与存储由实际实验报告 |

游戏数值与论文结论须按一手来源及核验日期登记；本次版本修订以本地审核和决策为依据，不新增外部事实核验结论。来源编号继续沿用项目引用日志格式。

## 附 C：审核建议到 v5 条款的对应

| 审核报告 | v5 落点 | 交付/验收 |
|---|---|---|
| §1 真实进度 | §0.4、§9.1 | 区分工程与理解、实现与训练、历史与本次证据 |
| §2 参数、广播、checkpoint 边界修复 | §5.5、§7.5、§8.1 | 拒绝无效参数且不改变状态；批量动作不广播混样；保存/加载均核验容量 |
| §2 正式通过判定 | §5.5 | 样本/轮次/确定性证据完整，smoke 与正式 passed 分开 |
| §3.1 观测/动作容量 | §4.1、§4.3.1、§9.1 | 扩容前集中契约，核对最大目标索引，迁移另记决策 |
| §3.2 数值编码 | §4.2、§7.2、§8.1 | 模型侧共用尺度与编码版本，初始化探针不充当训练收益 |
| §3.3 rollout/PPO | §7.5–§7.6、§8.4 | 原观测原 mask、终止与切段、torch 更新、手算例子 |
| §3.4 复现与并行 | §8.1–§8.3 | 实际源码快照、恢复一致性、独立性能测量 |
| §4 验证限制 | §0.4、§5.5、附 B | 保留失败和未验证项，不宣称全机制等价 |

v5 的方案条款不等于对应代码已经实现。除 §0.4 列明的现有证据外，训练、扩容与架构验收仍须在各阶段实际执行并落盘。

---
