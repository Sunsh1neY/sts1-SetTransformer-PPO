# 决策日志

> 用途：记录所有已裁定决策及其**推翻条件**。推翻已有决策前，必须先在文末"决策变更记录"登记；未登记的推翻视为违规。
> 来源：`spec-v4.md`（2026-09-01 定稿），2026-09-03 建仓库时入账。
> 当前执行依据：[spec-v6.md](../spec-v6.md)（2026-09-08，D27）；以上来源日期保留建账历史。
> 元规则（v6 §10.3）：任何单一问题连续投入超过 3 天无进展 → 强制停下，写下卡点、已试方案、两个**不同**方向，选更不一样的那个。

## 2026-09-03：v4 定稿 12 项裁定入账

### D1 时间预算：18 周 × 30h = 540h（2026-09-01 起算，2027-02 初出发，扣缓冲 4 周）

- 理由：出发日期是硬约束；v3 的 40h × 26 周假设不成立。
- 推翻条件：连续两周实际投入 < 20h，或出发日期提前 → 立即按 §10 降级路径执行，不等下一个 Gate。

### D2 环境来源：C++ lightspeed 正式后端；Python 教学实现；现有日志有限校准（D16/D20 修订）

- 理由：训练通道需要可控、可复现、高吞吐；STS2 生态不成熟（v4 §0.3）。
- 日志已定位：`colinking/runlogger`（GitHub），逐行 JSON 带 `_type` 字段；拉取时记录版本和实际 schema。按 D20 只用现有字段做有限校准，不因日志未记录某字段而扩充 mod。
- 推翻条件：后端出现影响基本训练或实验结论且无法修复的差异时，按 D16 与 §10.3 重审；日志字段或片段覆盖不足本身不触发后端切换。

### D3 技术中心：先 A（Set Transformer + PPO）→ 后 B（Decision Transformer），A/B 对照即产出

- 理由：两者嵌套（B 的 state token = A 编码器输出），先 A 后 B 省一次重写；真实目标是理解 Transformer 原理。
- 推翻条件：Gate 3 触发 → PPO 出局，A 阶段改 BC + Set Transformer 监督学习，架构对照保留；B 阶段不受影响。

### D4 机制范围：两步扩容（第 1-4 周最小切片 → 第 5 周起中等档），不做完整 Act 1

- 理由：先建立可训练的最小切片，再按模型学习需要扩容；使用后续更大范围测试发现差异，不以全机制日志覆盖为前提（D20）。
- 推翻条件：Gate 1 的基础正确性、确定性、合法性、稳定性或性能门槛失败 → 砍掉中等档，锁死最小切片到项目结束（§10）；日志字段或片段覆盖不足不单独触发该路径。第 5 周 HF 卡表抽查不过关 → 卡表改手工整理。

### D5 观测/动作表示：环境返回结构化 dict；FlattenWrapper 与 TokenWrapper 同源派生；动作扁平 `(槽位, 目标)` + mask

- 理由：同源派生保证 MLP 与 Transformer 特征一致，对照实验才干净；扁平动作让 DT 的 action 天然是单个离散 token（v4 §4.3）。
- 动作头补充：已按 D17 裁定保留实体与动作编号的对应关系，逐个候选动作打分；扁平动作编码本身不变。
- 信息边界补充：按 D19，仅将玩家可见信息及经核验的公开规则语义编码为状态；补齐当前可见字段，排除隐藏顺序和未来随机结果。
- 推翻条件：替代动作头须通过 D17 的换位验收，并先登记变更。

### D6 v3 处置：冻结为愿景文档；继承与规模无关的设计约定；放弃规模主张

- 推翻条件：本项目 18 周完结且有富余，STS2 属于**下一个项目**，不在本项目内解冻。

### D7 奖励：当前直接使用终局成绩，λ=0.5 起，γ=1；A/B 共用奖励与累计口径（D18/D20）

- 历史状态：本条原本只描述独立战斗。D27 保留其 `battle_reward_v1` 数值，但取消“默认再校准一次”的执行授权，并新增独立的 `run_reward_v1`；现行口径以 D27 为准。
- 理由：胜利成绩用剩余血量区分；失败统一为 0（D18）。当前非终局奖励为 0，只在终局发放成绩，γ=1 使累计回报直接等于整场成绩；势能型反馈暂不实现，把时间留给模型学习。
- 推翻条件：第 5-6 周仅在实际分布显示需要时校准血量系数 λ 一次，之后锁死；训练诊断明确需要辅助反馈时另行登记，并验证 D18 的累计一致约束。奖励 bug 修复或参数变更后重算离线数据回报。GAE 的 λ 是独立的训练参数，不是此处的血量系数。

### D8 评估协议：1000 隔离种子、按遭遇类型分桶、配对 bootstrap 95% CI、附效应量

- 理由：STS 种子间方差极大，非配对比较检不出真实差异（§6.2）；回报双峰，t 检验正态假设不成立（§6.5）。
- 落地：`eval_seeds.json` 已生成（sha256 `38a093486535aa529d54ebfcfd535e67fe043daff8286b5740b5766a6131af9c`），由 `scripts/make_eval_seeds.py` 按固定规则生成（seeds = 0..999，非随机抽取，可审计）。
- 推翻条件：`eval_seeds.json` 提交后不可变——变更即作废全部历史结论。分桶后某关键桶样本不足，允许单独加采样（预授权，无需登记）。

### D9 正确性验证：基础回归与稳定性检查 + 现有日志有限校准（D16/D20 修订）

- 执行边界：C++ 后端的确定性、动作、奖励与稳定性回归保留；Python 与 C++ 差分仅用于教学和定位具体差异，不作为正式 C++ 后端的独立真值证明。现有日志缺失字段明确记为未验证，不开展全机制逐状态对拍。
- 推翻条件：实际差异影响训练或 A/B 结论时，补针对性证据并修复；扩大日志采集范围须另行登记预算。原半天 build 门槛已由 D15 收口。

### D10 工具链：CleanRL 单文件 PPO 起点；架构手写；YAML + dataclass + argparse；W&B 或 TensorBoard

- 理由：PPO 对实现细节极度敏感，无抽象层才能逐行看懂（v4 §8）；架构手写是学习目标本身。
- 推翻条件：CleanRL 与结构化 dict obs + mask 的冲突过大 → 允许参考其结构自写训练循环（保留逐行可读性），登记后执行；W&T vs TB 见 U2。

### D11 止损：日历闸门 + 指标门槛，谁先触发算谁；降级路径预先写死

- 推翻条件：仅当门槛本身写错（元错误）才可修订，修订需登记并说明为何排期时未发现。
- 已登记修订：D20 按用户新的投入优先级修正 Gate 1 日志覆盖门槛；原排期未先确认日志 schema，后端切换后也未同步验证成本。其余门槛不变。

### D12 真实目标：通过工程实践理解主流 Transformer 技术原理；游戏是载体

- 理由：所有降级路径都服务于此——Gate 3 降级后架构对照依然成立（§10.1）。
- 推翻条件：无。这是锚点，其余一切可为它让路。

### D13 反编译查阅政策（2026-09-03，用户确认）

- 裁定：**非目标维持**——反编译不作为获取游戏逻辑的主路线；允许在规格争议仲裁时**定点查阅**反编译源码；红线：**不得照抄反编译代码进模拟器**（保住 lightspeed 差分与对拍体系的独立性）。
- 使用阶梯：wiki / 社区文档 → lightspeed 源码 → 反编译 Java → runlogger 日志兜底。
- 存放（用户指定）：反编译产物放 `reference/sts1-decompiled/`（gitignore，不入库——版权风险只在公开分发，本地查阅无风险）。生成命令见 `reference/README.md`。
- 首例仲裁已立案：Vuln/Weak 乘法先后（mechanics.md §4 出现矛盾证据），第二档入库前终审。
- 落地进度：游戏本体已定位 `E:\SteamLibrary\steamapps\common\SlayTheSpire\desktop-1.0.jar`；jadx 未下载，首次仲裁前生成即可。

### D14 RNG 实现：主流 xorshift128+，洗牌临时 Java LCG（2026-09-03 初裁，2026-09-05 正式修订）

- 原裁定纠正：2026-09-03 将战斗全部随机性写成 `java.util.Random`，范围判断错误。当前实现早已区分双层 RNG，但当时未同步决策日志；本次才正式登记，不能把代码注释中的“已登记”当作过去发生的记录。
- 当前后端：`sts::Random` 使用经 murmurHash3 初始化的 xorshift128+；洗牌消耗 `shuffleRng.randomLong()` 产生种子，构造临时 Java 48 位 LCG 执行洗牌。Python 教学实现对应为 `StsRandom` 与 `JavaRandom`。
- 初始化：`BattleContext::init` 中 `aiRng`、`monsterHpRng`、`shuffleRng`、`cardRandomRng` 由 `Random(run_seed + floor_num)` 的同一初始状态复制；`miscRng`、`potionRng` 继承整局 `GameContext` 状态。不能写成“层数不进入 RNG”或“全部流重置”。
- 教学边界：Python `CombatRNG(combat_seed)` 的四条流仍从战斗种子新建，misc 没有完整继承整局历史，只是教学近似；它与正式 C++ 后端的固定 seed 可复现性是两种检查，不等于两者逐状态一致或已还原真实整局。
- 证据范围：2026-09-05 核对本地锁定 lightspeed 的 `include/game/Random.h`、`src/combat/Actions.cpp`、`src/combat/BattleContext.cpp:28-34` 与 `sts/env/rng.py`；这是当前实现依据，不能由此宣称所有 RNG 调用路径均与真实游戏一致。其余来源争议仍按 D13 处理。
- 推翻条件：具体差异证据表明上述实现说明或初始化边界错误时修订；不以未经核验的游戏整体准确率宣传替代证据。环境 RNG 与训练侧生成器分离，seed 及 RNG 内部值不得进入观测。

### D15 lightspeed 纳入：build 成功（C++ 侧），Python 绑定路线第 3 周定（2026-09-04，U4 试 build 收口）

- 结果（半天时限内约 40 分钟）：MSYS2 mingw64 **gcc 16.2.0** + CMake 4.4.2 + Ninja 编过 `test` 目标；冒烟 `test.exe simple_agent_mt 1 1 3` 跑通（3 局完整 playout 2.5ms，w/l 正常输出）。
- 构建摩擦与解法（均为**构建侧**，不触碰锁定源码 commit `7476a81`）：
  1. vendored 子模块 `json` / `pybind11` 未随克隆拉取 → `git submodule update --init --recursive`（checkout 到父提交锁定的 SHA，锁版纪律不破）；
  2. CMake 4.x 拒绝老 `cmake_minimum_required` → 官方开关 `-DCMAKE_POLICY_VERSION_MINIMUM=3.5`；
  3. gcc 16 不再传递包含 `<algorithm>`（`std::sort/find_if` 等大量报错）→ `CMAKE_CXX_COMPILER_LAUNCHER` 强制 `-include algorithm/numeric/cstdint`（`build/gxx-wrap.bat`，不入库，命令在 README），零源码改动。
- **Python 绑定已修复（2026-09-04 同日，路线 (a) 执行完毕）**：vendored pybind11 v2.7.1 对 CPython 3.13 编译失败（`PyFrameObject` 不透明化），子模块升级至 **v2.13.6（SHA `a2e59f0e`）**——选 2.13 而非 3.x：官方支持 CPython 3.13 的最低稳定系列且保持 v2 API，对 2021 年的绑定源码零改动。`slaythespire.cp313-win_amd64.pyd` 构建成功，`import slaythespire` 验证通过（20 个绑定：Card / GameContext / GameOutcome / MonsterEncounter / NNInterface 等；`get_seed_long` 种子换算冒烟通过）。运行时注意：mingw 三件套运行时 DLL（libstdc++-6 / libgcc_s_seh-1 / libwinpthread-1）须与 .pyd 同目录或在 PATH。子模块指针变更在 gitignore 的 `third_party/` 内不入库；回滚：`git submodule update --checkout pybind11` 回到锁定 SHA `787d2c8`（v2.7.1）。
- 理由：U4 门槛是"试 build，成功即纳入"——C++ 模拟器本体已可用，纳入裁定成立；绑定路线是工程通道选择，不构成翻案点。
- 推翻条件：仅当 gcc 16 + 包装器构建产物的**模拟行为**与上游工具链出现差异（如浮点语义影响 RNG）时重审；纯构建摩擦不算。

### D16 正式训练后端：C++ lightspeed 战斗环境（2026-09-05，第 3 周）

- 裁定：正式训练环境切换为 `sts_lightspeed` C++ 后端。Python 侧只保留模型、训练循环和薄包装；现有 `sts/env/` Python 模拟器降级为教学、结算顺序验证和小规模回归用途，不再继续扩展为完整游戏规则实现。
- 第一阶段边界：先暴露战斗级 `IroncladBattleEnv.reset/step/action_mask/observation`，当前限定 A0 的 Jaw Worm、Cultist、Louses 三种遭遇；`reset` 拒绝范围外配置，后续扩容时再同步开放接口与观测。规则、随机数、卡牌结算、敌人行动全部复用 lightspeed；不在 Python 绑定中重写。
- 接口约定：动作仍为 `slot * 3 + target`，`30` 为结束回合；在 C++ `search::Action::isValidAction` 基础上落实项目的目标约定，无目标卡只允许 `target = 0`，避免重复动作；观测包含手牌、敌人、全局状态和三类 mask；seed 只用于复现，不进入特征。
- 奖励约定：遵循 D7、D18、D20 和 spec-v4 §4.4；非终局 0，胜利终局 `1 + 0.5 * HP_remaining / HP_max`，失败及硬超时终局 0，累计 γ=1。当前不实现势能反馈，A 阶段 RL 与 B 阶段 DT 共用该标量及累计口径；实现验收另行报告。
- 理由：最小 Python 冒烟模拟器已经暴露出规则重写和逐步对拍的维护成本；lightspeed 已完成构建、导入和固定 seed 战斗 smoke，直接复用成熟 C++ 规则能把工程精力留给环境接口与模型学习目标。
- 推翻条件：C++ 绑定在固定 seed 确定性、合法动作掩码或 runlogger 校准中出现无法修复的行为差异；届时按 §10.3 登记证据后再切换降级路径。尚未接入 PPO，当前裁定只改变环境后端，不改变 A/B 模型路线。

### D17 动作头：保留实体对应关系，逐个候选动作打分（2026-09-05，项目审核后用户确认）

- 裁定：Set Transformer 保留每张手牌和每个敌人的独立编码；动作头结合局势信息，对每个“牌—目标”候选动作打分，再按当前槽位、目标索引排列为原有离散动作 logits。结束回合单独打分，全部 logits 合并后统一施加合法动作掩码并做 softmax。
- 编码边界：槽位号不进入 token 特征；实体与输出编号的对应关系由数据排列维护。无目标卡仍只使用目标 0（mechanics.md §5），不产生重复动作。
- PMA 用途：全局池化结果用于价值估计及局势上下文；动作选择同时读取未池化的实体编码。
- B 阶段：DT 的序列仍为 `(R_t, s_t, a_t)`，动作仍是一个离散 token；预测 `a_t` 时，动作头同时读取当前手牌、目标的实体编码及截至 `s_t` 的因果上下文，不能仅凭池化后的状态摘要预测槽位。
- 理由：手牌 `[Bash, Strike]` 与 `[Strike, Bash]` 在其余状态相同、两牌均合法时拥有相同集合摘要和合法掩码，但“打 Bash”的动作编号不同；只用摘要的固定输出头无法区分。该结论由规格内的换位反例推导，不依赖游戏数值或论文性能主张。
- 验收：仅重排手牌或敌人并同步重排相应 mask 后，动作分数和概率应随对应实体重排，结束回合分数及全局价值保持不变。此为模型阶段待实现的验收要求，本次仅登记设计。
- 推翻条件：替代方案能够保留上述对应关系、通过换位验收并遵守 D5 的离散动作和 mask 约定；须先登记理由与证据。具体评分函数留到模型阶段确定。

### D18 最终成绩：失败同分，胜利按剩余血量加分（2026-09-05，用户确认）

- 历史状态：本条继续作为独立战斗目标的来源；D27 将“win”限定为 `battle_won`，完整爬塔的 `run_won` 另用稀疏 0/1 奖励，禁止混用。
- 裁定：整场最终成绩 `R = win * (1 + λ * HP_remaining / HP_max)`。正常失败与硬超时判负均为 0，不因对敌伤害、剩余敌方血量或拖延回合而获得不同的最终成绩；胜利仍按 D7 的 λ 约定加剩余血量分。
- 理由：用户确认“把敌人打到残血后失败”和“几乎未造成伤害就失败”应同分；训练目标与最终评价均围绕获胜及获胜时的剩余血量。
- 奖励口径：最终成绩 `R`、逐步奖励 `r_t`、累计回报 `G_t` 分别定义。A/B 共享逐步奖励及回报累计口径；从一场战斗起点累计的回报须与 `R` 一致，不能在辅助反馈中重新引入不同的失败成绩。
- 辅助反馈边界：按 D20，当前直接发放终局奖励、γ=1，不实现势能反馈，U8 基础版本已明确。未来若需要势能反馈，须另行定义初始与终止势能、累计方式及自举规则；单步公式应为 `γ * Φ(next) - Φ(current)`，且必须证明累计回报仍符合本裁定。
- 验收：不同初态、伤害量、持续步数的失败与硬超时轨迹最终均为 0；胜利累计回报符合上述公式；A/B 对同一奖励序列计算一致。具体实现的测试结果单独记录，文档裁定不视为已通过验收。
- 推翻条件：仅在用户明确调整最终评价目标后重审，并先登记原因；任何奖励定义变更均须重算全部离线数据集回报。

### D19 观测边界：只使用玩家可见信息（2026-09-05，用户确认）

- 裁定：模型状态包含机制范围内的当前手牌、双方血量与可见状态、敌人显示的意图；不得因模拟器能读取某个内部值就将其输入模型。具体字段的游戏内可见性须逐项核验，未核实项标记 `[未核实]`。
- 必需信息：卡牌可见语义及当前费用、玩家能量与格挡、双方可见 buff/debuff 的层数或时长、敌人当前显示的意图及伤害等。Weak 等已实现且可见的状态须纳入；敌人内部招式枚举须先映射到经核验的可见语义，不能额外暴露未显示的伤害 roll 或未来招式。
- 隐藏信息：排除 seed、RNG 状态及消耗计数、抽牌堆实际顺序和未来随机结果。若后续纳入牌堆内容，须先核验可见性，并采用无序集合或计数表示；当前手牌与动作编号的对应关系仍按 D17 保留。
- 数据边界：调试及复现记录可以保存内部状态，模型编码器只读取观测白名单；不得把整个 `info` 自动拼入训练、离线状态编码或评估输入。FlattenWrapper 与 TokenWrapper 同源，A/B 每个时刻的状态字段采用相同信息边界。
- DT 条件：可使用此前已获得的合法观测、已执行动作和已发生奖励；训练用 return-to-go 仍可由完整轨迹计算。评估时使用预设目标回报并按已发生奖励更新，禁止读取该次战斗尚未发生的真实回报；此条件变量不属于游戏状态观测。
- 验收：固定允许输入的当前可见信息、合法动作和历史，仅改变隐藏值时，编码后的输入保持一致；固定目标回报并关闭模型随机性后，动作分布也应一致。另按白名单逐项检查可见字段是否遗漏。本次仅登记边界，C++ 观测及后续数据转换仍待实现和验证。
- 理由及推翻条件：用户选择在玩家可见信息范围内比较模型；只有用户明确改为允许隐藏信息的实验任务时才可重审，须先登记，并与既有结果分别报告。

### D20 基础建设优先：有限日志校准，暂不扩充日志工具（2026-09-05，用户裁定）

- 裁定：当前不扩充 runlogger 字段，不新增日志 mod，不建立全机制逐状态对拍。保留现有日志的有限字段校准，逐项记录已比较内容、差异与缺失字段；缺失或未覆盖项标记“未验证”，不阻塞进入训练，也不单独触发砍掉中等档扩容。
- Gate 1 修订：将原“真实日志全部目标状态逐步 100% 一致”改为完成上述有限字段校准记录；结算顺序单测、固定 seed 确定性、合法动作、随机战斗稳定性及性能要求保留。Gate 2–5 与 §10.3 的止损规则不变。
- 理由：用户明确拒绝为当前切片扩充日志，选择后续更大范围测试发现差异，并要求快速推进基础建设、把时间留给模型学习。原排期把日志未提供的字段列为硬门槛，未先核实 schema，且在 D16 切换成熟 C++ 后端后仍沿用了自写模拟器的验证投入；本次依用户裁定修正该门槛，不把覆盖不足写成已证明准确。
- 后续处理：更大范围测试发现差异后，按其对动作、奖励、确定性与实验结论的影响修复；稳定性通过或现有字段匹配不能证明全机制等价。正式实验基于锁定的模拟器版本，结论适用范围如实记录。
- 基础奖励实现：为落实 D18，当前非终局 `r_t = 0`，胜利终局 `r_t = 1 + 0.5 * HP_remaining / HP_max`，失败及硬超时终局 `r_t = 0`；从战斗起点按 `γ = 1` 累计，A/B 口径相同。硬超时为任务内终止，`terminated=true`、`truncated=false`、`info.timeout=1`，不自举；训练批次截断不能冒充任务硬超时。GAE 的 λ 可在训练阶段另定，与血量奖励系数 λ 区分。
- D27 后适用范围：上述实现只对应 `battle/minimal-v1/battle_reward_v1`；不得据此把战斗胜利解释为完整爬塔通关。
- 接口范围：当前 `reset` 仅接收 A0 的 Jaw Worm、Cultist、Louses 三种遭遇，拒绝未接入观测的范围；到后续机制扩容阶段再同步开放配置与状态字段，避免当前接口默许不完整的任务。
- 势能型辅助反馈暂不实现：U8 的基础版本以直接终局奖励销账。只有学习实验明确显示需要时才另行登记其方案，仍须满足 D18 的累计一致约束；任何奖励变更均须重算离线数据集回报。本次决策不代表代码已通过验收。
- 同步纠偏：D14 的主 RNG 应为 xorshift128+，洗牌内部另用 Java LCG；战斗种子含楼层，misc 继承整局状态。当前代码已改变而旧决策未同步，本次才正式登记修订；Cultist 跳过首次 Ritual 加力量、回合末逆手牌弃牌等已确认漂移同步至 mechanics.md。其余未核验机制继续标记 `[未核实]`，D13 的查阅与引用政策保留。
- 重审条件：实际差异妨碍基本训练或改变 A/B 结论时，针对该差异修复并补必要证据；若用户重新要求全机制等价验证，再单独登记范围与时间预算。

#### D20 本次有限校准记录（2026-09-05）

- C++ 日志回放通道：补入已记录的玩家 HP 检查后，两份日志的 17 / 11 个快照均报告差异。日志玩家初始 HP 与上限为 88，`ConsoleSimulator` 初始为 80，后续 HP 持续相差 8；这说明回放初态尚未对齐，根因未定，不能把这些快照记为通过，也不能直接归因于正式 `IroncladBattleEnv` 的战斗规则。
- Python 教学回放：第二份 Jaw Worm 日志的 12 个快照在已记录字段上匹配，但缺少终局一步记录；因此只登记有限字段匹配，不宣称完整战斗全状态一致。
- Python/C++ 差分探针：修正 DLL 加载后，12 个 seed 中 2 个匹配、6 个因进程退出码 3 的断言失败、4 个因范围外跳过。进程异常必须计为失败，范围外单列跳过，均不能并入匹配数。
- 按 D20，本次记录上述已知限制后继续推进正式基础后端，不扩日志或继续修补上游回放；正式后端的确定性、动作、奖励、随机战斗稳定性及性能仍单独验收。本记录只反映上述工具通道的输出，不能替代 Gate 1 第 2–5 项结果。

#### D20 基础修复验证补记（2026-09-05）

- C++ 适配器已重新构建：终局奖励、硬超时判负、无目标卡动作去重及可见状态字段已落地；接口字段顺序由 `HAND_FEATURES` / `ENEMY_FEATURES` / `GLOBAL_FEATURES` 暴露，宽度分别为 3 / 12 / 11。
- 全部现有及新增回归：82 passed，无跳过；T23 已补齐满手牌抽牌失败回归，其他新增测试覆盖失败同分、胜利回报、硬超时终止、动作去重、可见状态及验证器漏报问题。
- 正式后端随机冒烟：训练范围 seed 100000–109999，三遭遇合计 10000 场、183266 步，无崩溃、无非法动作异常、无硬超时；每场累计奖励均符合 D18。单进程测得约 117030 步/秒（本机墙钟时间 1.566 秒，非跨机器性能保证）。另有 60 场完整轨迹的观测、奖励及 info 重放一致。
- 可复现交付：本项目适配器补丁 `patches/lightspeed-battle-env.patch` 可应用于锁定上游的干净源码；版本锁定与构建入口见 `scripts/lightspeed-lock.json`、`scripts/build-lightspeed.ps1`。Python 可编辑安装已成功。
- 评估种子哈希仍为 `38a093486535aa529d54ebfcfd535e67fe043daff8286b5740b5766a6131af9c`。本次适配器补丁哈希为 `91569efde52d3bd5a1f8a82c5b18efa6c023d88109c8e579cf744f3ff7b3c07c`；详细本机冒烟记录在 `build/foundation-smoke.json`（构建产物，不入库）。这些基础结果不消除前述回放初态、日志字段与探针通道的已知限制。

### D21 TokenWrapper 与可训练投影的阶段边界（2026-09-05，Week 4 T1）

- 裁定：Week 4 的 TokenWrapper 只做确定性数据准备，输出按卡牌、敌人、全局分组的实体特征及 mask；类型专用的类别 embedding 和数值投影属于第 11–12 周模型编码器，由模型参数把这些特征映射到共同的 `[n_token, d_model]`。TokenWrapper 不使用固定或随机投影冒充模型编码器。
- 同源约束：FlattenWrapper 与 TokenWrapper 都只读取 `docs/observation-contract.md` 定义的规范观测 dict，共用无效行清零、类别映射和连续值处理约定；两条路径不得从 `info` 或 C++ 内部对象补取字段。
- 理由：投影需要随训练目标更新，把它放进纯数据 wrapper 会混淆可复现的数据转换与可训练表示，也会让 MLP/Transformer 的信息边界难以审计。此裁定澄清原 §4.2 将 TokenWrapper 直接写成 `[n_token, d_model]` 的阶段歧义，不改变 D5/D17/D19 的模型输入信息、动作对应或换位要求。
- 验收：相同规范观测经 TokenWrapper 多次转换必须逐字节相同；投影参数只出现在模型状态中。仅改变隐藏调试信息时，两种 wrapper 输出均不变。具体编码宽度与缩放规则在 T3 实现时记录并测试。
- 推翻条件：若后续框架强制 wrapper 持有模型参数，须先证明同源对照、保存/加载和确定性边界仍清晰，并登记替代接口。

### D22 补齐玩家可见的牌堆无序构成（2026-09-05，Week 4 T3）

- 裁定：正式观测新增 `draw_pile` 与 `discard_pile`，表示玩家当前可查看的抽牌堆、弃牌堆无序构成；绝不暴露抽牌堆真实顺序。当前最小切片仅有 Bash、Defend、Strike 且无升级，故两者均使用 `[bash_count, defend_count, strike_count]` 的 `int32[3]` 精确计数。原 `draw_count` / `discard_count` 暂保留并作为计数一致性校验。
- wrapper：两条路径继续同源。FlattenWrapper 在全局段后依次加入抽牌堆、弃牌堆计数；TokenWrapper 将两堆分开保留为各一个 `[1,3]` 位置明确的 pile token 原始特征，未来模型投影时再加入 draw/discard location embedding。空牌堆仍是有效的全零计数向量，不使用 padding mask。
- 边界：当前三张牌均不会进入消耗堆，消耗堆在合法轨迹中恒空且不影响当前决策，因此本切片不新增 exhaust 字段；第 5 周扩卡若出现可消耗卡，须同步开放其可见无序构成。若未来加入 Frozen Eye 等能公开抽牌顺序的机制，必须另行登记，不得沿用当前隐藏顺序约定。
- 理由：此前观测只有两堆张数。两个状态可以拥有相同手牌、张数、玩家/敌人状态和 mask，却在“Bash 位于抽牌堆还是弃牌堆”上不同，下一轮可抽牌分布和当前决策因而不同；仅用张数会把玩家能区分的状态错误合并。
- 验收：每一步三类卡在 `hand + draw_pile + discard_pile` 中的计数总和均为初始 `Bash=1, Defend=4, Strike=5`；结束回合前后可见构成按实际移动变化；仅重排 C++ 内部抽牌顺序时两个 pile 计数和双 wrapper 输出不变。范围外卡牌直接拒绝，不能静默漏计。
- 推翻条件：扩容后若固定计数向量过宽，可改为带 location 的无序卡牌实体 token，但必须保持相同可见信息、隐藏顺序，并同步调整 MLP 对照输入。
- [O1] ✅ 本机正版 STS1 `desktop-1.0.jar`：`DrawPilePanel.openDrawPile` 可在战斗中打开牌堆；`DrawPileViewScreen.open` 复制全部 draw-pile 卡牌，并在无 Frozen Eye 时排序后显示；`DiscardPilePanel.openDiscardPile` / `DiscardPileViewScreen.open` 显示弃牌堆卡牌。JAR sha256 `cfad868ac8d65a88e71a0bf096fb09f78811e553effe0787c5309a655e081673`；定点反编译产物仅在 `reference/` 查阅、不入库 — 一手 — 2026-09-05。
- [O2] ✅ 用户按正版 STS1 实际界面确认：“显示乱序，但是可以显示有哪些卡” — 一手实测陈述 — 2026-09-05。

#### D22 实现验证补记（2026-09-05）

- C++ `BattleObservation` 已新增两堆三类卡计数并重新构建；Python 规范 dict、FlattenWrapper 与 TokenWrapper 已贯通。训练 seed 100000 的双虱初态为 draw `[1,2,2]`、discard `[0,0,0]`，与各自 size 字段一致；结束首回合后的牌堆移动测试通过。
- 三遭遇固定轨迹逐步检查 `hand + draw + discard = [Bash 1, Defend 4, Strike 5]`，且两堆计数和始终等于全局 size。完整回归 `115 passed in 0.35s`；补丁反向校验、`compileall`、主仓库及上游改动 `diff --check` 均通过。
- 更新后的适配器补丁 sha256 为 `6753a1a7e4f131461de8ef2e7014445ded4527e38ec68fe3ff3d734ae708a511`，替代 D20 基础版本的旧补丁哈希；上游锁定提交不变。

### D23 Agent 决策与环境执行分层（2026-09-05，T4 用户审计后修正）

- 裁定：正式 Agent 统一通过 `decide(observation)` 返回 `AgentDecision`，其中包含掩码后的 `probabilities: float64[31]` 与本次已选的原动作编号 `action: int`。概率表示抽样前的策略分布；整数表示已经发生的单次抽样结果，两者不得混称或互相替代。用户复核后确认 one-hot 与整数表达同一个离散动作，接口直接使用更紧凑的整数，不传冗余 one-hot。
- 随机 Agent：以全零 logits 调用同一 `masked_softmax`，得到合法动作上的均匀概率；随机抽样由 Agent 自己持有的独立 RNG 完成，随后返回所选整数。未来模型 Agent 也在自身边界内根据掩码后分布作出决策并保留训练所需概率或 log-prob。
- 接入层：通用 episode runner 只调用 Agent、严格校验所选整数属于 0–30 且在该策略分布中概率大于 0，再原样交给环境；不持有 RNG，不读取卡牌、能量或敌人字段，不重算合法性。环境只负责 `reset/step` 与产生下一观测，Agent 与具体 wrapper 可互换。
- 终局：runner 根据环境返回的 `terminated/truncated` 停止，不在终局再次调用 Agent。非终局全非法由 Agent 的 masked softmax 显式报错；终局观测全 False 由环境契约测试验证。
- 理由：T4 初版 `MaskedRandomPolicy.act()` 直接返回整数且自身包含 `run_episode()`，把 Agent 决策、动作表示和环境控制流揉在一个类中；未来输出 31 维分布的模型无法无缝替换，且 masked softmax 没有进入随机 Agent 的真实路径。用户审计指出该耦合后，本决策取代 T4 初版接口，不改变 D17 的动作编号或 D19 的信息边界。
- 验收：随机 Agent 的 31 维概率 shape/dtype/归一化正确，返回的整数动作必须属于环境 mask；runner 不含 RNG 且能用同一接口驱动 FlattenWrapper、TokenWrapper 与测试 Agent；两条正式 wrapper 路径均跑到终局并逐步核对后端实际收到的动作。
- 推翻条件：若 PPO/DT 框架要求返回包含 action、log-prob、value 等更丰富的批量对象，可扩充 `AgentDecision`，但必须保留“Agent 内完成选择、runner 不重算合法性和不二次随机”的边界，并先登记迁移方案。

### D24 从最小切片起统一逐牌实体观测（2026-09-06，用户确认）

- 裁定：正式卡牌观测由 D22 的三类牌堆计数迁移为逐张 `CardObservation` 多重集；每张记录至少包含项目稳定 `card_id`、`location`、`upgraded` 与当前可见 `cost`。手牌按后端槽位顺序保存，记录行位置维持 D17 的动作对应，但槽位号不进入卡牌语义特征；抽牌堆、弃牌堆和消耗堆按允许公开的字段确定性排序，重复卡保留重复记录，不暴露真实抽取顺序。
- 注册表：新增简单、版本化的 `CardRegistry`，集中维护 `registry_id`、后端 `CardId`、名称和 `target_kind`。`PAD_ID=0`，当前固定 `Bash=1`、`Defend=2`、`Strike=3`，已有 ID 不因名称排序或扩卡而改变；未知有效后端 ID 必须报错。`target_kind` 明确区分 `NO_TARGET` 与 `ENEMY`，但环境动作仍沿用 D16/D17 的 31 位 `slot * 3 + environment_target_index` 协议。
- 可见字段：当前最小记录不复制后端 `uniqueId`、内部牌序、RNG、`specialData`、`freeToPlayOnce` 或 `retain`。`cost` 在手牌中表示当前可见费用；非手牌区域在完成显示语义核验前使用 `cost_known=false`，不能把未知费用冒充真实 0。后续机制确需新的玩家可见实例属性时，先登记字段含义与可见性再扩 schema，不预建万能效果 DSL。
- 规范布局：语义层保留 `hand/draw_pile/discard_pile/exhaust_pile` 四个变长记录列表；张量准备层将手牌放在固定 10 行，将三个非手牌区域合并到可配置的 `pile_capacity` 行，以 `location` 区分。当前最小切片 `pile_capacity=10`；超容量在丢信息前报错，不截断。`global` 增加由有效记录导出的 `hand_count` 与 `exhaust_count`，四区 count 与有效记录必须一致。
- 双路径：FlattenWrapper 与 TokenWrapper 共用注册表、规范记录、padding 清理、dtype、缩放和 mask。类别以整数传递，类别 ID 不作为连续大小输入；wrapper 不含可训练 embedding。为支持 MLP 中的类别 embedding，FlattenWrapper 名称保留，但返回契约改为类别整数、数值、known/valid 位及动作 mask 的结构化 `FlatInput`，由 MLP 在模型内编码后 flatten；TokenInput 保留相同字段的实体维度。D21 的“可训练参数只属于模型”继续有效。
- 阶段边界：本次只前移观测协议及数据转换，不前移 PPO、Set Transformer、动作评分网络或 DT 序列模型。D22 的计数版实现、测试数字与 Gate 1 记录作为历史输入版本保留，不能冒充实体版已经验收。实体版必须经正式 C++ 路径、第四类合成卡探针、双路径信息等价、隐藏顺序、padding、容量、动作闭环、确定性、稳定性及性能重新验证后，才可记为实施完成。
- 理由：D22 已证明三类计数在原最小切片中信息完整；但第 5 周扩至 30–40 张卡时，按牌名增长固定计数列会持续修改 wrapper 结构。统一逐牌记录能让新增可用现有字段描述的卡主要通过注册数据接入，同时保持 MLP 与 Set Transformer 的同源信息边界。这是表示策略与实施时点调整，不否定旧实现的正确性。
- 推翻条件：若新机制出现现有字段、可见性或动作语法无法表达的事实，针对该机制登记 schema 扩展；不得静默截断、合并不同实例或退回按牌名特判的正式输入。若性能下降，先实测区分 C++ 导出、排序、padding、wrapper 和模型成本，再登记替代方案。

#### D24 P2 实现补记（2026-09-06）

- C++ 项目适配层、pybind 与 Python 规范观测已完成四区逐牌导出；非手牌记录在 C++ 边界使用允许公开字段排序，Python 注册表映射为项目稳定 ID，并交叉检查四区 count。锁定上游提交和 31 位动作协议不变。
- 扩展重新构建成功；C++ 接口与 Python 薄包装的针对性回归 `39 passed in 0.28s`，补丁反向检查、`compileall` 及两层 `diff --check` 通过。适配补丁 sha256 为 `619d7d1c00ec4fd9c1e1499d9a3b37d84d8b98f01cc932d68f61be07ce2e7a6e`，本机构建扩展 sha256 为 `e55552b105755889b1130967420f37591cc6e74433fcac370d8218ff7bda4ec9`。seed `100000` 双虱结束回合前后的四区数量为 `5/5/0/0 → 5/0/5/0`。
- 此补记只表示 P2 源头与规范层完成。完整套件实际为 `110 passed, 28 failed in 1.20s`；28 项均位于仍是 V1 的双 wrapper、依赖闭环及旧序列化入口，须由 P3 恢复全绿。第四类探针、压力和性能尚未执行，不能据此宣称 D24 全部验收完成。

#### D24 P3 实现补记（2026-09-06）

- 双 wrapper 已迁移到共享的 V2 prepare，分别返回结构化 FlatInput 与保留实体轴的 TokenInput；卡牌类别保持整数，数值、known、valid 分离，padding 全部使用 PAD/0/False。旧 V1 wrapper 常量和返回字段已删除。
- Week 4 验收序列化已迁移四区逐牌记录并升为 V2，报告元数据加入 schema、registry 版本及内容哈希。完整套件实际为 `145 passed in 0.72s`，P2 记录的 28 项阶段失败已全部恢复。
- 本补记只完成 P3。现有 Agent 闭环回归继续通过，但第四类合成卡扩展探针属于 P4，一万场稳定性和分路径性能重验属于 P5，均尚未据此宣称完成。

#### D24 P4–P5 验收补记（2026-09-06）

- P4 已补第四类合成卡探针：只在测试 registry version 2 登记新类别，并把记录分别放入手牌、抽牌堆、弃牌堆和消耗堆；Flatten/Token 无按牌名修改即可逐字段恢复相同信息。未知 ID、区域错配与非手牌超容量仍明确失败。真实双 wrapper × 三遭遇的 AgentDecision/runner 闭环、槽位与目标路由、无目标列 0、结束动作 30 和终局停止继续通过。
- P5 按 V2 重新执行 10000 个唯一训练配置、四路径各三轮及 60 场分层确定性重放。每路径每轮均为 10000 场、184117 步、8711 胜 / 1289 负，异常、非法动作、硬超时、奖励契约失败均为 0；路径内及跨路径规范语义失败为 0。raw/dict/Flatten/Token 单核完整路径中位数分别为 14702/5882/3841/3790 steps/s，全部超过 2000 门槛。
- 完整回归为 `146 passed in 0.74s`；实体版独立报告见 `docs/entity-foundation-report.md`。D24 工程迁移验收完成，可以回到 Week 5–6 主线；第四类测试卡不进入正式 reset，不代表后端或模型支持新卡机制。

### D25 在扩容前用最小切片完成 PPO+MLP 冒烟（2026-09-06，用户确认）

- 裁定：把 D24 已验收的 A0 三遭遇、三类基础卡固定为 `minimal-v1` 诊断环境。在继续中等档代码扩容前，先接入可复用的 MLP policy/value、masked PPO 最小训练环和配对诊断评估；Set Transformer 仍留在 MLP 与 PPO 主链路稳定之后，不在本阶段训练。
- 冒烟分三层：第一层检查输入批处理、类别 embedding、前向、pre-softmax mask、有限梯度、参数更新与保存重载；第二层用固定小样本做过拟合诊断，区分模型/动作头错误与 RL 探索问题；第三层才在 `minimal-v1` 上训练 PPO+MLP，并与均匀合法随机策略比较。前两层通过不等于策略已经学会，第三层失败也不能直接归因于环境。
- 评价口径：当前随机策略 10000 个配置总胜率已经为 87.11%，Two Louse 桶为 100%，所以冒烟主指标使用 D18 的逐局总回报差，并报告胜率、剩余 HP、每遭遇分桶和 paired bootstrap 置信区间；不能仅凭总胜率判断学习成功。正式 `eval_seeds.json` 不用于反复调参：开发训练和诊断留出 seed 都取互不重叠的 `>=100000` 区间，最终 Gate 仍使用固定评估 seed。
- 扩容方式：M1 来源审计可保留现有记录，但 M2/M3/M4 的实现改为机制批次递增；每批先补规则与观测回归，再以相同初始化、训练预算和诊断 seed 从头短训。上一批通过而本批失败，只把排查范围缩小到本批新增字段、动作、机制和分布变化，不把时间相关性冒充单一因果证明；warm-start 可作附加诊断，不能替代从头复跑。
- Gate 边界：本次是扩容前诊断检查，不新增正式 Gate，也不提前宣称 Gate 3 通过。Gate 2 仍在锁定的中等档上比较规则与随机，Gate 3 仍要求中等档 PPO+MLP 显著优于随机，Gate 4 仍保留 MLP 与 Set Transformer 的正式配对对照。
- 理由：先在机制少、已通过稳定性验收的环境中建立学习闭环，可以把后续失败区分为“基础模型/PPO 从未工作”或“某批扩容后退化”；同时保持 v4 原本“先 MLP、后 Set Transformer”的诊断顺序。简单环境下两种架构效果接近只是待检验假设，不作为跳过最终 Gate 4 的依据。
- 推翻条件：若 `minimal-v1` 的随机策略天花板导致回报也几乎无可分辨空间，则保留前两层结构诊断，并用第一个仍可审计的小扩容批次完成策略学习冒烟；若单一 PPO 问题连续三天无进展，执行 §10.3，而不是继续扩环境或提前引入 Set Transformer。

#### D25 S1 实现补记（2026-09-06）

- 新增 MLP 模型内类别 embedding、数值/敌人/全局投影、两层 trunk、31 位 policy head 与 value head；`FlattenWrapper` 仍是无参数的确定性数据准备层。policy 分布在 softmax 前使用环境提供的 mask，PPO 后续可从同一分布复算 action log-prob 与 entropy。
- checkpoint 保存模型配置、输入 schema、registry version/hash 与权重；当前注册表发生变化时显式拒绝加载旧 checkpoint。S1 专项 `4 passed in 5.16s`，完整回归 `150 passed in 4.99s`，失败 0、跳过 0；`compileall` 和 `git diff --check` 通过。此结果只完成 S1，不代表 S2 过拟合或 S3 PPO 学习已经通过。

#### D25 S2 实验约定（2026-09-08，运行前登记）

- 固定数据：环境 seed `200000..200023`，随机 Agent seed `300000..300023`，三遭遇各 8 局；选样 RNG `400000`，各取 16 个至少有两个合法动作的非终局观测，再增加 mask 完全不变、动作标签随不同手牌换位的配对。标签是随机行为的实际动作及单次 Monte Carlo 回报，不是专家答案。回报必须包含失败 0 和足够方差；同批记忆不能证明泛化或游戏策略改进。
- 固定预算：模型初始化 seed `500000/500001/500002`，每次 CPU 单线程全批量 Adam、学习率 0.001、1500 次更新，policy NLL + 1.0 × value MSE。每次均须准确率 >=99%、换位配对全对、policy NLL 降至初值的 <=10%、value MSE <=0.001 且 <=初值的10%；完整重跑第一 seed，检查数据、曲线及权重逐字节一致。门槛在查看结果前固定。
- 数值处理补充：S1 的原始 HP 等数值直接进入 tanh 已在初态产生饱和；S2 起在模型内使用明确的正数尺度，费用/能量除以3，HP/block/total_enemy_hp/intent_damage除以100，回合除以50，牌数除以10，强度/层数/次数除以10，one-hot不变。缩放不裁剪、不从评估集拟合，写入模型版本和 checkpoint；后续 Set 模型共用这套尺度。wrapper 仍输出相同原始事实。
- MLP 边界：本诊断沿用 D25 S1 的 flatten MLP 与31位输出。换位训练样本用于测试是否能学到槽位对应，不主张该模型在结构上满足 Set Transformer 的置换等变性。D17 的实体保留与共享候选评分仍在 Set/DT 实现时验收。

#### D25 S2 实现补记（2026-09-08）

- 81样本=48原始轨迹观测+33同mask不同动作的手牌换位观测，mask-only策略的同批准确率上界43.21%。三种初始化按预先登记预算运行，动作及换位对准确率均100%；policy NLL约0.00021..0.00023，三个value MSE分别为0.00085089、1.53e-8、2.61e-8，均满足门槛。首组完整重跑数据、曲线、权重一致，checkpoint重载一致。
- 数值缩放已落实，checkpoint格式升2；HP/energy梯度、尺度篡改拒绝、真实轨迹重放与五步训练失败负对照通过。完整工作树191 passed、0 failed、0 skipped；compileall通过，Ruff未安装未执行。本次代码与数据另存源码归档，详见 `docs/mlp-overfit-report.md`。S2工程完成，S3 PPO与S4策略比较尚未执行，学习理解不据此自动勾选。

### 2026-09-08 审核实现补记（沿用 D16/D23/D24/D25，不新增设计裁定）

- 给定模型动作严格校验 int64、批量 shape、设备、范围和环境 mask，防止 `[B,1]` 广播为 `[B,B]` log-prob；环境控制参数拒绝隐式整数截断；checkpoint 保存/加载均核验词表容量与注册表是否相容。
- T5 结果拆分本次 `checks_passed` 与正式 `passed`，后者继续要求已有的一万配置、三轮性能和三遭遇各二十场重放。小规模、零样本、缺少轮次与 NaN 不得冒充正式通过。
- 修复后回归 170 通过；纳入同期 S2 改动后 191 通过，均无失败、无跳过。历史 D24 报告仍满足修正后的判定；本次小规模首次性能失败与复查通过分别保留，不能替代正式复验。
- 完整证据、已修复问题、五行敌人观测与三目标动作的扩容约束见 `docs/project-audit-2026-09-08.md`。本补记不改变游戏规则、奖励、信息边界、动作协议或 Gate 排期。

#### D25 S3 运行前约定（2026-09-08）

- CleanRL起点锁定`fe8d8a03c41a7ef5b523e2e354bd01c363e786bb/cleanrl/ppo.py`，对应与许可见`docs/ppo-source.md`。采集时保存原mask、动作、log-prob、value及本transition终止标记；PPO更新使用同一个原mask重算分布。终止不自举，rollout切段仍自举，拒绝把reset的新局价值计入上一局。
- 首轮预算262144步，8环境×128步采样，4epoch×4minibatch，学习率0.00025线性衰减，gamma=1、GAE lambda=0.95、clip=0.2、entropy=0.01、vf_coef=0.5、Adam eps=1e-5、grad norm=0.5。参数在`configs/ppo-minimal.yaml`落盘；模型从头初始化，不用S2记忆权重。沿用MLP共享主干，线性层按CleanRL正交初始化。
- 训练环境seed锁定[1000000,2000000)，初始化/动作采样/minibatch洗牌分别600000/610000/620000。训练前后在900000起的三遭遇各100局开发样本上配对比较，策略按分布抽样，开发诊断不消耗训练RNG；eval_seeds.json保持不变且不用于调参。
- U2选择TensorBoard（本机日志），同时保存JSON曲线、逐局动作/回报、配置、来源版本、源码归档、可恢复训练checkpoint。批量PPO Agent在内部选择动作并保存log-prob/value，采集器仅转交；扩展D23的批量训练输出，不改变原单局AgentDecision接口。
- 完成判断分开：S3需要有限更新、合法性、可追踪监控与最小环境学习信号；单初始化开发CI不冒充多训练seed证据，不提前完成S4/S5或中等档Gate3。S4再在固定预算下检查多个初始化，S5才正式冻结代码与配置。

### D26 基于项目审核发布 v5，固化接口容量与训练验收边界（2026-09-08）

- 裁定来源：用户要求“基于项目审核报告修改项目方案 v4，改成项目方案 v5”；依据 `docs/project-audit-2026-09-08.md`，并纳入已登记的 D25 S3 运行前约定。
- 文档裁定：`spec-v5.md` 成为唯一执行依据，`spec-v4.md` 冻结为历史方案；README、AGENTS 与当前执行计划同步入口，历史报告、来源核验和运行快照保留当时的 v4 引用。18 周、540 小时、A 后 B、五个 Gate 及既定降级路径不重置。
- 文档衔接：学习目标与方法使用同期新增的 `docs/learning-path-v2.md`，其执行依据同步为 v5，旧 `学习路径.md` 保留历史。Gate 1 若曾正式触发“锁定最小切片”降级，后续 Gate 在登记的降级范围继续，门槛和周次不变；当前 Gate 1 已通过，仍按 D25 在中等档验收 Gate 2/3，不得用最小冒烟替代。
- 进度裁定：D24 与 D25 S1/S2 已有工程通过证据；S3 当前已有实现文件，但尚无本次核对可确认的训练验收产物，记为“实现进行中”；S4/S5 与中等档待完成。工程通过与用户理解复盘分开，审核的 191 项回归和历史一万配置不冒充本次重跑。
- 扩容裁定：M0/S1–S5 收口后，M1/M2 必须先集中定义手牌、敌人观测、动作目标、牌堆容量、字段顺序、类别表与版本。当前保留 31 动作与三个目标列；候选遭遇必须检查分裂、召唤后的最大目标索引。Gremlin Gang / Lots of Slimes 暂不开放；51 动作只是五目标迁移的条件示例，实施前另记决策并联动 C++、mask、双 wrapper、模型头、checkpoint、轨迹与验收，禁止静默截断或自动代选二次目标。
- 训练裁定：模型侧固定数值缩放由 MLP/Set 共用并校验编码版本；给定动作须为同设备 `int64[B]` 并通过原观测 mask 校验。PPO 显式保存本步观测/mask、动作、旧 log-prob/value、奖励、终止标记与批次末状态，使用原 mask 更新；任务硬超时不自举，采样切段在未终止时自举，优势不得串到 reset 后的新战斗。
- 复现裁定：每次训练同时保存源码快照与逐文件哈希、完整配置、各 RNG/环境 seed、锁定依赖与扩展指纹、评估文件哈希、曲线与 checkpoint。PPO 增加优化器、训练计数、RNG 与进行中战斗恢复依据；必须验证中断恢复后的下一批轨迹、更新与权重一致，不能仅凭可加载权重宣称可续训。并行实现使用独立工作树；正式性能验收避免并发计算干扰。
- 验收裁定：报告分列通过、失败、跳过、未验证；小规模 `checks_passed=true` 或退出码 0 不等于正式 `passed=true`。正式基础验收沿用一万唯一配置、四路径各三轮及三遭遇各二十场重放；扩容版本须补新范围证据，不能沿用旧吞吐。正式与开发评估均以 D18 回报差为主，修正 Week 5–6 计划误写的“胜率差”口径；不改变 Gate 2/3 的回报 CI 门槛。
- 理由：审核发现容量不一致、广播混样、checkpoint 兼容性及证据判定风险；将已修复的问题与下一阶段约束写进方案，减少在新机制和新算法同时变化时失去定位依据。
- 推翻条件：只有新的实测证据表明任务范围或协议确需改变时，先登记新的决策及受影响产物，再修改 v5 与实现；本条不授权跳过既有 Gate、不启动额外框架工程，也不把待实现事项记为完成。

#### D25 S3 验收补记（2026-09-08）

- 首轮固定262144步、256次PPO更新完成；去重后17803个完整episode，训练和开发对局非法动作与超时均0。300局开发配对中，PPO/随机回报1.439583/1.177479，差值95%CI=[0.219811,0.308563]，胜率100%/89.33%；三遭遇回报差CI均为正。此为单初始化S3信号，不完成S4/S5或中等档Gate3。
- 首个进程记录到64轮但只保留32轮完整checkpoint，已从32恢复完成256；重叠32轮算法指标和2026条对局记录均一致。单测验证恢复后下一批轨迹、指标、RNG和权重一致。末轮explained variance=-0.2573，保留value拟合诊断项，不声称已完成调参。
- 完整回归259 passed、0 failed、0 skipped；本轮新增训练/诊断范围Ruff与compileall通过。运行档案、源码哈希、最终权重与边界见`docs/ppo-minimal-report.md`。更新当前进度为S3工程完成，理解复盘另列；下一执行点S4。

### D27 发布 v6，正式拆分 battle 与 run 奖励任务（2026-09-08，用户裁定）

- 裁定来源：用户提供 `STS_Reward_Strategy_v5_to_v6_Agent.md` 并要求按其把项目修改为 v6；以仓库真实 v5、现有实现和历史产物为底稿，保留无关章节、排期、Gate 和历史证据。
- 版本裁定：`spec-v6.md` 成为唯一执行依据，`spec-v5.md` 冻结为历史。文档版本与运行时奖励版本分离；当前战斗使用 `battle_reward_v1`，未来完整爬塔使用 `run_reward_v1`，不能用笼统 `win` 混合两种成功。
- battle 裁定：首次战斗胜利终局奖励 `1 + 0.5 * hp_exit/max_hp_exit`，其他步骤、失败及任务硬超时均为0，gamma=1、beta=0。退出 HP 在最后动作、连锁和已实现的战斗结束效果全部结算后读取；任何真终局的 HP 非法时明确报错。0.5 在本次正式对照锁定，后续改变须建立新奖励版本并处理数据兼容，D7 原“按需校准一次”不再生效。
- run 裁定：仅本次转移首次完成版本化最终通关谓词时奖励1，其他所有步骤、普通战斗胜利、死亡和规则硬超时为0，gamma=1、beta=0。普通战斗结束不结束全局 episode，不重置 PPO/DT 的全局轨迹和 RTG。完整 RunEnv 尚未实现，当前只接入奖励/版本契约；在角色、难度、开局分布、终点、上限、失败条件、`task_spec_id` 与 `success_predicate_id` 齐全前拒绝正式 run 训练。
- 边界裁定：任务成功、死亡和任务硬上限是真终止且不自举；rollout 收满但任务继续时从后继状态自举；外部截断使用 reset 前最终观测自举，但优势不跨 reset 递推。异常轨迹单独报告，不伪造成零回报失败样本。
- 数据裁定：同一任务内 MLP-PPO、Set-PPO 与 DT 使用相同逐步 reward、gamma 和完整真实 RTG。先在完整 episode 计算 RTG 再切窗口；外部截断且未恢复到终局的轨迹不赋精确失败0。battle 数据缺少整局结果时禁止改标 run 成功。奖励变化只从可验证原始字段或确定性重放重算，保留旧版本；字段不足则重新生成。
- 势能裁定：v6 基线固定 beta=0，不实现势能模型、不拼接手写代理分数、不把在线 critic 当势能。未来实验须另记固定有界势能、冻结预处理、全部真终局势能为0、截断不清零及完整轨迹抵消测试；势能保持固定起点下策略排序，不保证累计数值逐条相等。
- 落地裁定：C++ 继续是 battle 奖励唯一产生点；Python 共享契约只做校验、元数据和离线重算，训练/评估不得再复制公式。新训练 manifest、episode 与 checkpoint 记录任务、奖励、终止版本及 outcome/reason，恢复训练逐字段核验。v5 时点的 S3 checkpoint 不能静默恢复为 v6 checkpoint；可显式 warm start，并须重新评估。
- 验收边界：R1–R14 的当前可执行 battle、GAE、RTG 和版本分支须以确定性测试验证；真实 RunEnv、外部截断环境集成、终局状态反序列化及 DT 端到端仍按实际状态记未接入，不能由纯函数测试冒充完成。
- 推翻条件：只有用户明确改变任务目标，或真实规则/数据证据表明契约需修订时，先登记新任务/奖励版本、受影响数据与 checkpoint，再修改实现；不得静默覆盖 `battle_reward_v1` 或 `run_reward_v1`。

## 未决事项（v6 §12，不阻塞开工）

| # | 事项 | 何时定 | 默认 |
|---|---|---|---|
| U1 | ~~动作头是否保留实体对应关系~~ | **已裁定 → D17（2026-09-05）**：逐个候选动作打分，保留扁平动作编码；具体评分函数第 11 周定 | — |
| U2 | ~~W&B vs TensorBoard~~ | **D25 S3采用TensorBoard，本机日志与JSON双存档（2026-09-08）** | — |
| U3 | 中等档卡表来源核实 | **已销账（2026-09-11，M1 收口）** | 正版 JAR 全卡来源审计、原十张 class/升级方法定位和候选/排除表已记录；行为与接口验收仍属 M2/M3 |
| U4 | ~~`sts_lightspeed` 是否纳入~~ | **已裁定 → D15（2026-09-04）**：build 成功，纳入 | — |
| U5 | ~~battle 的 HP 系数~~ | **已裁定 → D27**：`battle_reward_v1` 固定0.5；以后改变必须新建版本 | — |
| U6 | ~~oracle 主通道~~ | **已裁定 → D16/D20（2026-09-05 修订）**：C++ 正式后端的基础回归 + 现有 runlogger 有限字段校准；Python 差分仅作教学及具体差异定位 | — |
| U7 | ~~反编译源码查阅政策~~ | **已裁定 → D13（2026-09-03）** | — |
| U8 | ~~奖励基础版本的折扣、边界与回报累计口径~~ | **D20 初裁，D27 按双任务修订**：gamma=1、beta=0；真终止不自举，外部截断自举但不跨 reset；真实 RTG 由完整轨迹计算 | battle/run 版本不得混用 |

## 附 B 未核实事实销账表（v4 附 B → 实测后填）

| # | 事实 | 状态 | 核实记录 |
|---|---|---|---|
| 1 | runlogger 的局数、卡牌覆盖、schema | 部分（2026-09-05） | 已有两份真实日志供有限校准；当前 schema 不覆盖全部目标状态。回放初态差异、有限字段匹配与终局缺失见 D20 本次校准记录；完整局数和机制覆盖未销账，按 D20 不扩日志工具 |
| 2 | sts_lightspeed 的 API / 构建 / 覆盖 | **已销账（2026-09-04，D15）** | gcc 16.2 + CMake 4.4 + Ninja 编过（3 项构建摩擦及解法见 D15）；`test.exe` 提供 `replay <seed> <asc> <actionFile>` / `simple_agent_mt` / `mcts_save` 等命令；冒烟 3 局 playout 2.5ms（1 线程）；Python 绑定 pybind11 2.7.1 × CPython 3.13 编译失败，路线第 3 周定 |
| 3 | HF 卡表数值准确性 | 待实测（第 5 周） | — |
| 4 | 正式 C++ 后端吞吐（D16 修订） | 以当前构建的实际测量为准 | Gate 1 第 5 项约束正式训练后端；纯 Python 吞吐仅作教学参考，不再作为该门槛 |
| 5 | determinismfix 是否必需 | 待实测 | runlogger README 明言游戏有可复现性 bug、需装 Determinism Fix，基本确认必需 |

## runlogger 侦察记录（2026-09-03）

- 仓库：https://github.com/colinking/runlogger （Steam 创意工坊订阅安装）
- 日志格式：每行一个 JSON 对象，含 `_type` 字段标明对象类型
- 路径：进行中的 run 在 `saves/IRONCLAD.run.log`；完成后改名并移入游戏目录下 `runlogs/<角色>/<时间戳>.run.log`
- 风险：mod 处于 beta，格式可能 breaking change → 拉取日志时锁定 mod 版本并记录
- 第 1 周剩余动作：本机游戏是否已装此 mod；统计 `runlogs/` 实际局数、卡牌覆盖；schema 快照存 `docs/` 或 `research/`
- 核验来源：runlogger GitHub README（2026-09-03 抓取）

## 决策变更记录

| 日期 | 变更内容 | 触发条件 | 新决策要点 |
|---|---|---|---|
| 2026-09-05 | 补充 D5，裁定 U1，修订 spec-v4 §7 的动作头与 §12 未决事项 | 项目审核发现集合池化丢失槽位对应关系；向用户解释换位反例后获确认 | D17：保留实体编码，逐个候选动作打分后排列为原有动作编号；PMA 用于价值及上下文；A/B 均保留动作与当前实体的对应关系 |
| 2026-09-05 | 补充 D7、D16，修订 spec-v4 §4.4 的最终成绩定义，新增 U8 | 项目审核发现失败轨迹因剩余敌方势能而异分；用户确认所有失败应同分 | D18：失败及硬超时为 0，胜利按剩余血量加分；辅助反馈及 A/B 累计回报须符合该目标，具体边界与折扣待统一确定 |
| 2026-09-05 | 补充 D5，修订 spec-v4 §3、§4.2 的观测信息边界 | 当前 C++ 观测漏掉部分状态；用户确认模型只使用玩家可见信息 | D19：补齐可见字段，排除隐藏顺序、内部 RNG 与未来结果；统一 A/B 状态信息边界，区分 DT 训练回报标签与评估时的目标条件 |
| 2026-09-05 | 新增 D20，修订 D2/D4/D9/D11 与 spec-v4 的 Gate 1、日志校准范围和排期出口 | 用户拒绝当前扩日志，明确优先快速基础建设与学习；原门槛未先核实日志字段 | 有限字段校准并诚实记录未验证项；日志覆盖不足不阻塞训练或单独触发砍扩容；其他基础门槛与后续 Gates 保留 |
| 2026-09-05 | 依 D20 同步 D7/D16/D18、spec-v4 §4.4，销账 U8 基础版本 | 用最小实现落实已确认的失败同分与 A/B 累计一致目标 | 非终局 0，终局按胜负及剩余 HP 计分，γ=1；暂不实现势能反馈；实现验收另行报告 |
| 2026-09-05 | 修订 D14，同步 mechanics.md 的 RNG、Cultist 与弃牌顺序 | 审核确认当前实现已改变，旧决策与规格未同步；此处为本次正式登记 | 主 RNG 为 xorshift128+、洗牌临时 Java LCG；combat seed = run seed + floor，misc 继承整局 RNG；教学 Python 仍有近似；Ritual 跳过首次增长、回合末逆手牌弃牌 |
| 2026-09-06 | 新增 D24，修订 D5/D21/D22 的正式输入版本并同步 spec-v4 §3/§4.2/§7.1/§9 | 用户要求从可扩展最小原型起使用逐牌实体，并在 Week 4 学习复盘后确认继续实施 | 四区逐牌记录、稳定注册表、location、同源结构化双输入；保留 31 位环境动作；模型与 DT 仍按原周次实施 |
| 2026-09-06 | 新增 D25，调整 spec-v4 §9 的 Week 5–10 顺序并重排 Week 5–6 计划 | 用户要求先在当前最小环境做 MLP 冒烟，再分批扩建，以缩小新增状态或机制引起的训练失败范围 | 扩容前完成三层 PPO+MLP 诊断；Set Transformer 继续后置；不新增 Gate，Gate 2/3/4 的正式环境与门槛不变 |
| 2026-09-08 | 新增 D26，发布 spec-v5 并冻结 v4、切换当前入口 | 用户要求基于项目审核升级方案 | 统一容量与动作审计、PPO rollout/批量契约、源码与恢复证据；纠正进度和回报判定口径，保留预算、Gate 与历史证据 |
| 2026-09-08 | 新增 D27，发布 spec-v6 并冻结 v5、拆分 battle/run 正式奖励 | 用户提供 v5→v6 奖励任务书并要求执行 | battle 保留终局胜利加 HP，run 仅最终通关得1；补终止/截断、RTG、版本、迁移和势能研究边界；不扩建完整 RunEnv |

### D25 S4 运行前登记（2026-09-11）
用户要求后台先运行测试。三组新初始化各固定262144步，共用minimal-v1与300局开发配对配置；seed、报告口径和失败保留规则见 docs/ppo-s4-preregistration.md。沿用D25/D27，不修改正式Gate；此条为实验登记，不是完成声明。

### D25 S4 结果登记（2026-09-11）
预登记A/B/C均完成262144步，300局开发回报差CI下界分别0.220873、0.217833、0.218790，九个遭遇分桶CI均为正，达到本轮S4改进门槛。详见docs/ppo-s4-report.md。末轮EV负值保留诊断；当前三组后端哈希一致但与历史S3报告不同，原因未核实，不宣称历史二进制一致。S5冻结与正式Gate未完成。

### D25 S5 冻结登记（2026-09-11）

三组第224轮恢复至256轮，指标、轨迹、最终checkpoint全内容和开发评估完全一致；278项完整回归通过。后端哈希变化对应D27奖励校验补丁及重建，上游锁定提交不变，未声称历史二进制等价。三组全部原始运行及实际后端纳入本地冻结包，细节与边界见docs/ppo-s5-freeze.md。S1–S5工程完成，下一点M1；S5理解另行验收，不扩展Gate结论。

### M1 全卡审计复核补记（2026-09-11）

- 裁定：按用户要求将审计覆盖扩至全部 Ironclad 卡；正式目标仍是 30–40 类，当前 31 动作不变。`docs/ironclad-card-audit.md` 是候选审计，不是正式白名单。
- 理由与纠错：正版 Ghostly Armor 升级仍虚无；Whirlwind 的 X 不需玩家另选；Heavy Blade 的通用伤害函数已补足一次力量。撤销对应误判；Infernal Blade 必须检查生成卡的后续选择。证据见报告 §10。
- 后续（收口前状态）：修订统计为 75 类/150 版本，A6/B123/C16/D5；`docs/m1-candidate-batches.md` 提供 35 类实施候选与 M2 前置要求，未批准 reset 配置。原十张数值证据及候选行为收口前，U3 仍未销账。
- 推翻条件：正版定点证据、后端完整调用链或行为测试发现反例时，重新分类并登记；不以静态入口覆盖或已有最小环境测试冒充扩卡验收。

### M1 候选方案收口（2026-09-11）

- 裁定：M1 以“全卡来源审计 + 候选分批方案”收口，不以行为测试或代码扩容作为 M1 完成条件。正版 JAR 的 75 个 Ironclad 类、150 个基础/升级版本已覆盖；35 类实施候选与排除理由记录在 `docs/m1-candidate-batches.md`。
- 数值证据：原十张卡的正版 class 构造器/升级方法偏移、`cards.json` 文本键、锁定后端调用链已补入 `docs/m1-numeric-evidence.md`。这完成 M1 来源证据补充，但不把静态证据写成运行时行为通过。
- 边界：候选方案不是正式白名单；不改变当前 31 位动作、reset、牌组、容量、观测、训练代码或后端。扩展卡基础/升级行为测试、正式 registry、M2 固定牌组/容量/观测契约仍未完成。
- U3：按“中等档卡表来源核实”维度，以正版 JAR 一手定位和候选/排除表收口；后续行为与接口验收属于 M2/M3，不得回填为 M1 行为通过。
- 后续：进入 M2 前，另行锁定固定牌组、遭遇、手牌/目标/非手牌容量、可见观测字段及版本契约；若证据发现反例，按本日志先修订分类，不静默开放卡牌。

### M1 收口审核与 M2 首批启动（2026-09-11）

- 审核结论：M1 来源与候选阶段收口成立；U3 销账不包含全卡行为验收。原十张证据、候选分批及规格状态一致，进入 M2。
- 首批设计：medium-b1-v1 使用三个固定15张牌组，覆盖基础3类和首批11类；仅当前三个遭遇、基础版、无生成牌路径。非手牌容量15、手牌10、敌人观测5、可寻址目标3，动作31不变。详细字段、容量证明与迁移要求见 docs/m2-battle-contract.md。
- 理由：真正扩展牌组和类别，同时用无生成牌批次建立可证明容量，按D25隔离新增机制。旧minimal-v1保持独立回归配置。
- 边界：首批契约设计完成，不声称代码已接入或整个M2完成；后续持续状态、生成牌和新增遭遇另建契约，行为验收后才可运行训练。
- 推翻条件：配置存在漏审的可达生成、选择或隐藏状态依赖时，修订本批契约并重验，不静默改变规则。

### M2 训练初态改用公开玩家对局来源（2026-09-11，用户纠正）

- 裁定：撤销将人工“基础十张加五张”牌组作为正式训练/评估分布的首批设计；该配置只保留机制诊断用途。用户指定的是公开玩家对局库，不是本机个人runlogger。
- 新路线：先核查公开STS1数据的版本、字段与可重建性，筛选与支持卡牌、遭遇、遗物、药水和状态一致的战斗入场场景；不能删掉不支持的牌/遗物后仍称原始真实场景。没有匹配样本时报告缺口，必要的范围调整另行登记。
- 数据划分：先按整局run及重复seed/重复记录分组，再划分训练、开发和保留评估；同源战斗与其随机复跑不得跨组。不按输赢筛选。现有eval_seeds.json保持不可变，场景清单需另行版本化；新协议经登记后实施，不能默改旧评估含义。
- PPO用法：公共记录提供初态分布，由当前策略在模拟器重新采样轨迹；不直接把玩家历史动作作为PPO的on-policy批次。重建的真实可见初态与无法恢复而重新采样的隐藏状态分别记录，不冒称精确回放。
- 证据：候选大型库为“77 Million Runs — An STS Metrics Dump”，原帖 https://www.reddit.com/r/slaythespire/comments/jt5y1w/77_million_runs_an_sts_metrics_dump/ 。原帖称数据主要来自2018–2020，不能直接假定与锁定正版版本一致；尚未下载核验其数据包。另已读取 MaT1g3R/Slay-the-Spire-data 仓库的一份样本，具有牌组、逐层血量和事件等统计，但这不证明全部入场状态可精确恢复。
- 下一步：对公开数据小样本做字段与重建审计后，再决定容量、配置和训练/评估场景清单。M1卡牌来源成果保留，M2仍进行中。

### M2 范围修订：按公开样本分批纳入遗物、药水与升级（2026-09-11）

- 裁定：撤销“第二档一概不含遗物、药水、卡牌升级”的旧范围表述。遗物、药水和升级属于公开玩家战斗初态中可能影响转移的输入，允许按真实样本、字段证据、动作语法和后端机制逐批支持；这不是今天的正式白名单，也不是已完成的机制实现。
- 不变项：`minimal-v1` 仍保持原固定初始卡组、A0、三种遭遇、旧 schema/registry/31 动作及旧回归；新场景不得回填或改标旧环境与旧 PPO 结果。
- 初态时点：M2 采用“战斗初始化、开战效果和初始抽牌均已结算之后”的入口作为候选规范时点。若来源只记录这一时点，reset 注入不得再次触发开战遗物/效果；若只记录战斗前或时点无法对齐，场景排除或降为诊断，不补默认值。
- 证据边界：必须同时保留牌组实例/升级、遗物 ID 与动态计数、药水槽位、当前/最大 HP、遭遇、进阶、版本、可见状态与公开意图；历史最终 `master_deck` 不能替代入口牌组，缺失计数不能视作0，隐藏 RNG/牌序只能重新采样时必须显式记录。
- 交付状态：`scripts/audit-public-runs.py`、`docs/m2-public-data-audit.md`、`docs/m2-real-battle-contract.md` 负责本修订的审计与候选契约；原人工15张牌组文档 `docs/m2-battle-contract.md` 仅保留机制诊断历史身份。
- 推翻条件：真实样本证明某批存在未记录的可达生成、二次选择、容量或隐藏状态依赖，先修订本决策与契约，再决定是否延期；不得为了达到卡牌数量删除不支持内容、把高进阶降为A0或让后端代替玩家选择。

### M2 公开数据审计执行登记（2026-09-11）

- 已固定并审计 `MaT1g3R/Slay-the-Spire-data` 提交 `097aaf3564c2247835162d267cbc7c55d2c9039e` 的12个 Ironclad `.run` 小样本，以及 `colinking/runlogger` 提交 `02679f51c19c7a8d26da618ec0377faa390f347f` 的1个公开 Serialization Mod JSONL 示例。原始文件与 SHA-256 在 gitignored `reference/public-run-audit/`，精简索引在 `docs/m2-evidence-index.json`、`docs/m2-scene-candidates.json`、`docs/m2-exclusions.json`。
- 结果（R1–R5修复前的初始登记）：固定提交共发现157个 Ironclad `.run` 文件；抽取的12个独立 summary run 均只有逐层汇总，不能还原战斗入口。详细 JSONL 有231条记录、7个每楼层首个战斗入口，初始工具将2个标为 `reconstructable_but_backend_pending`、5个因动态遗物计数/升级表示歧义排除；仅1个独立详细 run，不能宣称总体代表性或正式训练分布。修复后状态以本节 R1–R5 登记及 `docs/m2-review-and-repair-report.md` 为准。
- Archive `slay-the-data.-7z` 只读取元数据，记录 `SlayTheData.7z` 约29 GiB及其 MD5，未默认下载整库。没有把它或 summary 数据转成战斗场景，也没有写入 PPO on-policy trajectory。
- 工具层真实失败与修复均保留：首次 Python HTTPS 请求出现瞬时 `SSLEOFError`，后续增加默认证书校验和重试；GitHub REST API 触发 403 配额后增加固定提交 codeload ZIP 目录回退，不关闭 TLS 校验。API/codeload 实际路径与哈希见审计报告。
- 边界：本登记只证明公开字段审计与初态候选抽取可行，不等于 M3 registry/C++/wrapper/model/runner 实现，不启动训练，不修改 `eval_seeds.json`，等待主审独立复核。

### M2 主审修复裁定：R1–R5（2026-09-11）

- R1 分类：公开场景必须分别记录 `structure_parse_status`、`visible_entry_status`、`internal_state_status`、`version_mod_status` 和 `backend_status`。字段能解析不等于初态可重建；当前两个原候选因缺少 BattleContext 内部继续运行证明，降为 `visible_entry_verified_internal_state_unproven`，不得写成只等待后端接入。
- R1 校验：场景审计使用有限整数、HP/最大HP、能量、楼层、实体/容量和牌区多重集的严格检查。允许由固定 Serialization Mod 语义解释“省略的空牌区”，但不允许因此放过牌组与牌区不一致；首层基础10张场景少5张抽牌必须失败。未知实体/未登记遭遇/未知动态语义进入排除或待审状态，不映射 PAD、不补零。
- R2 汇总日志：`.run` 同时尝试入口 A（开战前重建）和入口 B（开战后快照）；必须检查 Neow 起始奖励、拿牌/删牌/升级/变形、HP 时点、遗物计数、药水库存和遭遇实例。缺 hand/intent 只能阻止 B，不能单独证明 A 不可用；每份 summary 输出具体字段证据和缺口。
- R3 内部状态：post-init snapshot 必须列出可继续运行所需的 `InputState/ActionQueue/CardQueue`、CardInstance 动态值、牌堆后端顺序、monster move/history/misc/status、遗物 bit/data、药水容量/计数及 RNG 依赖。隐藏状态可以按明确的独立 environment seed 重新采样，但不得补零；当前公开候选只证明可见入口字段，内部构建仍未证明。
- R4 契约修订：`public-b0-core` 遗物容量改为实际2；limbo 是内部 reset/容量检查的一部分，合并进唯一 `pile_capacity`，不另加 card shape；status 使用明确固定注册字段而非无证明的 `status_capacity=8`；66动作只预留最多3槽、无二次选择且经批准的药水子集。Potion Belt、进阶容量、药水/卡牌生成和选择路径整体拒绝或另建迁移，不仅 mask 掉不支持实体。
- R4 终止：`public-battle-v1` 的50回合任务硬上限与外部采集截断分开记录；任务硬上限使用 `terminated=true,truncated=false,termination_rule_version=battle-hard-limit-v1`，最后一步先结算胜负；外部限制使用 `truncated=true` 并按 v6 旧最终观测自举。容量证明不能改变旧 reward。
- R5 预审整合：把 `Burning Blood` 实际默认持有但 formal adapter 未调用 `exitBattle`、Neow's Lament/遗物计数回写、Pen Nib 9/10 疑点、Sacred Bark/Blood Potion 分支、Red Slaver miscInfo/意图等列为独立待核查依赖。不得因预审疑点直接改正式环境或 `battle_reward_v1`；h_exit 是否包含战后治疗交由主审裁定，旧 minimal-v1 结果不回填。
- 本轮修复交付保持 M2 边界：只修改数据审计、测试、M2 契约和必要规格同步；不修改三个并行预审报告、不修改正式环境/模型、不训练、不改 `eval_seeds.json`，完成后再次等待主审。

### M2 第二轮定点收口：优先原生入口 A（2026-09-11）

- 裁定：撤销只以 post-init 为规范入口的限制。优先研究原生 pre-init（A）；B 必须独立提供完整恢复证据，无逐字段逆向证据禁止 B→A。
- A 的敌人 HP/意图、手牌/牌序和内部实例由版本化初始化及独立环境 seed 生成，不要求来源记录这些生成字段。无计数遗物不索要 counter；A11 的两个空药水槽可作为来源规则候选，不因此改动旧31位动作或批准新66位实现。
- 先以本地 sample-007.run 首层、HUNDRED_GOLD/NONE 为定点证据。区分按锁定标准规则可构造、来源版本/mod语义已核验、正式后端可运行三层。来源缺少完整 mod 清单且版本为2022-03-07，不得靠当前JAR直接证明历史来源完全一致。
- 本轮只改审计、测试、证据和契约；不实现正式reset、不训练、不变更旧奖励。M2的正式分布、容量和独立run划分未完成时不宣布收口。

### M2 批量公开场景库（2026-09-12）

- 用户明确要求多局、多构筑的数据流程；单场仅为方法验证。扩大至固定MaT1g3R仓库全部内容按character筛选，不能只按路径中IRONCLAD字样筛选，也不能把重复副本计成独立run。
- 建立独立corpus工具与索引，保留原12份审计作为历史。按原始SHA/play_id/source seed关联组去重与预划分；同源全部楼层与复跑保持同组。预划分仅验证数据管线，不代表正式训练/评估已准入。
- A批量重建先支持具有来源证据的Neow增删/变形/升级、普通战斗奖励、休息/锻造和明确遗物状态；未知事件/商店变更、永久卡牌动态值、遗物计数与药水时序必须显式阻断完整场景，不删内容凑可用数。
- 药水若只能恢复库存多重集，使用独立命名的模拟器槽位规范化策略，不宣称原槽位复盘。无法证明库存时不批准整场。
- 来源版本/mod兼容、字段重建、内容支持、全过程容量、正式后端可运行分别统计。多源公开构筑是目标，不能以单一run多seed或所有首层基础卡组代替。

### M2派生初态准入与M3–M5并行实施裁定（2026-09-12）

- 用户授权主agent调用subagent尽快推进至M5。采纳来源复审：登记 `public-derived-standard-v1`，从公开实录内容和有证据前缀派生入场配置，基础初始化采用锁定标准规则。历史完整mod/行为等价仍为unverified，不能改写为verified；该维度不再全局阻塞派生数据准入。源矛盾、非空自定义卡修饰、未知永久值/计数/前缀仍整场拒绝。
- M2首批契约是独立PublicBattleEnv入口A，源场景候选不删内容；M1的35类与必需辅助状态/诅咒登记为实施范围，TrueGrit升级和随机/二次选择生成不开放。数据闭包99场/75run是实施输入，不充当M3类数或M4遭遇数的验收。
- 观测采用具名语义记录及变长完整牌堆；10手牌、5目标、3预留药水槽共66动作。旧minimal-v1类、字段和checkpoint保持独立。动态牌值、玩家/敌人状态及药水/遗物必须完整可见，隐藏RNG/牌序/实例ID不输出给模型。
- 首批采用外部动作采集预算，不新增失败任务硬上限；预算耗尽在已执行动作的完整最终观测处truncated并可自举。不得在溢出后丢字段。静态master deck<=96，临时ID的int16边界必须在执行前由单步生成上界/配置预算证明保护。生成边界未通过之前不开放对应运行批次。
- 新环境胜利出口明确结算已支持的战后遗物治疗并以结算后的HP/maxHP算battle_reward_v1；不修改旧结果。完整exitBattle若触发run控制流则只实现已核实的战斗出口效果，保持独立battle终止。
- 允许M3/M4接口和行为测试、M5纯观测规则评分并行推进，独立记录完成状态；没有30–40类行为证据、约10普通+3精英实际战斗和规范观测规则策略证据前，不提前标记里程碑完成。配对Gate/PPO收益仍另行验收。

### M2–M5并行工程主审记录（2026-09-12）

- 首批M2派生契约已落地并通过实际reset/step：99内容闭包场景/75run，不宣称历史精确复盘或完整中等分布；标准配置在sts/env/public-battle-contract.json。
- M3语义接口、35 Ironclad＋5辅助牌、8遗物及15直接药水已实测；新张量/模型头/轨迹迁移和该环境PPO短诊断未完成，周计划M3仍保留进行中。
- M4工程通过：14普通＋3精英、46专测、136显式机制诊断；真实精英初态不足仍独立记录。
- M5规则基线工程首验通过：只读规范obs/mask、独立66动作runner；修复后99场规则/随机均无异常，99/72胜仅开发对照，不作Gate2。
- 已用正版定点证据修SeeingRed耗尽、RedSlaver一次束缚与55阈值，并修公开敌人Strength导出。全部后端改动进入完整patch；旧实验日志不回填。
- 全仓566测试通过；最终包装/策略增补复核43项通过；标准构建脚本完成，minimal/public导入均通过；eval_seeds.json未变。M6–M8和学习理解验收未完成，未commit/push或启动新PPO。

### 真实卡组驱动的训练起点：撤销历史整场还原前置要求（2026-09-12）

- 裁定来源：用户明确只需真实情况作为训练起点，完整卡组来自真实记录即可，怪物可自主调整；随后要求形成计划文档。
- 裁定：新增规划中的 `real-deck-configured-battle-v1`。直接读取真实完整卡组及升级，允许使用明确标为最终卡组的 `master_deck`；HP、遗物/计数、药水、怪物、进阶及生成上下文由版本化训练配置明确指定。历史缺失字段不再阻止使用该卡组创建新战斗。
- 替代范围：旧 M2 中完整前缀、历史药水/遗物状态、原遭遇及火精英身份的证明要求，只继续约束声称还原历史入场的旧来源策略，不再约束新配置战斗。火精英首批不纳入，新配置明确使用非火精英；无需证明原局是否火精英。
- 保留要求：卡组完整、多重集与升级正确、机制可执行、容量与动作可表达、独立 seed 初始化、隐藏信息不进观测、按来源 run 隔离划分。不静默删牌；未知永久牌值须拒绝或另行登记明确标准化策略，不能称为已恢复历史值。
- 数据命名：新场景称“真实卡组驱动的训练场景”，不宣称历史复盘、原始联合分布或总体玩家代表性。旧场景、失败审计与实验结果保留，不回填为新路线已通过。
- 工程状态：此次仅规划。旧正式入口仍按现有契约执行；实施时增加新策略的正式校验，不以 diagnostic 模式绕过准入。奖励、旧协议、eval_seeds.json、原排期和 Gate 不因本裁定改变。
- 执行计划见 `docs/real-deck-training-plan.md`：P1 卡组提取 → P2 配置与划分 → P3 reset/基线 → P4 模型/轨迹迁移 → P5 配对评估。历史重建和三方分布报告返工退出训练关键路径。

### 真实卡组 P1 提取执行记录（2026-09-12）

- 按用户“开始”实施本地卡组提取。`scripts/extract-real-decks.py`复用固定ZIP校验及来源关联分组，直接读取最终master_deck，输出`docs/real-deck-manifest.json`与`docs/real-deck-data-report.md`；不要求历史怪物、HP、遗物或药水字段。
- 实测203文件/157run组/46重复别名；157份原始最终卡组全部保留。146副完整实例可解释、内容各异，11副缺RitualDagger永久伤害值，独立拒绝；不补造该值、不删牌。当前中央契约整副内容兼容0组，卡牌缺口与提取状态分开统计。
- 内容支持读取现有中央契约及升级限制；报告逐项影响和联合缺口，Anger单项可解锁1副11张卡组，但不代表足够的训练分布。所有条目保持split未分配、runtime未运行、正式训练准入待配置及reset。
- 新增卡组边界与实际来源集成测试，连同复用来源分组回归共49项通过。没有运行新战斗、训练PPO、改动eval_seeds.json或commit/push。P2–P5仍未完成。

### P2开发批次冻结、进入M3及MLP扩展边界（2026-09-12）

- 用户要求快速完成准备进入M3，并明确MLP只在尽快冻结的环境与Set Transformer比较，之后扩展环境只使用Set Transformer。
- 为避免最终卡组缺牌阻塞，复用已经验收的中途完整卡组，不新增历史重建：旧99场提取为92个组内卡组条目/75run/27种内容。新批次自主配置A20、第一幕floor8、两套HP/遗物/药水条件、两个普通和三个非火精英，形成920个来源配置组合。
- 冻结comparison-battle-v1，详见`docs/mlp-set-comparison-boundary.md`。来源策略为real-deck-configured-battle-v1；中央卡牌能力不等于实际卡组覆盖。首批为早期构筑开发范围，不宣称成熟构筑或玩家总体分布。
- 新正式reset分支验证批次与候选完整性，不走diagnostic=True；同源划分不跨组，开发侧不得用于train，当前批次拒绝冒称正式保留evaluation。65run训练侧/10run开发侧；4种相同卡组内容跨侧公开报告。
- 后续MLP/Set必须在冻结环境、相同信息、动作、奖励、采样和评估协议下比较；扩展环境只迁移Set，MLP代码/配置/checkpoint/结果归档。不能将扩展Set分数与旧MLP直接作架构对照。
- M3正式推进中：reset与来源采样已接入，模型张量/头/轨迹迁移、资源预检、短训和正式评估仍待完成。环境范围冻结不等于这些步骤通过。
- 实测补记：27种卡组×2条件×5遭遇，随机/规则各270局，均自然终止，0异常/0截断；43/224胜只作开发集成证据。相关64测试及规格检查通过。详见`docs/real-deck-m3-report.md`，没有启动PPO或宣称正式Gate。

### M3架构、逐牌容量与四组GPU训练裁定（2026-09-12）

- 用户grilling确认：MLP和Set顺序推进、每种2次独立训练、相同交互步数；要求相对初始策略有可重复提升，Set不必超过MLP。正式训练/评估/重跑合计至多30分钟，最多2轮有明确假设的诊断修正。用户已明确允许直接执行，不再等待逐步批准。
- 用户坚持逐张表示，批准方案B容量外部截断，并要求生成余量收紧为8、改用GPU。固定卡组初始最大14；模型64个卡实体位置，达到57后外部截断，56+8=64保留完整最终状态。8的边界仅适用已冻结内容，后续战士全卡审计另行进行。
- 新运行协议comparison-battle-v2/battle-capacity-collection-v1记录此采集变化，奖励不变，旧540局报告不回填。最大512动作仍保留。压力探针20局中10次容量截断，最大59实体/单步净增长6；非训练分布统计。
- MLP为逐牌32维投影展平+全局64维后128/128网络；Set为卡牌SAB+PMA，32维4头，再拼相同全局投影后128/128网络。相同逐牌输入、mask与结构化66动作头；参数381150/140222。详细字段、预算及评估规则见docs/comparison-m3-training.md。
- 当前机器RTX3060Laptop/6GB，原PyTorch2.6为CPU版。独立.venv-gpu安装同版本CUDA12.6构建，保留原CPU环境。资源预检通过后才冻结正式每组步数；eval_seeds.json不变，当前结果仍属开发评估，不宣称Gate或总体泛化。

### M3固定环境工程与四组PPO训练收口（2026-09-12）

- comparison-battle-v2的逐牌编码、MLP/Set Actor-Critic、66动作masked PPO、容量截断自举、元数据与checkpoint恢复已完成。本轮M3工程收口仅指已冻结27种卡组/5遭遇/两条件，不等于全战士卡牌实现或正式Gate。
- RTX3060Laptop、PyTorch2.6.0+cu126实跑MLP/Set各2组，每组138轮/70656transition，共282624transition。正式训练与评估1097.41秒，低于1800秒上限；未追加诊断重跑或挑选最好组。
- 四组最终减初始回报差95%来源run组级区间依次为MLP0[0.2724,0.3540]、MLP1[0.7223,0.7999]、Set0[0.4305,0.5409]、Set1[0.3186,0.4072]，均通过预写学习改善标准。Set−MLP两组区间[-0.0102,0.0824]和[-0.0177,0.0194]均含0，不宣称Set更优。
- 每组前后各120个开发场景均自然结束；训练四组合计15131个episode，其中Set1有1次容量截断，完整回报保持未知、最终观测自举，不记失败0。eval_seeds.json哈希仍为38a093486535aa529d54ebfcfd535e67fe043daff8286b5740b5766a6131af9c。
- 验证：CPU完整633项回归通过；CUDA运行时下15项相关测试通过；两种模型GPU预检恢复后下一轮更新逐项完全一致；最终4个checkpoint全部实际恢复成功，步数及合法mask独立核对通过。曲线已生成并视觉检查。
- 证据：docs/comparison-ppo-report.md、docs/comparison-m3-training.md、docs/comparison-gpu-runtime.md，runs/comparison-ppo-20260912-gpu-v1保存源码、配置、日志、checkpoint、评估和TensorBoard。仍不宣称未见构筑泛化、总体训练随机性已充分验证或用户学习理解已经验收。后续MLP冻结，扩展只推进Set；未commit/push。


### 统一实体Set恢复规格与首轮四层训练（2026-09-12，用户确认）

- 原比较实现仅卡牌SAB再融合全局向量，偏离spec §7.1/7.2统一实体及目标评分要求；实施补记不构成用户批准偏离。旧结果只证明实际旧模型的开发学习改善。
- 用户逐项确认五类实体、动态输入、候选匹配动作头、Pre-LN及GELU，最终改为4层SAB并授权实现后GPU两初始化训练。暂不用ISAB，瓶颈实测后另议。训练评估总预算四小时取代本轮原半小时建议。详见docs/unified-set-training.md。

### 全卡扩展独立开发与逐里程碑提交裁定（2026-09-12）

- 用户明确授权在独立 worktree 按全卡计划开始扩展，每个小里程碑 commit 并落盘。允许与原对照训练并行开展代码开发，替代计划 F0 对所有开发的前置阻塞；F0 仍约束最终基线同步、扩展训练与集成验收。原训练目录只读，不修改其进程、文件、环境、结果或暂存区。
- 目标为限定第一幕 A20、五遭遇、现有8遗物/15药水的75类150版本，继续 Set；不是任意真实卡组、全部辅助卡或完整 run。奖励、评估 seed、原排期与 Gate 不变。
- 独立 ironclad-expansion-contract-v1 与150版本覆盖台账从开发状态开始。旧69版本是继承准入数，扩展接口的验收独立记录；没有行为及接口证据的新增版本不得准入。当前正式扩展训练关闭。普通动作保留66位，选择阶段在E3独立版本登记；全卡容量证明未完成不得沿用旧界开放生成闭包。
- 共享结算修复保存为独立增量后端补丁，冻结原补丁与对照契约。每次提交附复现命令、验证结果、未关闭项；已提交不等于整个E阶段完成。进展见 docs/ironclad-expansion-progress.md。


### E1真实动态与状态探针的独立准入（2026-09-12）

- 用户要求继续全卡扩展。为验收E1公共字段，先实现Rampage、Blood for Blood、Barricade、Corruption、Combust五类探针，验收后按实际版本更新台账；不提前宣布E4/E5完成。
- 独立ironclad-registry-v1保留旧40个项目ID，新ID按全部40个缺失牌名排序固定，未实现牌不准入。新增ironclad-observation-v2区分印刷费用、战斗费用与当下有效手牌费用及其来源；非手牌即时费用仍标未知。Set编码同步新版本，旧权重不得冒充精确恢复。
- 五类本身不生成新牌，不新增敌方生成路径，不引入选牌或复制；结合当前白名单，继承每外部动作最多15新ID的后端安全界。512动作下Rampage最多4096成长；最多96张初始牌限制Combust能力打出次数<=96（Power消失、尚无Exhume/复制），其int8失血计数暂不溢出。新增生成、重放、回收卡前必须重新证明。模型64实体容量仅作开发编码拒绝边界，未批准完整扩展截断与训练。


### E1d完整决策点容量采集（2026-09-12）

- 独立ironclad-capacity-v1绑定当前79版本注册表及运行契约hash；内容变化即拒绝继续沿用容量证明。原comparison的64/57/8不改。
- 扩展开发采集暂用64实体、50触发阈值、15生成余量、最多49初始牌与512动作；49+15=64保证截断后的完整观测可编码。最坏累计分配49+512×15=7729，小于int16界。此参数只覆盖当前有限内容，不能宣称全75类容量已证明。
- 当前实体和累计分配量分别计数；后者只进info，不进模型token。真实终止不标截断；动作预算和实体预算同时触发时同时记录原因；异常校验失败使当前采集失效，必须reset。
- 仍限development显式机制场景，正式扩展训练、真实卡组批次迁移和多阶段PPO尚未开放。


### U0：统一实体Set主架构与跨任务分工（2026-09-12）

- 用户明确要求统一实体：每牌/敌人/药水/遗物一token，玩家与全局一token，各类型独立投影后共同参与自注意力。替代本worktree的卡牌Set＋其他信息展平融合主线；旧四组实验与checkpoint冻结保留，不能改名或直接恢复。
- 实现前冻结首版配置：2层、宽64、4头、FF128、pre-LN双残差、GELU、dropout0、类型embedding、无位置编码；公开目标伤害转卡牌→敌人关系bias，不能作为固定敌人槽列进入卡牌token。动作按源/目标token＋池化上下文评分。完整配置与验收见docs/unified-entity-architecture.md。
- 输入/候选实际长度，批内动态padding；移除64张模型结构限制。初版总token512及注意力元素预算为资源保护，不是固定padding或游戏规则；当前有限内容采集边界改为96初始牌/480卡实体/466阈值/15余量/512真实决策，新增内容必须重做证明。
- 复用已完成全卡字段和选牌路由；True Grit+真实暂停工作继续，补上暂离四区的resolving实体。另一个独立任务负责新增敌人/药水机制，本任务提供共享编码/模型/候选和接口文档，不重复实现其新内容。
- 允许小规模前向、反向和采集，不授权新增长时间正式PPO；无活动环境更新边界的模型/优化器/RNG恢复与完整PPO轨迹恢复分开验收。原排期、Gate、reward和eval_seeds不变。


## 统一实体输入v3裁定（2026-09-12）

用户逐项确认并授权实施：费用展示与恢复信息精简、只记已知顶牌、删除目标伤害预览、保留后端格挡预览、取消正常对局牌数截断但保留资源保护、随机与自动动作交环境。完整契约见docs/entity-input-v3-contract.md。奖励保持battle_reward_v1；Feed长期收益评价登记待修改，未来修改必须独立版本。此裁定替代前述伤害关系bias及466张截断设计。



## 全战士卡牌工程准入完成（2026-09-13）

用户限定先完成75类战士牌，不扩大到任意真实卡组/全遗物药水。当前150版本具名准入完成，证据见docs/ironclad-full-cards-report.md。全卡主线用统一实体collector；旧15张生成假设替换为写入前编号/队列、计算和数值资源保护。未知组合资源异常不当作战败。reward保持不变，正式训练和敌人药水合流另验收。


## 全卡扩展修复裁定与实施计划（2026-09-13）

- 卡牌 token 与结算流程解耦：普通 CARD token 继续只表示卡牌身份、区域、基础属性及当前实例属性；自动打出、重复打出、暂停后继续结算等流程信息不得塞入普通卡牌 token。合法性仍由环境 mask 提供；只有在成对场景证明动作评分缺少公开差异时，才通过候选动作接口或独立的公开结算上下文提供最小必要输入，并实际接入动作评分器。
- 不导出隐藏队列、随机结果、内部实例 ID、不可见牌序或依赖未来随机结果的选择数量。新增流程输入必须有公开事件来源、必要性说明和真实可达验证；不以日志、Python 路由元数据或info字段冒充模型可见。
- Searing Blow +100 是本轮已声明的极端资源边界，不作为本轮合并阻塞。保留+0至+100的可构造范围、多次升级公式和资源保护；不静默溢出、不伪造战败，不把+100擅自改写成正版规则的不可升级终点。+100之后继续升级若触及当前资源边界，作为已知工程限制记录；正常升级、普通Armaments及多次升级仍须正确。
- B01、B02、B04按审计反例修复并补行为/编码/合法性证据；优先复用现有费用字段和既有区域/队列语义。B05先完成现有输入的成对可区分性核验，只有证实动作评分缺少公开差异才新增最小候选上下文。B01–B05不改变battle_reward_v1、不启动正式长时间PPO、不自动合并或推送。
- 修复小里程碑依次为：裁定登记与问题复现；费用与分类（B01/B04）；Havoc生命周期（B02）；暂停动作接口核验及必要修复（B05）；最终回归、恢复契约和合并报告。每步提交对应代码、测试和记录；旧MLP/Set实验、旧checkpoint及历史里程碑保持历史身份。
- 当前状态：裁定已登记，B01/B02/B04/B05修复和最终回归尚未验收。完成结论以`docs/card-token-fix-report.md`为准，可合并与仍有阻塞不得预先写死。


## B05公开暂停上下文收口（2026-09-13）

- 成对真实路径核验确认：手动Headbutt、Double Tap→Headbutt和Havoc→Headbutt的当前CARD/PLAYER_GLOBAL/选择候选能表达当前候选集合，但不能直接表达来源模式、来源强制耗尽和待重复选择数量。日志、路由凭据和mask不能替代这些动作评分输入。
- 采用最小独立`resolution_context`，不改变CARD 115维。字段固定为`source_mode`、`source_will_exhaust`、`pending_replay_count`；编码为候选上下文并实际拼入`UnifiedEntityActorCritic.action_head`。NORMAL候选上下文全零。
- `pending_replay_count`只是公开重复队列语义的数量汇总，不发布队列条目、卡牌内部ID、随机状态或隐藏牌序；当前不增加X能量快照、通用来源枚举或完整历史记忆。
- 因动作评分输入变化，接口/模型升级为`unified-entity-interface-v4`/`unified-entity-set-v3`，扩展观测/动作契约升级为v2/v5；旧checkpoint必须通过指纹拒绝精确恢复。该决策不等于正式训练准入。


## 最终复核收尾裁定（2026-09-13）

- 用户授权完成复核收尾：同一公开结算上下文同时进入动作头与价值头，普通CARD保持115维；模型升级为unified-entity-set-v4，接口仍v4。价值头使用有效候选上下文的均值，排除padding，空候选取零，不依赖候选数量和顺序。当前真实选择的上下文在所有候选间相同。
- 撤销Searing Blow +100的canUpgrade额外过滤，保留直接升级时的明确资源异常；此极端资源限制不作为本轮合并阻塞，不改变游戏升级终点。
- B05真实路径测试证明公开来源及接入，但尚未证明移除上下文后完整输入碰撞；保留最小公开流程提示的工程选择，不宣称三字段均有不可替代性证明。人工成对编码探针只验证模型接入。

### 敌人与药水独立扩展登记（2026-09-12）

- 用户授权在独立worktree与全卡开发并行，优先第一幕普通/精英/Boss和Ironclad药水；旧冻结对照结果不改写，扩展仅面向Set。
- 新增enemy-potion-battle-v1开发诊断入口、独立能力表与增量后端补丁。旧public/minimal契约、奖励与eval_seeds不变。正式训练关闭，完整RunEnv未实现。
- 选牌、生成卡池、临时费用复用全卡分支未来接口；本分支不定义第二套选择协议。未知状态/容量异常抛错，外部预算截断保留完整观测。
- 第一批将检验三Boss及直接药水；准入结果以docs/enemy-potion-expansion-progress.md实际测试登记为准，枚举存在不等于支持。


### 统一实体Set接口裁定（2026-09-12，用户追加）

- 每张卡、敌人、药水、遗物各一个token；玩家与全局合成一个token；类型编码后投影同宽，共同进入可堆叠自注意力。有效实体按实际数量组织，动态批次补齐和mask由全卡任务统一实现。
- 本分支只交付敌人/药水公开实体记录、动作引用映射、样例和生命周期夹具，不建立另一套Transformer、PPO或候选选择协议。固定5目标/3药水仅是当前后端动作槽位容量，不是模型token行数限制。
- 身份、HP、格挡、公开意图/状态/历史为语义；槽位、引用及观测修订号只用于动作路由。隐藏RNG/未来行动/内部实例号不进特征。全卡选择/候选接口待合并，依赖项继续拒绝。
- 动态token不解除后端容器与int16累计实例分配边界：第一幕独立15张单动作上界、96初态/512外部动作预算仍需逐配置验证，不继承旧64张模型容量或五遭遇8张余量。


### 桌面工作树迁移与EP2b登记（2026-09-12）

- 用户要求迁移至桌面继续开发；新工作树为C:/Users/19091/Desktop/sts2-enemy-potion，分支codex/enemy-potion-expansion。原目录被应用占用导致git worktree move权限错误，采用完整复制、231关键文件SHA校验和git worktree repair完成登记迁移。原目录保留非活动备份，其.git入口改名，后续不在原目录执行开发。
- 新目录保留未提交改动；构建缓存的旧绝对路径不得继续使用，移至本工作树build-before-desktop-move并按新路径重建。构建脚本增加迁移缓存检测。
- EP2b新增后端契约SHA256导出/加载拒绝；新字段或注册表变化不能误用旧构建。未对原public或冻结模型增加此要求。
- 只读核查全卡共享接口unified-entity-interface-v1：统一模型和EXHAUST_ONE已实现，不能继续统称“共享接口完全未就绪”；目前新增5药水与activation/potency_unit、死亡实体策略和全卡观测入口合并仍待对齐，多选药水仍未准入。审计结果记录共享源码/契约哈希，不修改全卡worktree。


### 第二、三幕敌人分批准入（2026-09-12）

- 用户明确暂停药水审计，继续第二/三幕敌人；能独立验收者加入，特殊敌人提交用户审核。本轮不扩药水白名单，不引入完整RunEnv。
- 首批13候选（第二幕10、第三幕3）实现入口幕/楼层校验、公开状态与意图；正式准入以实测结果收口，不以候选名单冒称通过。升级独立契约/观测v2，normal66动作与旧冻结public/minimal不变。
- 巨人头颅定点发现倒计时/高进阶阈值与后端回合公式需复核，移入特殊审核；扎刺之书、大嘴的随回合增长多段攻击不能继承15生成/50队列边界。召唤、复活、反应换意图、多阶段Boss等另列具体审核选项，不擅自开放。

第二/三幕首批收口：新增13遭遇、92专项测试通过，相关回归共307通过；22特殊遭遇逐一验证reset拒绝并提交用户审核。药水审计暂停。先前13候选现转为当前独立开发准入，不等于统一模型或正式训练准入。实际清单与证据见docs/act23-enemy-review.md。


### 三位置后端意图编号输入（2026-09-12，用户确认）

- 所有敌人统一输入当前/前次设置/再前次设置的后端招式编号与逐位置有效性mask，类别embedding由全卡共享编码器负责。不是已执行历史，不输入编号大小关系。
- 后端原数组两格扩为三格，setMove向后移动；原有直接覆盖当前意图（例如史莱姆分裂）仍不移动尾部，避免改变前两格与选招行为。第三格不参与模拟决策。
- 用户选择暂不逐怪审计内部编号可见性，后续遇到特殊敌人单独映射；不能宣称全部编号已通过公开性核验。隐藏RNG、miscInfo及未公开未来序列仍不导出。
- 当前伤害/次数、公开状态继续保留；不添加飞行减伤倍率或巨口成长推导值。已存在的行动/睡眠/蓄力公开计数暂保留，通用phase字段尚未实现。
- 独立契约/观测升v3，实体交接升v2；旧冻结文件与正式训练不改。

EP3落地补充：三大颚虫初始化伪招式DARKLING_REGROW在独立导出归零并mask，保持选招不变；NoOpRollMove不移动历史，直接覆盖也不移动尾部。317相关测试、4容量检查通过；详见扩展进度。


### 后续敌人授权与字段审批（2026-09-13）

- 用户授权按推荐顺序分批推进剩余22遭遇；每批验证后准入，不再逐怪请求研究许可。新增或修改观测/token字段必须先向用户提出具体方案，批准后实施。
- 用户确认倒地待复活等仍属于战斗的敌人保留token，目标合法性单独限制。实现所需新增/修改字段仍受上一条约束。
- 实际容器/实例编号扩容方案，以及当前战斗加战后变化的范围，用户要求通俗解释，尚未确认；不得把询问当作授权。
- 已读取全卡牌输入任务01a09402-4c2e-7ba0-824c-1ead46e89259最终结果：其报告全75类150版本、CARD115、模型unified-entity-set-v4、950测试及350局集成通过，提交1dc281d/2325d55，尚未合并。此处为任务交接证据，未在本分支复跑。后续复用其费用、选择、结算上下文接口，不建立第二套协议；全卡通过不等于扩展敌人已集成。


### 敌人续批边界收口（2026-09-13，用户确认）

- 暂不投入极端容量推演或专门扩容，不将“玩家必先死亡”登记成已证明结论。保留已有错误保护；常规机制验证实际遇错再报告，不伪造失败0。
- 盗贼逃跑与彻底死亡在实体移除、目标不可选及正常战斗结束处理上等价；不新增逃跑原因输出。后端实际金币变化仍正确结算，怪物token不增加战后字段。当前奖励保持battle_reward_v1，不增加金币权重、诅咒惩罚或掉落奖励。
- 永久加牌由后端落实，环境负责战后变化；不推进下一场战斗，不让奖励函数代替实际状态更新。新增/修改输出字段仍先请求用户确认。
- 铜球扣押牌优先复用共享卡牌region字段，不增加怪物token专属扣牌字段。只读核实共享当前regions为hand/draw_pile/discard_pile/exhaust_pile/resolving/offer；NORMAL目前拒绝非空resolving，不能直接借用而不修改语义。真正接入时提交复用或新增region值的具体方案，批准前不修改共享接口。
- 此次只登记讨论结果，未新增敌人准入或执行训练。


### 用户接手裁定与审批更新（2026-09-13）

- 用户要求本任务接手后续全部工作，不再使用subagent；保留各worker成果，由当前任务审核集成。
- 同实例复活保留后端自然历史变化，不为观测清空参与选招的moveHistory；新实例仍按原生初始化。
- 字段待批不妨碍独立验证召唤/死亡/复活/终局后端机制。
- 批准新增卡牌region=stasis；持牌铜球与卡牌的公开关系表达尚待具体裁决。
- 批准C的终局info.battle_exit最小出口：实际deck_changes与gold_delta，不进入token/reward，不新增逃跑原因，不推进下一战。
- phase和新增公开状态词表仍待本轮讨论，不能当作已批准。


### 铜球关系暂缓与公开状态接入（2026-09-13）

- 用户批准phase=NONE/PHASE_1/PHASE_2及Flight/Thorns/Slow/Intangible/Fading/Shifting/Reactive/Time Warp，阶段尚待逐怪实现。本批先实现八状态的独立导出，保持statuses容器及观测v3结构，中央契约增加状态修订并通过SHA拒绝旧后端。共享模型词表仍待接入，未新增遭遇准入。
- 用户要求暂停铜球关系实现，先处理其它已批准机制；stasis区域仍获批准，但本轮不实现区域或关系模型改动。待用户带回GPT-6 Pro审阅结果后再定案。
- 已核实共享EntitySample.edges当前最后维度为0，EntityBlock.forward不读取关系。关系注意力是待讨论架构改动，不是已存在的可直接复用能力。详细咨询材料见docs/stasis-relation-design-review.md。


### 非铜球续批候选（2026-09-13）

先接MAW/TRANSIENT，两者复用现有回合、意图、已批准Fading/Shifting；不新增成长字段，不改奖励。真实后端与公开接口测试通过后登记开发准入；原22条台账保留历史身份并逐条更新。全卡生产集成另行验证，不以35牌独立入口测试冒称全75牌兼容。


### 全卡与非铜球敌人实际集成（2026-09-13）

- 从全卡2325d55引入已验收的共享编码/候选/卡牌规范化和后端机制，在本分支增加integrated-card-env独立绑定。冻结public绑定与public契约不替换，公共规范化复制为full_card_public仅供集成入口；共享来源及哈希有清单。
- 集成入口仍限定A20，先接旧5遭遇与MAW/TRANSIENT；独立敌人入口支持A0..20。普通CARD保持115维，敌人两次类别历史替换为三位置原始类别编号+有效性，one-hot接线性投影等价于分位置类别embedding，ENEMY输入768维、投影后仍64宽。模型版本unified-enemy-entity-set-v1，零宽edges保留，不实现铜球关系。
- 该版本不是旧checkpoint精确恢复目标；正式训练未启动。共享全卡的原测试结果不能冒充本分支集成回归，必须本地实测。

用户裁定（2026-09-13）：暂缓时间吞噬者接入；DEFEND_DEBUFF不加入当前词表。非铜球首批验收与入口范围见enemy-full-card-integration-report.md。


### 铜球 B 方案批准与第二幕完整接入目标（2026-09-13）

- 用户带回外部审阅并明确选择 B：单一公开 holds_card 关系，经当前实体引用派生紧凑索引，从各类初始投影取得卡向量，一次 CARD→持牌 ENEMY 融合后进入现有自注意力；卡保留独立 token。不得另建第二套模型、候选协议或双份手工关系事实源。
- 此项撤销此前铜球关系暂停。采用关系存在标志，无关系增量严格为零；引用与索引仅用于路由，不作为语义特征。外部报告的测试建议不是本地通过证据；stasis 逐字段公开性和同名实例可辨识性仍须核验，不因批准 B 就导出完整后端 CardInstance。
- 用户批准新增 DEFEND_DEBUFF 意图类别，撤销此前时间吞噬者暂停；词表、编码、版本和后端需同步后验证。时间吞噬者属于第三幕，单列验收，不计入第二幕完成数。
- 新目标是完成第二幕所有敌人及全卡交互。普通/精英/Boss、事件战斗及进阶覆盖的准确交付范围见本轮待决清单；既有授权不重复申请。尚未完成实现，不以登记等同准入。
- 继续由当前任务单独实施，保留桌面独立 worktree；不改冻结对照、不启动正式 PPO、不推进下一战、不改 battle_reward_v1。


### 第二幕范围与未知属性确认（2026-09-13）

- 用户确认包含蒙面强盗与竞技场两场事件的战斗部分；全卡生产准入以 A20 为主，不实施事件选择或两战衔接。
- 用户接受 stasis 不明实例属性以未知为主；不能用后台真值替代公开知识。若需要修改具体 CARD 字段的未知表达，仍先提交准确改动。
- 核查现有台账：三个事件遭遇此前均为 out-of-batch，不能声称已完成事件入口审核。竞技场可复用既有敌人机制，但组合/初态/终局/全卡入口仍需验收；蒙面强盗的三类敌人仅发现后端分支及意图编号登记，尚无本任务专项准入证据。
- 用户指出 Painful Stabs 与随从状态通常常驻、铜球扣牌状态未必存在。后端核查确认扎人的书初始化获得 PAINFUL_STABS；铜球仅实际扣牌成功后设置 STASIS，抽牌堆与弃牌堆均空时直接返回。B 已有关系存在标志，不必重复新增 Stasis token 特征。
- MINION 是遭遇角色，不能一概说由怪物种类唯一确定：地精首领遭遇为普通地精额外设置该标志。是否省略冗余公开状态须按场景与可推导性决定，本轮不将用户说明自动视为三项新状态导出批准。


### 第二幕开始实施（2026-09-13）

用户确认 Minion 不额外导出；不追加 Painful Stabs/Stasis 冗余特征。此前四项讨论已收口，开始第二幕含事件战斗的 A20 全卡集成。普通核查不再请求审批；实际新字段仍提交准确方案。


### 第二幕全卡完成与B实施口径（2026-09-13）

- 用户批准的剩余8遭遇已接入，全卡A20第二幕22/22；不扩大旧独立入口或正式训练。
- B选用单一holds_card事实、紧凑held_card_index路由、零初始化无偏置Linear(65,64)一次融合；保留独立扣牌CARD。只显示公开牌名，stasis区域整体遮罩其它未核验属性。不是后台完整实例，也不新增一套选择协议。
- 公开phase仅为NONE/PHASE_1/PHASE_2；第一勇士在愤怒实际执行后转换。不额外导出Minion/Painful Stabs/Stasis。
- 正常候选加snapshot路由封装，旧快照动作拒绝；底层66位动作与现有SELECT_CARD协议继续复用。新构造敌人引用递增，同实例自然历史保留。
- 真终局执行单场exitBattle，回调不进入地图。战后deck_changes为实际牌组计数差；金币差包含被击杀盗贼已偷金币的收回，逃跑金币不收回。奖励仍为battle_reward_v1，外部截断无战后出口。
- 新模型与接口为v2，具体指纹、测试与限制见act2-completion-report.md；不宣称旧checkpoint精确续训或训练收益。

## I0：环境集成与A路径执行裁定（2026-09-13）

用户批准先保存相关成果为本地提交，在独立集成工作区依次集成全卡和敌人药水成果，完整回归后本地合入main；不push。原工作区、辅助worker及旧实验保留。新任务以a-path任务书和本轮计划为需求：四层SAB、64维、4头、FF128、PMA单seed；来源与条件敌人目标两阶段采样、一次env.step、联合PPO。替代旧全局候选评分和扩展两层均值池化；奖励、信息边界、eval_seeds不变。工程验收后32768步smoke，最多三个初始化各262144步，训练评估总上限四小时，最多一次额外32768步诊断修正。

统一扩展以后端锁定HEAD＋冻结基础补丁＋敌人集成增量重建；不得再重复叠加全卡增量。冻结public Python适配器从主线保留，扩展使用full_card_public。当前只有集成进行中，尚未声明集成回归或A路径通过。

## I1：基准推送与A路径模型里程碑（2026-09-13）

用户追加授权先commit/push当前基准，然后开始A路径；每个里程碑报告完成、解决的问题和未完成项，需要裁决时等待。main基准615cffa已push并通过远端引用复核，覆盖此前I0仅本地不push的限制；后续A路径里程碑当前只作本地提交。

I0算法不变，模型与Agent首批项目验收完成，证据和T01–T32边界见a-path-model-milestone.md。新增结算上下文只进入PLAYER_GLOBAL，保持普通CARD语义边界；旧模型保持历史身份。

执行关联划分后旧训练候选收缩到13条记录、12run、10种内容。是否纳入47份静态合格最终卡组，作为不同分布的新配置战斗，提交a-path-data-decision.md等待用户裁定；不得先将候选改标正式准入或启动训练。奖励和原预算不变。

## I2：原则接受B，先审阅卡组分组与采样份额（2026-09-13）

用户原则上接受B，但要求防止最终卡组主导：先按实际内容梳理简单与组合卡组，解释分组采样及简单组保留比例，评估逐组报告，不只凭混合平均回报判成功；方案解释前不冻结训练池。

本轮仅形成a-path-group-sampling-proposal.md及逐内容台账。建议S/T/C真实transition配额50/25/25、8环境按4/2/2、minibatch64按32/16/16；当前内容分类得到训练10/8/21种，历史开发17/5/13种。此处登记建议不等于用户已批准比例。正式采样、数据池冻结和训练仍未执行。

## I3：撤销正式内容配额，来源组均匀与全局PPO shuffle分离（2026-09-14）

用户认可三组内容分类作为评估诊断维度，但不批准50/25/25正式训练分布、4/2/2环境绑定或32/16/16 minibatch配额。四小时训练暂停，修订后审核。I2的上述配额建议由本条替代，未曾运行。

核查comparison实际完成版本为comparison-battle-v2；冻结范围与run均匀采样保持不变。A路径属于对照后的Set-only扩展，拟新设source-group-aware baseline，不回写旧对照。新提案先均匀抽33个已知关联来源组，再均匀抽组内去重deck/状态，再抽条件和遭遇；每次reset独立抽环境seed。PPO每epoch对完整rollout全局随机排列，无内容组配额或重加权。

精确概率审计为39deck、390个不含环境seed的初态配置候选，simple/transition/compositional自然开局比例13/66、7/33、13/22；不是实际transition比例。完整机会表和评估口径见a-path-group-sampling-proposal.md、a-path-source-sampling-audit.json。

simple-heavy最多32768 transition仅保留为独立smoke/curriculum diagnostic提案，不启动、不作为正式结果、不用其权重启动正式baseline。正式数据入口、PPO闭环与恢复仍待工程验收；四小时预算继续暂停。

## I4：用户授权直接启动四小时正式训练（2026-09-14）

用户最新明确要求尽快开始四小时训练、可以跳过smoke，并在训练实际启动后结束对话。此授权替代I3暂停与等待审核要求。继续采用来源关联组均匀initial-state sampler、独立环境seed及全局PPO shuffle；不恢复50/25/25或固定env/minibatch组配额。

已完成33个train来源组的当前已知来源隔离核验；39种训练卡组×两条件×五遭遇，在随机和未训练A路径两策略下780局全部自然终止。每来源组27组一副、6组两副，无大组稀释问题。冻结独立a-path-training-pool.json，不改旧comparison。正式入口严格校验注册内容与训练/开发划分，未用diagnostic执行PPO。

完成真实PPO更新、联合概率目标、全局shuffle、活动环境重放及CPU/CUDA下一更新一致性短预检后直接启动正式运行，不单独执行32k smoke。每初始化最多262144真实transition、最多三初始化，与四小时训练评估时限取早者；学习率从开始按262144定义。预留保存/评估时间，后台程序到限或异常保存并停止，不自动延长预算。

## E12：第一、二幕全卡统一入口与 main 整合（2026-09-14）

用户本轮要求阅读交接说明，将全卡、第一和第二幕全怪整合到 main，并核查怪物整合缺口。本次授权涵盖环境修复、独立工作区验证和本地合入 main。当前查明 main 已含全卡及第二幕源码，但全卡入口第一幕仅 5/20，且 main 本地 ignored 后端仍为旧构建。

在独立 codex/act12-full-card-main 工作区补齐第一幕 20 个普通/精英/Boss 遭遇及锁定后端的两个事件战斗变体（MUSHROOMS_EVENT、LAGAVULIN_EVENT），复核第二幕 19 个普通/精英/Boss 加三个事件战斗。事件只运行单场战斗；卡牌维持 75 类、150 版本及既有辅助牌，已有第三幕 MAW/TRANSIENT 保留。修复第一幕 Boss 房间分类，遭遇与幕的对应由同一契约生成并校验。

本轮不改变观测/模型字段、动作与奖励语义，不合入或启动 A 路径训练，不改历史运行及冻结 seed。仅提高整合修订号并更新运行指纹；原五遭遇容量采集器保持五遭遇边界，经相应回归后重绑新指纹。若发现确需新模型字段或未裁定机制，暂停相关变更，提交具体证据；不能用白名单并集冒充行为验收或正式训练准入。main 的旧后端须完整保留后再切换；原 A 路径工作区保持原样。

完成条件：锁定上游与两层补丁可重放构建；全卡统一入口的初始化、真实决策、选择、分裂/召唤、终局与编码通过定向验证；全仓 CPU 回归和独立 C++ 夹具分别记录；提交并本地合入 main 后，从 main 实际加载新后端复核。事实及剩余限制登记在 docs/act12-main-integration-report.md。

E12 验收补记：环境代码 `65248a2` 已本地合入 main，main 已独立重建并实际加载。main CPU 1345 项通过、独立 C++ 夹具 1 项通过；隔离逐卡诊断 44 遭遇 × 150 版本共 6600 局，6397 自然终局、203 明确预算截断、0 异常。修复中包含 Burn+ 既有升级版本的注册漏项，模型字段与维度不变。旧 main 后端完整保留于 third_party/sts-lightspeed-before-act12-20260914，原交接说明及 A 路径代码/后端已核验未改；未启动新训练任务、未 push。具体指纹、分版本证据和依赖路径问题的解除记录见整合报告。

## I5：A 路径合入 main 并成为后续主线（2026-09-14）

用户明确 A 路径是当前及后续项目主要方向，授权将归档分支 2f3beb9 合入 main。此裁定替代 E12 本轮不合入 A 路径的任务边界；保留全卡、一二幕环境修复和历史对照，不以旧 66 动作训练器替代 A 路径。当前主模型为四层 SAB、64 维、4 头、FF128、单 seed PMA 与来源/条件目标两阶段联合 PPO。

本次将独立 A 路径池的运行契约及注册表指纹绑定到 main 已验收后端，并重算池哈希；原 39 个训练内容、来源组、两条件、五遭遇、抽样概率、容量与奖励保持不变。全卡诊断入口的 44 个一二幕遭遇不自动进入训练池。旧检查点保留原指纹，禁止放宽恢复校验；本次运行短工程闭环和回归，不重新启动四小时训练。后续模型、采集、训练与学习文档默认沿 A 路径推进；历史入口只作回归及对照。


## I6: Clear research starting point and bounded documentation cleanup (2026-09-16)

**Decision.** The owner selected a mentor-readable research snapshot that also serves as a clear working starting point. The current research question is whether the existing A-path Set Transformer + PPO policy improves over its own initialization within the fixed admitted combat distribution. Set-over-MLP superiority is not a completion requirement. Keep the frozen comparison as a short earlier-experiment summary, with inconclusive differences and its limitations intact. DT remains later work, not a prerequisite for this snapshot.

**Language and organization.** New/revised public entry documents are English; the owner does not need a parallel Chinese guide. Gradually migrate supported content without making full translation of existing technical documents, comments, tests or CLI text a first-stage gate. This supersedes the blanket Chinese-only AGENTS rule. Keep one active specification at spec-v6.md and retain historical evidence. Distinguish current references, pending valid plans, and obsolete material under archive/. Prioritize documentation and navigation; keep legacy code in place unless a verified dependency fix is needed.

**Scope.** The owner explicitly authorized work in the current Git checkout and a local commit after checks; no push. Preserve the two pre-existing untracked documents and all ignored artifacts. Directly fix stale facts, links and rule conflicts. Record substantive implementation or reproduction gaps for later; wait for owner verification on new decisions or critical issues. No new training, reward/model changes, pool expansion, or relic implementation in this pass.

**Next priority.** Relics and faithful Act 1/2 combat remain near-term development goals, with preparation retained as pending work. This supersedes treating relic development as permanently out of scope; it does not admit unverified scenes or authorize implementation now. The original learning objective remains central, while mentor readability is now an accepted presentation goal, updating the older exclusion in spec-v6 section 0.1. Existing schedules, Gate rules and compute budgets are not reset.

**Minimal dependency repair.** After inspection showed that legacy source-snapshot utilities hard-coded root-level v4/v5 files, the owner authorized archiving those originals and correcting the explicit paths. Current A-path algorithm/fingerprints stay unchanged; archived experiments and checkpoint validation are not rewritten or weakened.

**Reason and reconsideration.** A first project needs a bounded question and an unambiguous entry route. Revisit the scope only after the owner accepts a concrete next-stage plan or a verified dependency/evidence conflict requires a decision. The first cleanup ends when the entry route explains the question, current path, evidence and next work; it does not claim clean-clone reproduction or new experimental success.


## I7: Defer A+B initial-state preparation until mechanism and reward work is complete (2026-09-18)

**Owner decision.** Use two complementary initial-state sources: A, battle-entry states reconstructed from sufficiently complete real records; B, explicitly constructed scenes using verifiable real material plus declared configuration choices. The owner identified `SlayTheData.7z` as the large `.run` source to investigate. Existing metadata and small-corpus reports do not establish the large archive's sample contents, recoverability or usable scene count.

**Required ordering.** Complete and validate the previously agreed relic-state/mechanism, reward revision and potion work before starting the new large-dataset investigation or A+B initial-state preparation. The earlier discussion placed reward/potion work before the required relic work; this decision preserves that prerequisite block and does not authorize an internal rescheduling. After those prerequisites: inspect dataset samples, prepare and validate candidate initial states, obtain approval for any new admission/distribution decisions, freeze environment/reward/pool/evaluation versions and budget, then train and evaluate. A focused action-head ablation remains conditional on time and a separately frozen comparison protocol. The assistant's suggestion to investigate the dataset alongside or before the prerequisite work was not adopted.

**Reward/potion context.** The referenced owner discussion selected net HP change normalized by starting maximum HP, allowing healing, and a potion-use cost as the candidate reward direction. Coefficients, event/terminal handling and bounds still need specification and verification; the intended terminal outcome separation is not a proof of lexicographic win-rate optimization. Existing potion support must be inspected and completed as required, not assumed absent. No new reward version is implemented by this record; current runtime reward remains unchanged.

**Source and evidence boundary.** The prior decisions are recorded in task `01a0b489-0af0-7cf3-88ae-47fa7b450a11` (title: 审计并准备CUHK MSc AI申请), with `research-decisions.md` in that task's application workspace. This entry records the STS-relevant ordering and source strategy, not application deadlines or a new compute allowance. Use [the deferred work item](plans/initial-state-source-plan.md) for scope and exit criteria. Do not invent missing relic counters, potion inventories or permanent card values; constructed alternatives must be explicitly labeled and cannot be presented as historical reconstructions.

**Authorization and reconsideration.** This turn authorizes English documentation only. It does not start source acquisition, extraction, implementation, pool expansion, training or publication. Preserve the existing admitted pool and historical results. If the later source audit reveals mechanisms or state requirements outside the completed prerequisite scope, report that specific gap for owner verification rather than silently reopening scope, dropping scene components or admitting incomplete states. Only the owner can advance this work ahead of the prerequisites.


## I8: Implement battle_reward_v2 for A-path (2026-09-18)

The owner approved B=2, alpha=1, lambda=0.05 and true-terminal-only settlement: R = 2*I(victory) + (HP_end-HP_start)/max_HP_start - 0.05*potion_uses. Starting HP is captured from the registered pre-combat state; final HP includes the existing supported victory-exit healing. Genuine failures retain their actual HP term and potion cost. External truncations receive no fabricated terminal payment and retain final-observation bootstrap. This is a scalar tradeoff, not lexicographic win-rate optimization or a claim of globally proven outcome separation.

Implement the version at the A-path environment boundary using backend outcome/HP and successfully executed potion actions; preserve the backend v1 diagnostic reward and all legacy paths. The shared Python reward module owns the v2 formula. The current admitted potion set uses explicit drinking actions; automatic consumption or generated-potion extensions need event instrumentation before admission. Record initial/final inventories and each use, and retain reward inputs in episode/evaluation records. Checkpoints carry the v2 contract and implementation fingerprints; do not relabel or loosen compatibility for v1 checkpoints.

Implementation and focused runtime/recovery tests are authorized in an isolated worktree. No substantial training, pool expansion, new relic mechanics, dataset acquisition or push is authorized. I7 ordering remains in force.


## I9 — Relic state implementation with one-hot identity (2026-09-19)

The owner authorized relic import implementation after I8, explicitly retaining one-hot identity (no relic ID embedding). Use an isolated worktree based on reward v2. Preserve the shared four-SAB/64-width/PMA/source-conditional-target PPO backbone and the fixed admitted pool. Implement a versioned relic registry, strict public state, actual backend mechanisms and recovery tests, beginning with the existing eight relics and six counter representatives. Special relics affecting selection, binding, visibility, capacity or unresolved semantics require a concrete owner review before implementation of that part. Continue independent ordinary work. No substantial training, pool expansion, merge or push is authorized. Mechanism diagnostics are not training admission or proof of learning.


## I10 — Full Act 1/2 relic scope audit before batch two (2026-09-19)

The owner requests an audit of all relics encountered in Act 1/2 before importing the second batch. Audit the original-game catalog against the locked backend, classify standard Ironclad acquisition/carry-in scope, required public and internal state, backend paths, adapter gaps and special review items. Other-character, later-act and special-mode entries remain explicit exclusions or conditional entries, not silently omitted. Complete the audit artifact first; then import the ordinary second batch supported by the findings. Retain I9 one-hot identity, I8 reward v2, strict fingerprints, fixed pool/seeds and the owner review requirement for special relics. No new training or pool admission is authorized.


## I11 — Owner refinement of special relic scope and public state (2026-09-19)

The owner accepts deriving omitted state only from the current policy-facing observation, not unprovided history or backend access. Required simulator initial/recovery state must still be supplied. Runic Dome must hide all direct current-intent encodings; history contains only actually observed public events, never previously hidden planned moves.

The owner excludes Lizard Tail and Necronomicon, reverses the prior Nilry's Codex approval to exclusion, and retains the nine earlier exclusions. Unceasing Top, Runic Pyramid and Mutagenic Strength are approved without additional relic fields, relying on existing public state and backend resolution; verify sufficiency at policy decision boundaries and report any counterexample before adding fields. Mutagenic Strength still requires correct Artifact interaction/order; ownership alone does not prove that ordering.

Gambling Chip is conditional on an existing complete multi-selection/deselection/finish interface; current public SelectionRouter is single-choice, so import is deferred without silently designing a replacement. Snecko Eye is conditional on retaining unknown non-hand effective costs while revealing actual hand costs; verify backend draw/randomization behavior. Velvet Choker and complex counters should read authoritative backend state. Existing cards_played_this_turn is already exported and is used by the backend Choker legality check; verify event semantics before reuse.

Frozen Eye replaces known_top with draw_position_known/draw_position in the new observation version, including known-top effects outside Frozen Eye; frozen historical versions are not rewritten. Bottled relics reuse the general Stasis entity-relation concept with concrete original card-instance binding. The owner conditionally permits same-name fallback if exact binding is complex, but current backend bottleIdxs/card uniqueId infrastructure makes exact binding the preferred implementation; do not choose an ambiguous name fallback without a concrete blocker. Relic identity remains one-hot.

See relic-import-decisions-2026-09-19.md for the consolidated scope. This refinement does not admit scenes, authorize substantial training, merge or push.


## I12 — Execute owner-approved special relic imports (2026-09-19)

The owner authorizes sequential import of I11 relics with a progress report after each completed relic and continuation to the next. Import Runic Pyramid, Unceasing Top, Snecko Eye, Velvet Choker, Mutagenic Strength, Orange Pellets, Runic Dome, Frozen Eye and the three bottled relics. Gambling Chip remains deferred because the existing public selection interface does not satisfy the approved condition. Excluded relics remain excluded.

The resulting development registry is relic-state-v3: 33 fixed one-hot identities, three Orange Pellets progress bits, and three existing counter fields (39 raw dimensions). Card encoding replaces the known-top bit with draw-position-known and position/512 (one extra raw dimension). The fixed 64-wide A-path backbone and joint policy are unchanged. Exact bottle input is {name, card_index}, where the index refers to the supplied master-deck array and never becomes a semantic feature; a relic-to-card entity relation follows the original instance. Array order for relic initialization is acquisition order. Current public resolved powers express Mutagenic Strength/Artifact ordering without an extra model field.

These are diagnostic imports with scoped runtime tests. Preserve strict checkpoint fingerprints, reward v2, existing training admission and immutable evaluation seeds. No substantial training, main merge or push is authorized. See relic-third-batch-report.md for evidence and remaining limits.


## I13 — Remaining combat-scope relic imports (2026-09-19)

Owner approved all remaining 102 eligible Act 1/2 entries within the existing Battle Agent boundary: verify 56 combat entries; represent ownership for 46 run/acquisition entries using explicitly supplied post-acquisition initial state, validating any actual combat/exit hooks. Do not replay acquisition, add RunEnv, reopen the 12 exclusions or deferred Gambling Chip, expand training admission, or train. Report each completed relic and continue automatically. Version changed model-facing contracts and preserve strict fingerprints.

## I14 — Small-corpus initial-state pilot and versioned data contract (2026-09-19)

The owner authorizes the audited route: pinned MaT1g3R source inventory and field mapping, historical prefix reconstruction, canonical storage/deduplication, provenance-safe grouping, reset/Token/model/action routing diagnostics, bounded complete-combat validation, then a gap report before deciding whether to investigate SlayTheData.7z. Report concrete progress and next steps in the active task; return new semantic/admission decisions to the owner. Use an isolated worktree with new project-specific files under `projects/battle-initial-states/`.

The owner selected the complete `battle-initial-state-v2` proposal in `projects/battle-initial-states/schema-decision.md`: preserve required gold, acquisition-ordered relics and exact card binding through canonicalization; preserve archived v1 identity. Unsupported permanent card state is rejected intact. Reuse the existing development-only `RelicEnv` and A-path model/reward/routing without relaxing formal pool fingerprints. Scope does not authorize training admission, new sampling weights, model-facing changes, substantial training, large-archive processing or push. I7 prerequisites are checked against the integrated reward/potion/relic evidence; the original schedule and budget remain in force.

After the first diagnostic closure, the owner selected D2's recommended bounded historical source-compatibility audit. Preserve unknown historical installation/rules evidence as unknown; do not turn field-format matching into formal A certification. The audit also checks actual Act 1/2 source coverage and distinguishes full run-end decks from reconstructed entry decks.

### I14 follow-up — Frozen A-v1 and coverage-augmented A-v2 (2026-09-19)

The owner authorizes freezing the existing 1,377 diagnostic states as A-v1, then creating a separately versioned A-v2 with Act 2 low-HP examples, rare card combinations and legal configured entry-counter values. Preserve immutable baseline bytes, source backbones, parent hashes and connected-component partitions. Synthetic variants are configured examples, not additional historical observations. Burning elites are deferred. A future SAB generalization corpus is deferred and is not created now. This authorization does not expand backend support, inspect the large archive, change formal training admission or launch training. Corpus version and canonical state schema version are separate identifiers.

The owner subsequently freezes the proposed experiment's training sampler: 50% A-v1 natural training states and 50% A-v2 augmentation training states; separately within each stratum, equal weight across eligible connected components and then uniform distinct states within the selected component. Dev/holdout remain fixed evaluation sets. This approves the sampling rule only; architecture, remaining hyperparameters, budget and training admission are still awaiting review. The natural label retains A-v1's existing projection/configuration provenance.

The owner authorizes the A-path architecture change after searching for an existing implementation: two shared SAB blocks followed by two actor-specific and two critic-specific SAB blocks, with separate branch normalization/PMA and existing policy/value heads. Each path retains depth four, width 64, four heads and FF128. Version the model and reject old checkpoints; preserve all frozen corpus evidence as evidence for its original architecture. This supersedes the fully shared four-SAB topology for the new experiment only. PPO settings, budget and training launch remain awaiting approval.

The owner then authorizes execution through smoke-test completion only. The reviewed draft parameters are used for the bounded smoke, with group offset 100, CUDA FP32 and a 900-second limit. A-v2 train-purpose admission is explicitly smoke-only; the legacy formal pool remains unchanged. The adapter rejects non-train partitions and altered states, uses no diagnostic reset bypass, and the trainer caps each smoke branch at 3,072 logical transitions. Formal training must not follow automatically. Evidence is in `projects/battle-initial-states/experiments/smoke-v1/`.

After successful smoke completion, the owner authorizes formal training and first requests a single TensorBoard run with train/dev namespaces. Execute experiment protocol v1 with one fresh initialization, 262,144 transitions and a four-hour total wall-clock cap including evaluation/closure; retain the final-hour reserve. A-v2 train admission is experiment-specific, leaving the legacy pool and frozen corpus bytes unchanged. Use 50/50 natural/augmentation with independent component-uniform sampling inside each stratum, the approved 2+2+2 model and reviewed PPO settings. Dev/holdout use fixed cases; no holdout-driven tuning. Record exact code/config/data/backend fingerprints and do not mutate them during the run.

### I15 follow-up — Frozen dev console and worktree retirement (2026-09-20)

The owner approved standalone frozen-weight dev replay, a trajectory player, and a dev-only interactive battle console with manual intervention. The owner then explicitly authorized consolidation into main, a separate model-switchable console directory, and removal of the initial-states worktree after preservation and verification. Best-dev and final M0 checkpoints both correspond to update 256 and have identical model tensors, although serialized file hashes differ. Keep both originals intact. Model switching is registry-based with strict architecture, SHA256 and launch-fingerprint checks; it clears the current episode. This is inference/diagnostics only, not a renewed training budget, new corpus admission, holdout use, or permission to load incompatible architectures. Preserve all ignored artifacts and historical code before retiring the checkout; do not push.
